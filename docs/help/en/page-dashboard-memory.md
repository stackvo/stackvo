# Memory

This machine's memory use: used and free.

## Worth knowing

- This is the whole machine's memory, including whatever Docker itself has taken.
- On Docker Desktop the containers run inside a virtual machine. That machine's limit is in Docker's own settings; the figure here is use, not the limit.
- For one project's memory use, see its Indicator tab.
