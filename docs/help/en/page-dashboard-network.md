# Network

Current download and upload rates, with the recent history underneath.

## Worth knowing

- This is the whole machine's network traffic. An image pull or a `composer install` shows up here.
- Traffic between containers flows over Docker's own network and may not reach these counters.
