# CPU history

A graph of recent samples. The figure at the top right is the latest reading.

## Worth knowing

- The graph only holds samples taken while the app was open. Nothing is sampled while it is closed, so there is no gap — the samples simply continue where they left off.
- A short spike is normal; a build or an install fills the CPU. A line that stays high means something is looping in the background.
