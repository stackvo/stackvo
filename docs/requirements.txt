# The documentation site. Pinned exactly: Material for MkDocs is in
# maintenance mode until 5 November 2026 and MkDocs 2.0 is incompatible with
# it, so nothing here should move without a person looking. Dependabot watches
# this file (`.github/dependabot.yml`, the `pip` entry).
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-git-revision-date-localized-plugin==1.4.7
