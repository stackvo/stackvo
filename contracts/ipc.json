{
  "$schema": "https://stackvo.dev/contracts/v1/ipc.meta.json",
  "contractVersion": "2.0.0",
  "description": "The Tauri IPC surface for StackVo Desktop — contract v1. Derived 1:1 from the Express routes in core/ui/server/routes/ and the Socket.io events in core/ui/server/, plus the commands the desktop app needs that no HTTP endpoint ever provided. This file is the agreement between the Vue front end and the Rust core; both sides are generated/checked against it.",
  "conventions": {
    "naming": "snake_case, <domain>_<verb>. The same string is the Rust #[tauri::command] fn name and the JS invoke() key.",
    "envelope": {
      "before": "Express returned { success, data?, message?, meta? } and the axios interceptor unwrapped .data — a 200 with success:false was treated as an error.",
      "after": "Tauri commands return Result<T, StackvoError>. Ok(T) is the payload directly (no envelope). Err(e) rejects the invoke() promise. The success:false-with-HTTP-200 ambiguity disappears.",
      "migration": "client/src/lib/api.js is the ONLY file that changes: its interceptor logic becomes a thin invoke() wrapper that re-throws StackvoError as an Error with .code and .hint attached. Pinia stores are untouched."
    },
    "argCasing": "Rust params are snake_case; Tauri auto-converts camelCase from JS. Send snake_case explicitly to avoid ambiguity.",
    "longRunning": "Any operation that can exceed ~2s (build, compose up, image pull) MUST NOT block on its return value. It returns an OperationId immediately and reports progress via events. This replaces the 600s nginx proxy timeout hack.",
    "idempotency": "start/stop/restart on an already-in-that-state container succeed silently (matches current DockerService behaviour)."
  },
  "errors": {
    "shape": {
      "code": "string",
      "message": "string (user-facing, localised key when possible)",
      "hint": "string?",
      "hintKey": "string? — set when the hint came from the catalogue in src-tauri/src/hints.rs. The frontend renders errorHints.<hintKey> from its locale and falls back to `hint` when the key is absent or untranslated; `hint` always carries the English, which is what the log and the MCP surface get. src-tauri/tests/hint_translations.rs fails the build if a catalogued hint has no translation in en.js or tr.js.",
      "details": "object?"
    },
    "codes": {
      "ALREADY_EXISTS": "project_create against an existing directory.",
      "BUILD_FAILED": "Docker build returned non-zero. `details.log_path` points at logs/build/<service>.log.",
      "CONFLICT": "Port or domain already claimed by another project.",
      "ENGINE_UNREACHABLE": "Docker daemon not running or socket/pipe not accessible. Replaces the ECONNREFUSED stack spam that isDockerUnreachable() papered over. The UI MUST offer engine_start().",
      "FORBIDDEN": "An administrator's policy file forbids this — today, writing a `.env` key listed in its `locked` array. Deliberately distinct from PERMISSION_DENIED, which means the OS refused and can be answered by elevating: this one can never be, so the UI MUST NOT offer a retry. `details.keys` names the settings and `details.source` the policy file, which is the only thing the user can act on. See src-tauri/src/policy.rs.",
      "GENERATE_FAILED": "The generator ran and failed. `details.log` carries stderr.",
      "INVALID_INPUT": "Failed schema validation. `details` carries the field path.",
      "INVALID_MANIFEST": "stackvo.json exists but violates project.schema.json. `details.path` names the offending key.",
      "IO_ERROR": "Filesystem failure.",
      "NETWORK_ERROR": "A host this app had to reach did not answer, or answered badly — the package registry over HTTPS is the only source of this today. Deliberately NOT ENGINE_UNREACHABLE, whose whole point is that the UI offers engine_start(): nothing can be started to fix this one, so a UI offering the same button would be offering the wrong thing. The message always names the URL, because the address is the only thing the person at the keyboard can act on.",
      "NOT_FOUND": "Container, project or service does not exist.",
      "NO_WORKSPACE": "No StackVo directory is selected, or the selected one no longer looks like a StackVo checkout. Desktop-only: the web UI could never hit this because it was mounted inside the repo it managed. The UI MUST offer workspace_pick().",
      "PERMISSION_DENIED": "Elevation refused or unavailable (hosts_apply).",
      "UNSUPPORTED": "Requested runtime/extension/server/platform feature has no implementation in v1 (e.g. runtime=python, extension=pdo_oci, external terminal off macOS)."
    },
    "policy": "No error message may contain a raw shell command line or a value matching the secret redaction regex from env.schema.json."
  },
  "commands": {
    "agents_install": {
      "new": true,
      "kind": "mutation",
      "args": {
        "client": "string",
        "allowWrites": "boolean"
      },
      "returns": "string",
      "why": "Writes the `stackvo` MCP entry into one client's configuration file, so registering the server is a click rather than hand-edited JSON. Returns the path written.",
      "notes": "Reads the file, inserts one key and writes it back — never renders from a template, so every other server and every unknown key survives. Takes a `.stackvo-backup` beside the file first, and writes atomically. `allowWrites` adds `--allow-writes`, which grants that assistant stack_down and project_stop as well as the read tools; it is passed from the UI and defaults to off there. Writes `STACKVO_ROOT` into the entry's env when a workspace is selected. Errors: NOT_FOUND when stackvo-mcp is not on this machine, INVALID_INPUT for an unknown client or a file that is not plain JSON, CONFLICT when the servers key holds something that is not an object. Audited as `agent_install`. Codex is TOML and is edited with toml_edit so comments, key order and quoting survive; the JSON clients keep their own indentation and key order for the same reason. Zed's path is not documented by Zed and differs between ~/.config/zed and ~/Library/Application Support/Zed, so both are looked for and whichever exists is written."
    },
    "agents_remove": {
      "new": true,
      "kind": "mutation",
      "args": {
        "client": "string"
      },
      "returns": "string",
      "why": "The way back out. A registration that can only be added is one people avoid adding.",
      "notes": "Removes only the `stackvo` entry; an empty `mcpServers` left behind is not tidied away, because that would be this app deciding something about a file it does not own. Removing what is not there succeeds, so a second click is not an error. Audited as `agent_remove`."
    },
    "agents_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ binary: string | null, source: 'sibling' | 'build' | 'path' | null, root: string | null, clients: Array<{ id: string, label: string, path: string, present: boolean, exists: boolean, parseable: boolean, command: string | null, current: boolean }> }",
      "why": "The README asked the reader to find their assistant's configuration file, work out its shape and paste a JSON block into it with a path they had to supply themselves. Competitive review K-1 — every rival with an MCP server installs itself. This is the read half: which clients are on this machine, which already point at the server, and whether the binary exists to point at.",
      "notes": "Reads only, and works with no workspace selected — the answer is about the machine, not the stack, and hiding the pane until a folder is chosen would hide it exactly during setup. `binary` is null when stackvo-mcp cannot be found, and then agents_install refuses rather than registering a path that does not exist; `source` says which copy was found. `parseable` false means the file cannot be edited by its own parser — JSON with comments (VS Code's format), or TOML that is halfway through being edited — which is reported rather than rewritten, because stripping comments would delete the user's own notes. Eight clients: six JSON, plus Codex whose file is TOML and Zed whose map is called `context_servers`. IMPLEMENTORS: a client's file MUST come back in its own shape — key order and indentation included. Measured against real files with examples/agent_config_probe.rs, which round-trips an install and an uninstall and compares byte for byte. See src-tauri/src/agents.rs."
    },
    "app_log_open": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string (project)",
        "id": "string (LogFile.id)",
        "tailBytes": "u64? (default 65536)"
      },
      "returns": "StreamId",
      "emits": ["logs:line", "logs:closed"],
      "why": "Follows one of those files. Shares the event pair with container_logs_open because the viewer renders one kind of line; a second pair would have meant a second frontend listener free to drift from the first. Closed with container_logs_close — one registry of abort handles, one way to stop a stream.",
      "notes": "`id` is an opaque `app:` or `server:` handle from app_logs, never a path. Paths do not cross the boundary in either direction: a log viewer that accepts an absolute path from its own frontend is a file reader for the whole disk. The id's components are checked for traversal before the filesystem is touched, and the resolved path is checked against its root again afterwards, because a symlink inside the project can escape with no `..` in sight. Polled twice a second rather than watched — a notification still only says that something changed, so the read-the-delta path exists either way. A file shorter than the reader's offset was truncated or rotated (`> laravel.log`, or a daily channel rolling over) and restarts from the top instead of going silent for ever."
    },
    "app_logs": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string (project)"
      },
      "returns": "LogFile[]",
      "why": "The container stream carries stdout and stderr, which is what the entrypoint and the web server say — not what the application records. A Laravel exception goes to storage/logs/laravel.log, an nginx 502 to the mounted error.log, and a queue worker that died to its own file under supervisord. None of those reach stdout, so none of them were visible anywhere in the app.",
      "notes": "Reads the host filesystem, not the container: the generated compose mounts projects/<name> at /var/www/html and logs/projects/<name> at /var/log, so both roots are already there. No engine required — which is the point, because a container that crashed on boot is exactly when its log is worth reading and exactly when there is nothing left to exec into. Discovery recurses, because Laravel channels nest (storage/logs/parser/parser-2026-07-28.log is a real path from the checkout this was built against), and is capped at depth 3 and 60 files so a stray node_modules cannot turn it into a full-disk walk."
    },
    "app_logs_all": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "ProjectLogFile[]",
      "why": "The per-project viewer answers \"what did this project write\". It cannot answer \"which of my eight projects just errored\", which is the question you have when you do not yet know where to look. Herd sells the cross-project view as a Pro feature.",
      "notes": "A project is a directory under projects/ holding a stackvo.json — an unadopted checkout is somebody's folder, not something this app follows. No engine is consulted anywhere on this path, so with Docker stopped this is the one list in the app that is still complete."
    },
    "app_logs_all_open": {
      "new": true,
      "kind": "mutation",
      "args": {
        "projects": "string[]? (all when omitted)"
      },
      "returns": "FanoutStream",
      "emits": ["logs:line", "logs:sources"],
      "why": "One live tail across every project, so an error surfaces without first guessing which project to open.",
      "returnsNote": "Returns the first scan's coverage rather than emitting it. An event would race the caller — the spawned task can emit before the frontend holds the stream id it filters events by, and the coverage line would then stay blank until the next rediscovery thirty seconds later. logs:sources carries updates only, because only an update has somewhere to arrive.",
      "notes": "**Live only, with no history — a decision, not an omission.** Nothing in this module parses a timestamp (Laravel, nginx and supervisord do not agree on a format), so the only chronology available across sixty files is the order bytes arrive in: real for new output, invented for old. Each file is therefore adopted at its current end and only what follows is shown; history stays in app_log_open, which reads one file and can honestly show all of it. Rediscovery every 30s is the other half — a daily channel rolls over into a filename that did not exist when the tail started, so a fixed file set would go quiet exactly at midnight. Files found by a later scan are adopted at offset zero, because everything in a file that new was written while the tail was already watching. Capped at 60 files, newest first, with what was dropped reported through logs:sources rather than trimmed silently. Closed with container_logs_close — one registry of abort handles, one way to stop a stream."
    },
    "apps_available": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ terminals: App[], editors: App[], browsers: App[] }",
      "notes": "Terminals, editors and browsers present on this machine, each with { id, name, icon, available, default }. Entries that are not installed are returned with available:false rather than omitted, so the picker can grey them out — a list that silently drops them reads as lack of support. At most one entry per list carries default:true — the one that would be used when the preference is unset, which is the first installed entry for terminals and editors and the system-default entry for browsers. Without it the picker was blank on a fresh install while the app happily opened *some* terminal."
    },
    "bootstrap_complete": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {},
      "returns": "void",
      "notes": "Writes the marker that makes workspace_get report bootstrapped:true. Called by the first-run screen after its last step, so a run that failed or was skipped is offered again next launch."
    },
    "catalog_get": {
      "replaces": "GET /api/supported-languages",
      "kind": "query",
      "args": {},
      "returns": "Catalog",
      "v1Change": "Served from the bundled contracts (php-extensions.json + env.schema.json), no longer re-parsed out of .env on every request. Runtimes without generators are marked available:false rather than silently offered."
    },
    "cert_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "installCa": "bool (default true)"
      },
      "returns": "CertPlan",
      "emits": ["certs:changed"],
      "elevation": "conditional",
      "why": "Reissues the wildcard certificate for the domains the projects actually have, and — when the CA is not trusted yet — installs it. Without this the certificate goes stale the moment a project is created, and on Linux and Windows the CA is never trusted at all: the Bash helper's trust step returns early on anything but macOS, so those users get a browser warning and no explanation.",
      "notes": "Trust installation delegates to `mkcert -install` rather than reimplementing four trust stores (macOS keychain, Linux ca-certificates, NSS for Firefox and Chrome, Windows Root). mkcert may need a password for the system store; on Linux it shells out to sudo, which a GUI process has no terminal for. That failure MUST be reported with the exact command to run, not swallowed — a CA that silently failed to install looks identical to one that worked until the browser disagrees. Issuing the certificate itself writes only inside the workspace and never elevates."
    },
    "cert_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "installCa": "bool? (default true)"
      },
      "returns": "CertPlan",
      "why": "Same order as hosts_plan: say what would change before doing it. Reissuing replaces the file Traefik is serving, and `installCa` can touch the system trust store, so neither should be the first thing the user learns about it.",
      "notes": "`domains` is the exact argument list mkcert would be invoked with — the same value cert_apply uses, not a second computation that could differ."
    },
    "cert_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "CertStatus",
      "why": "HTTPS was already working and entirely invisible. `core/cli/utils/generate-ssl-certs.sh` issues an mkcert wildcard covering stackvo.loc and every project domain, and traefik.sh installs it as the default certificate — but nothing in the app could say whether mkcert was installed, whether the CA was trusted, whether the certificate had expired, or which domains it actually covered. A user whose new project shows a browser warning had no way to learn why.",
      "notes": "Reads only: it never runs mkcert and never writes. The SAN list and expiry are parsed out of the certificate itself rather than inferred from the project list, because the file on disk is what the browser validates against — a list of what SHOULD be covered would agree with the certificate right up until it mattered."
    },
    "cert_trust_in_terminal": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {},
      "returns": "void",
      "notes": "Opens the user's terminal running `sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain <ca>` — the write mkcert itself performs, without mkcert's pre-check, which reports the CA already installed whenever it is merely present in a keychain. macOS grants the authorization only interactively, so sudo needs a terminal to ask in."
    },
    "compose_down": {
      "new": true,
      "kind": "operation",
      "args": {},
      "returns": "OperationId",
      "why": "`stackvo down` existed only in the CLI — the web UI could never stop the stack, because doing so would have killed the UI itself."
    },
    "compose_restart": {
      "new": true,
      "kind": "operation",
      "args": {},
      "returns": "OperationId",
      "why": "Same reason as compose_down."
    },
    "compose_up": {
      "replaces": "POST /api/start-with-profile",
      "kind": "operation",
      "args": {
        "mode": "'minimal'|'services'|'projects'|'all'|'custom'",
        "profiles": "string[] (custom only)"
      },
      "returns": "OperationId"
    },
    "compose_up_project": {
      "replaces": "POST /api/start-with-profile/project",
      "kind": "operation",
      "args": {
        "name": "string"
      },
      "returns": "OperationId"
    },
    "container_inspect": {
      "replaces": "GET /api/docker/inspect/:c",
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ContainerDetails"
    },
    "container_logs_close": {
      "new": true,
      "kind": "mutation",
      "args": {
        "streamId": "string"
      },
      "returns": "void"
    },
    "container_logs_open": {
      "new": true,
      "kind": "stream",
      "args": {
        "name": "string",
        "tail": "u32 (default 200)",
        "follow": "bool (default true)"
      },
      "returns": "StreamId",
      "emits": ["logs:line", "logs:closed"],
      "why": "Log following existed only as `stackvo logs` in the CLI. The UI had no way to tail a container."
    },
    "container_stats": {
      "replaces": "GET /api/docker/stats/:c",
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ContainerStats"
    },
    "container_stats_history": {
      "replaces": "GET /api/docker/stats/:c/history",
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "StatSample[]",
      "notes": "Sampler runs at 60s (startStatsSampler). v1 MUST persist history across app restarts — today it lives in memory and dies with the container."
    },
    "containers_restart_all": {
      "replaces": "POST /api/docker/restart-all",
      "kind": "operation",
      "args": {},
      "returns": "OperationId"
    },
    "containers_start_all": {
      "replaces": "POST /api/docker/start-all",
      "kind": "operation",
      "args": {},
      "returns": "OperationId"
    },
    "containers_stop_all": {
      "replaces": "POST /api/docker/stop-all",
      "kind": "operation",
      "args": {},
      "returns": "OperationId"
    },
    "db_dump": {
      "new": true,
      "kind": "operation",
      "args": {
        "service": "string",
        "path": "string"
      },
      "returns": "OperationId",
      "emits": ["db:start", "db:progress", "db:done", "db:error"],
      "why": "Lerd sells snapshots, Laragon sells automatic backups, ServBay sells both; StackVo had no way to take a copy of a database at all. The tools are already inside the container — `mysqldump`, `pg_dump` and `mongodump` ship with the images the stack runs.",
      "notes": "Streams the dump straight from `docker exec` into the chosen file rather than buffering it: a production-sized database does not fit in a UI process's memory, and the failure would arrive as an out-of-memory kill with no explanation. The password is passed through the child's environment and named — not valued — on the docker command line, so it does not appear in `ps` output on a shared machine."
    },
    "db_restore": {
      "new": true,
      "kind": "operation",
      "args": {
        "service": "string",
        "path": "string"
      },
      "returns": "OperationId",
      "emits": ["db:start", "db:progress", "db:done", "db:error"],
      "why": "A backup nobody has restored is a file, not a backup.",
      "notes": "DESTRUCTIVE: this replaces the contents of the target database. The UI MUST name the database and require confirmation before calling it. Reads the file into the container's stdin, the mirror of how the dump was written."
    },
    "db_snapshot_delete": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "name": "string"
      },
      "returns": "void",
      "why": "Removes one copy. The way out of a directory that would otherwise only grow.",
      "notes": "Missing is success, so a second click is not an error. Deliberately not audited: this deletes a copy, and the act that has to be accounted for is the restore, which is."
    },
    "db_snapshot_restore": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "name": "string"
      },
      "returns": "string",
      "why": "Puts a named snapshot back, replacing what is in the database.",
      "notes": "The same restore db_restore performs, so it is audited as `db_restore` with the snapshot's path. NOT_FOUND when the file is gone — the listing is the filesystem, so a snapshot deleted outside the app is simply absent rather than a broken index entry."
    },
    "db_snapshot_take": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "name": "string"
      },
      "returns": "string",
      "why": "Takes a named copy, into a path the app owns. Returns the operation id; progress and completion arrive on the same db:* events db_dump uses.",
      "notes": "The name is refused rather than sanitised when it is not usable as a filename — silently turning `../escape` into something else gives back a snapshot under a name the user did not choose and will not find. `auto-` is reserved so nobody can create a snapshot the scheduler would later prune. An existing name is ALREADY_EXISTS, never an overwrite: a snapshot silently replaced is the copy somebody thought they still had. Serialised per service against db_dump and db_restore."
    },
    "db_snapshots": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Array<{ service: string, name: string, path: string, bytes: number, takenAt: string, automatic: boolean }>",
      "why": "Competitive review G-2: `ddev snapshot` and `lerd db:snapshot` name a point in time and restore it by that name. db_dump could already write a file to a path chosen in a save dialog, which is raw material — a dump in Downloads is not something anybody comes back to.",
      "notes": "The registry IS the directory: <root>/backups/<service>/, the name is the file stem, and takenAt is the file's modification time. No index file, because a second answer to 'what snapshots exist' drifts the first time somebody deletes one in Finder, and the recovery is a restore naming a file that is not there. Only files with the engine's own extension are listed. `automatic` marks the ones the schedule took — the only ones retention may remove. See src-tauri/src/snapshot.rs."
    },
    "db_targets": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "DbTarget[]",
      "why": "Which database services can be dumped, what they are called, and whether they are running. The credentials were already read out of .env and rendered in the services list; what was missing was anything to do with them.",
      "notes": "Reads .env and the engine. A service that is enabled but not running is still listed, with `running: false` — the reason a dump cannot start right now is worth showing next to the button that is disabled because of it."
    },
    "debug_bridge_clear": {
      "new": true,
      "kind": "command",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "notes": "Removes the file rather than truncating it: the container may write as a different user, and a host process can always delete an entry from a directory it owns even when it cannot write the file."
    },
    "debug_bridge_events": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "since": "number?"
      },
      "returns": "{ total: number, events: Event[] }",
      "notes": "A cursor, not a stream: the producer is a container appending newline-delimited JSON to a file, and there is nothing to subscribe to. Events are only appended, so everything past `since` is new and a missed poll costs nothing. A `since` past the end means the file was cleared or rotated underneath the caller, and the answer starts again rather than being empty for ever. Each Event carries { at, kind, label?, file?, line?, request?, sapi?, value } — `kind` exists so queries and jobs need no second file when they arrive."
    },
    "debug_bridge_overview": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ project: string, enabled: boolean, mounted: boolean, running: boolean, events: number }[]",
      "why": "The per-project pane cannot answer \"which of my eight projects just dumped something\" — the question you have before you know which project to open. It is the same argument that made the log viewer a page, and it is what lets that page poll only the projects worth polling.",
      "notes": "One row per PHP project the bridge can serve; a Node project is not a place a dump can come from and is not listed. Reads files plus one container inspection each — no compose, no engine required for `enabled`, which is a file on the host and true whether or not Docker is running."
    },
    "debug_bridge_set": {
      "new": true,
      "kind": "command",
      "args": {
        "name": "string",
        "enabled": "boolean"
      },
      "returns": "void",
      "why": "Catching dumps used to cost a container recreate, because the mechanism was two environment variables and a container's environment is fixed at creation. Every debugging session started with a wait.",
      "notes": "Creates or removes one file in a directory the container already has mounted. No compose command, no recreate, no worker cycle. The bridge re-reads the flag on every request AND inside every emit, so a long-running queue worker stops writing the moment it is switched off rather than at its next restart."
    },
    "devserver_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "enabled": "bool",
        "command": "string? (defaults to the detected npm script)"
      },
      "returns": "DevServerStatus",
      "emits": ["devserver:changed"],
      "notes": "State is `projects/<name>/.stackvo/devserver.json` — a file rather than a manifest key, because the manifest schema is additionalProperties:false and because a file under .stackvo/ is commit-friendly: a teammate's clone gets dev mode with everything else. Its existence is the switch; its contents are the command. Node only; a PHP project is refused, and the overlay renderer refuses again for a config file dropped in by hand, since /app does not exist there and mounting over it would replace the site with an empty directory. The overlay is layered LAST of the six compose files, because it is the only one that overrides rather than adds — anything after it would merge onto a service already in a different mode."
    },
    "devserver_status": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "DevServerStatus",
      "why": "P2-15 was written as \"proxy the Node dev server through Traefik with HMR\", which sounds like a routing change. Reading node.sh against the other five server generators turns up something larger: **a node project has no bind mount at all**. nginx/caddy/apache/swoole/frankenphp all call generate_common_volumes; node.sh calls nothing, and its Dockerfile does `COPY . .` and `RUN npm install` at build time. The container holds a SNAPSHOT of the source. Hot reload was not misconfigured — it was structurally impossible, and no amount of WebSocket plumbing helps when there is nothing to reload.",
      "notes": "Three things have to be true at once, reported separately because they come apart and each has a different fix. (1) The source is live — a bind over /app plus an ANONYMOUS VOLUME on /app/node_modules, without which the bind shadows the install the image did for its own platform, or worse, exposes the host's macOS build of the same native packages. (2) The dev server is what is running — `npm run dev`, not the manifest's production start command. Both are the overlay's job. (3) The dev server accepts the request, and that one is in the USER'S repository. Read out of Vite's own source (hostValidationMiddleware, verified against the copy in this repo's node_modules): a Host header that is not localhost, not *.localhost, not an IP and not matched by server.allowedHosts gets a flat 403 — so a .loc domain presents as a site that is plainly up and returns 403, with nothing pointing at the dev server's config. The HMR client is the second half: StackVo's node router is websecure-only with tls=true, so the page loads over 443 while the dev server listens on 3000, and Vite's client would dial wss://domain:3000, which nothing routes, and degrade silently to full reloads. `snippet` is the exact config with the project's domain already in it. It is GENERATED, NEVER WRITTEN: silently rewriting somebody's build config is not a thing a local environment manager should do, and a snippet they paste is one they have read."
    },
    "diagnostics_bundle": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "{ path: string, bytes: number, entries: Array<{ name: string, bytes: number, truncated: boolean }> }",
      "why": "Settings could open the log folder and nothing more, which leaves the reporter to find the right file among seven daily ones, know that the doctor output is separate, and remember the version and platform. One archive gets the maintainer the same set every time.",
      "notes": "path comes from the system save dialog, as mail_attachment_save's does — the front end names no destination this process did not receive from the user. Contents: about.txt, preflight.json, doctor.json, engine.json, the rotating log and any crash reports. Every log line is passed through logging::redact a second time on the way in, because a bundle built today may hold lines written by a build whose redaction rule was narrower. Logs are capped at 1 MiB each and `truncated` says which were cut. Nothing is read from .env and no project source is included."
    },
    "docker_disk_usage": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "DiskOwner[]",
      "why": "docker_system_resources reports totals; the question a full disk raises is *which project*. Every StackVo container with its image size and writable layer, plus stack-built images whose container is gone — the bytes nobody remembers spending. No native-binary competitor can ship this at all.",
      "notes": "imageDedicated separates images this stack built (stackvo-<name>, freed by removing the member) from shared upstream images (mysql:8.0, freed by nothing short of docker rmi). Sorted largest first."
    },
    "docker_prune": {
      "new": true,
      "kind": "mutation",
      "args": {
        "images": "bool",
        "volumes": "bool",
        "buildCache": "'keep' | 'dangling' | 'all' (default 'keep')"
      },
      "returns": "PruneReport",
      "why": "SystemResources has reported reclaimable bytes since Phase 1 with no way to act on the number. Dangling images accumulate on every rebuild, and this app rebuilds a lot — every Xdebug toggle, every extension change.",
      "notes": "Images means dangling images only (the engine's default prune set — rebuildable by definition). Volumes are a separate flag because the engine's \"unused\" means \"not currently mounted\", and the database of a stopped project qualifies; the UI warns before passing volumes:true. buildCache is a level rather than a flag because its two useful values are different bargains: 'dangling' reclaims only records nothing references any more — what project_delete already does for itself after removing the project's image, since Docker offers no per-project handle on the cache (the filters are until/id/parent/type) — while 'all' also takes the layers every project image shares, because they all build from the same PHP base and the same extension installs. 'all' costs no data and costs every remaining project a full rebuild; the UI states that before passing it. Guarded by the 'prune' inflight subject."
    },
    "docker_system_resources": {
      "replaces": "GET /api/docker/system",
      "kind": "query",
      "args": {},
      "returns": "SystemResources"
    },
    "doctor": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Doctor",
      "why": "The boot gate answers \"can the app run\"; this answers the failures that arrive later, one failed compose up at a time. The single most common one — a host port already taken — compose reports as \"address already in use\" with no word on by what. The doctor names the culprit: the stack's own container (fine), someone else's container (named), or a host process (named, with pid).",
      "notes": "Diagnosis only. Every repair stays behind its own command — preflight_fix, hosts_apply (behind its reviewed diff), generate_run, compose_up, docker_prune — so no confirmation flow is bypassed. Degrades honestly: no workspace ⇒ port and generated checks read unknown; engine down ⇒ space is null, port owners cannot be named, and core containers read unknown rather than failed. `core` lists the containers in the compose `core` profile (Traefik today), read from generated/stackvo.yml rather than from a name hardcoded in Rust, each with { service, container, state, exists, running, image }. `exists` is separate from `running` because compose down removes containers: \"stopped\" and \"never created\" arrive from different actions and want different repairs, and reporting both as not-running sent people looking for a stopped container that was not there."
    },
    "doctor_drop_extension": {
      "new": true,
      "kind": "mutation",
      "args": {
        "subject": "string (project name, or `.env`)",
        "extension": "string"
      },
      "returns": "Doctor (re-run)",
      "emits": ["manifest:changed"],
      "why": "The doctor's design rule is a repair next to every finding — hosts get the reviewed diff, stale output gets regenerate, disk gets prune. The extension check shipped without one, which left the panel naming a problem and offering nothing.",
      "notes": "**Removing the extension changes nothing about what runs, and that is the point.** The generator already drops one it cannot install — silently, which is the bug CONFLICTS.md C-06 records — so it is already absent from every built container. This does not remove a capability; it stops the manifest claiming one the container never had. No alternative is offered because the catalog leaves none: `imap` is `install: core` with `removedIn: 8.2` and no PECL package, so on 8.2+ there is nothing StackVo could install instead. If the project genuinely needs it the answer is an older PHP version, which is the project owner's decision and not a button. Writes through the paths the rest of the app uses — manifest::write for a project, env_writer for .env, which backs the file up and serialises against the other writers. Removing an extension that is already gone is a no-op rather than an error: the state the caller wanted is the state on disk."
    },
    "engine_start": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "void",
      "why": "Launch Docker Desktop (macOS/Windows) or `systemctl --user start docker` (Linux). Resolves the chicken-and-egg problem end to end.",
      "notes": "Best-effort. Returns Ok as soon as the launch is issued; the caller waits on engine:status_changed."
    },
    "engine_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "EngineStatus",
      "why": "There was NO endpoint for this. The web UI could not report a stopped Docker daemon because the UI itself was a container that could not start. This is the single most important new command — it is what makes the desktop app usable when Docker is down.",
      "polling": "Called on startup and on a 5s timer while disconnected; emits engine:status_changed on transition."
    },
    "env_defaults": {
      "replaces": "none — new in the desktop app",
      "kind": "query",
      "args": {},
      "returns": "map<string,string>",
      "notes": "The values compiled into the binary. Carries no secret and needs no workspace: it describes the app, not a checkout. The settings form uses it to mark a value as a default and to offer going back to one."
    },
    "env_get": {
      "replaces": "GET /api/env",
      "kind": "query",
      "args": {},
      "returns": "map<string,string>",
      "notes": "MUST redact secret-matching keys unless `reveal: true` is passed and the user has confirmed in-session."
    },
    "env_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "patch": "map<string,string>"
      },
      "returns": "void",
      "why": "Settings.vue is a 24-line stub because .env was read-only over HTTP. Writing MUST preserve comments, key order and blank lines — a naive rewrite destroys a 159-key hand-annotated file."
    },
    "generate_run": {
      "new": true,
      "kind": "operation",
      "args": {
        "scope": "'all' | 'projects' | 'services'"
      },
      "returns": "OperationId",
      "emits": ["generate:start", "generate:progress", "generate:done", "generate:error"],
      "why": "`stackvo generate` was only ever invoked as a side effect of project_create / service_enable, via execAsync on a shell string. Making it a first-class command is what lets the fs-watcher regenerate automatically.",
      "notes": "Since the takeover, this writes with the Rust generator directly — no shell is spawned, and bash is no longer a generation prerequisite."
    },
    "generate_with": {
      "new": true,
      "kind": "operation",
      "args": {
        "scope": "'all'|'projects'|'services'",
        "engineMode": "'verify'|'rust' (default rust; 'bash' answers Unsupported)"
      },
      "returns": "{ operationId, engine, report?, note? }",
      "why": "Was the staged handover: Bash ran in every mode and `rust` refused to write on any byte difference. The takeover completed at 28/28 files identical on real data, the Bash engine was retired, and the Rust generator is the only writer.",
      "notes": "Kept for headless callers; the UI's engine selector is gone because there is no longer an engine to select. `verify` survives as a drift check — does the disk still hold what the generator would write — which catches hand-edited generated files. generate_run is the plain write path.",
      "rustInternal": true
    },
    "generator_verify": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "GeneratorReport",
      "why": "The migration gate. A fixture suite only covers the cases someone thought to write down; this renders every generated file with the Rust port against the user's real projects and real .env, so a divergence surfaces on their machine before anything depends on it.",
      "notes": "Reads only — it never writes a generated file. `readyToTakeOver` is true when every file matches byte-for-byte."
    },
    "git_available": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "boolean",
      "why": "The clone option is hidden without it. A webview cannot answer \"is a program installed\", and the answer has to survive an app launched from the Dock, which inherits launchd's PATH rather than a login shell's — the same class of fact that made $LANG useless for locale detection.",
      "notes": "Checks PATH and then the usual absolute locations. Apple's git at /usr/bin/git is on both; a Homebrew git at /opt/homebrew/bin is on neither in a packaged app, and a PATH-only check would tell a user with git that they have none."
    },
    "handover_apply": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "HandoverPreview",
      "why": "Write the instance table from `.env`, once.",
      "notes": "Recomputes the plan rather than accepting one: a plan decides ports against the machine as it was, and a stale preview could claim a port something else has since taken. `.env` is copied to `.env.pre-market.bak` BEFORE anything is written and its service keys are marked rather than removed, so the revert is deleting services/instances.json. Refuses when a table already exists — a second run would adopt the same volumes twice."
    },
    "handover_preview": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "HandoverPreview",
      "why": "The one migration that touches a workspace somebody is already using. handover.rs is built plan-then-apply so the reasons survive; a UI with only the apply half would have thrown them away.",
      "notes": "Pure: computes the plan and writes nothing. `blockers` non-empty means the handover is refused as a whole — a version enabled in `.env` and absent from the catalogue is never migrated to its nearest neighbour, because that is an unrequested upgrade performed on a datadir. `missing` names what to install to unblock it: a preview that only refused was a dead end with the answer one page away and unnamed."
    },
    "host_stats": {
      "replaces": "GET /api/docker/stats",
      "kind": "query",
      "args": {},
      "returns": "HostStats",
      "v1Change": "Backed by the `sysinfo` crate on the host instead of reading /proc from inside a container. The 293-line HostStatsService with its macOS fallbacks collapses to a direct, correct read — this is the clearest early win of the port."
    },
    "hosts_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "add": "string[]",
        "remove": "string[]"
      },
      "returns": "HostsPlan",
      "elevation": "required",
      "why": "Removes the manual `sudo tee -a /etc/hosts` step from the README. MUST show a diff and require explicit confirmation before elevating; MUST back up the file; MUST only touch lines inside a StackVo-managed marker block.",
      "notes": "Writes the previewed text via the OS elevation prompt (osascript / pkexec / UAC). Only lines inside the `# >>> stackvo >>>` marker block are rewritten; everything else in the file is byte-identical. A cancelled prompt is PERMISSION_DENIED, not a crash."
    },
    "hosts_missing": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "string[]",
      "why": "Every project domain with no hosts entry. Drives the one-click fix; the web UI could detect this but had no way to act on it."
    },
    "hosts_missing_core": {
      "replaces": "none — new in the desktop app",
      "kind": "query",
      "args": {},
      "returns": "Array<string>",
      "notes": "Only <suffix> and traefik.<suffix> — the two names the stack is addressed through, and the set the preflight gate blocks on. hosts_missing is the everything list, for the dashboard's fix-all banner."
    },
    "hosts_overview": {
      "replaces": "none — new in the desktop app",
      "kind": "query",
      "args": {},
      "returns": "HostsOverview",
      "notes": "Every domain the stack serves with its hosts state, plus entries inside StackVo's own block that nothing wants any more. MUST offer only StackVo-managed lines as stale: a hand-written entry belongs to whoever wrote it."
    },
    "hosts_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "add": "string[]",
        "remove": "string[]"
      },
      "returns": "HostsPlan",
      "why": "Computes the exact replacement text WITHOUT elevating, so the UI can show a diff and ask first. Elevating and explaining afterwards would be the wrong order for the one operation that touches a system file."
    },
    "projects_idle": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "IdleProject[]",
      "why": "I-2. Reports the idle time even with the threshold off — the number is worth seeing before somebody chooses what to set it to."
    },
    "projects_suspend_idle": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "string[] — the projects stopped",
      "why": "Returns NAMES, not a count: this is a background action whose whole risk is being surprising, and “3 suspended” is a number somebody then has to match against a list. MUST skip a project something else holds rather than queueing behind it — a sweep that waited behind a build would stop the container the moment the build finished."
    },
    "db_instances": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "DbInstance[]",
      "why": "G-4. A move names two instances, and `mysql-8-0` and `mysql-8-4` both answer to `mysql`."
    },
    "db_move_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "from": "string",
        "to": "string"
      },
      "returns": "DbMovePlan",
      "why": "G-4. Plan-then-apply, the same shape as hosts_plan/hosts_apply, because the target is EMPTIED and that sentence has to be on screen before anybody presses anything. MUST touch no container."
    },
    "db_move_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "from": "string",
        "to": "string"
      },
      "returns": "DbMoved",
      "why": "MUST re-check the plan rather than trusting the caller's — the plan crossed the IPC boundary and came back. MUST hold BOTH instances busy: a move reads one database and replaces another, and either being started or dumped underneath it is a torn result nobody could explain. Goes through a file rather than a pipe, and the file is KEPT on failure with its path in the error — at that moment it is the only copy of the source outside a container and the target is already replaced."
    },
    "routes_list": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "UserRoute[]",
      "why": "E-4. A route the renderer skips MUST still appear here, or the screen shows a route the proxy does not have."
    },
    "routes_save": {
      "new": true,
      "kind": "mutation",
      "args": {
        "routes": "UserRoute[]"
      },
      "returns": "UserRoute[]",
      "why": "Replaces the whole list and regenerates. Whole-list rather than add/remove/edit: the file is a handful of pairs in one table, and three commands over one small document is three chances for the file and the screen to disagree about order. MUST check every route before writing any, so one bad row fails the save rather than writing half of it, and MUST refuse a duplicate domain — two routers on one name is a coin toss the user cannot see."
    },
    "dns_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "DnsStatus",
      "why": "E-1. /etc/hosts needs a password per project and cannot express a wildcard at all, which is why E-2 was left half-done."
    },
    "dns_start": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "DnsStatus",
      "why": "Binds 127.0.0.1 on a high port — 53 needs root at every start and does not need to, because macOS resolver files take a `port` directive. MUST be idempotent for the same suffix and MUST restart for a different one: a responder left serving the old TLD answers for names nothing renders and refuses the ones it does."
    },
    "dns_stop": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "DnsStatus",
      "why": "Stops and waits for the thread, so the socket is actually free afterwards."
    },
    "dns_resolver_install": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "DnsStatus",
      "why": "Writes /etc/resolver/<tld> with a password. Deliberately separate from dns_start, the same separation hosts_plan/hosts_apply has: one is a socket this app owns, the other changes how the whole machine resolves names, and folding them into one button would make a password prompt appear from something that reads like turning on a feature."
    },
    "dns_resolver_remove": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "DnsStatus",
      "why": "Turning it off has to be as easy as turning it on. MUST undo the reload as well as the file — a drop-in removed from a dnsmasq nobody told is a machine still asking a port that may be gone."
    },
    "dns_check": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "DnsCheck",
      "why": "dns_status reads files and sockets this app owns, which cannot answer the only question a user has: does THIS MACHINE resolve a name under the suffix. Reading back a resolver file proves a write happened. So this asks the responder over UDP and TCP, asks the machine through getaddrinfo, and asks whether public names still resolve — four answers because the repair for each is different."
    },
    "hosts_status": {
      "new": true,
      "kind": "query",
      "args": {
        "domains": "string[]"
      },
      "returns": "HostsEntry[]",
      "why": "readHostsFile() could only READ a read-only mount and report isDomainConfigured. Same capability, but now it feeds a fix action.",
      "notes": "Rust-internal: projects_list uses it to fill `domainConfigured`. Deliberately not wrapped for JS — the list payload already carries the answer, and a second round trip is a way for the two to disagree.",
      "rustInternal": true
    },
    "imports_scan": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Array<{ source: 'xampp' | 'laragon' | 'mamp' | 'valet' | 'sail', path: string, sites: Array<{ name: string, path: string, domain: string | null, bytes: number, partial: boolean, detected: Detected, taken: boolean, services?: string[] }> }>",
      "why": "Competitive review §L. XAMPP has been frozen on PHP 8.2 since 2023 and lost its add-on ecosystem in September 2025; Laragon went commercial in 2025 and was forked. Those are the two largest installed bases in the category and every rival is courting them — EnvKit imports Laragon in bulk, ForgeKit lists six sources. StackVo could read neither.",
      "notes": "Reads only, and works with no workspace selected. `taken` says a directory of that name is already under projects/, so the list can say so before anything is clicked. `bytes` is measured because importing copies, and a size caps out at 20,000 entries with `partial: true` rather than walking a node_modules to the last byte on a page that lists eleven sites. XAMPP's own dashboard directories are excluded. Laragon's per-site vhost supplies `domain`; XAMPP serves by path and has no name to read, so it is null and adoption asks. See src-tauri/src/imports.rs. Five tools, in three shapes. XAMPP, Laragon and MAMP keep one directory of sites. Valet keeps none — it PARKS directories (every child is a site) and LINKS individual ones as symlinks — so it is read from its own config.json, tld included. Sail is not an installation at all: it is a composer package inside a project, identified by a docker-compose.yml that names laravel/sail, which is why it never appears in this scan and is reachable only through imports_scan_at. `services` is populated for Sail alone, from the compose file's own service list mapped onto this app's catalogue (pgsql→postgres, mongodb→mongo); a service with no counterpart is left out rather than substituted, and every other source leaves the field empty because their sites do not state what they need."
    },
    "imports_scan_at": {
      "new": true,
      "kind": "query",
      "args": {
        "source": "'xampp' | 'laragon' | 'mamp' | 'valet' | 'sail'",
        "path": "string"
      },
      "returns": "Install | null",
      "why": "The same, for an installation somewhere this app did not think to look — the well-known paths are installer defaults and people move things.",
      "notes": "null when the directory holds no htdocs (XAMPP) or www (Laragon), which is the honest answer for 'that is not one of those'. INVALID_INPUT for a source name that is not xampp or laragon. MUST accept every source imports.rs can read. It accepted two of the five, so somebody whose MAMP was not in /Applications — or who wanted a Valet or Sail project, neither of which a scan can find — was told the tool was not one this app can read. For Sail the path may be the project OR a directory holding several: both are what somebody means by pointing at it."
    },
    "imports_take": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string",
        "name": "string",
        "move": "boolean"
      },
      "returns": "string",
      "why": "Brings one site into the workspace. The generator bind-mounts ${PROJECTS}/<name>, so a project lives under the projects directory or it does not exist — an import is a file operation, and this is that half only.",
      "notes": "The manifest is NOT written here: the caller follows with project_adopt, so an imported project goes through the same validation, the same domain question and the same name rules as any other adoption. Copies by default. `move` copies first and removes the original only once the copy is complete — the reverse order turns a full disk into a site that exists in neither place — and a failure to remove is logged rather than rolled back, because deleting a correct copy to honour the word 'move' destroys the half that worked. Nothing is ever written into the other installation. A half-finished copy is removed: adoption would otherwise find it and produce a project missing files nobody can name. Errors: ALREADY_EXISTS when the name is taken, NOT_FOUND for a path that is not a directory, INVALID_INPUT when the projects directory is inside the site being imported. Audited as `project_import`."
    },
    "instance_apply_settings": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string",
        "patch": "Record<string, string>",
        "ports": "Record<handle, number>? — optional, null when nothing moved"
      },
      "returns": "OperationId",
      "why": "Write an instance's settings and rebuild its container with them. Replaces service_apply_settings, which wrote them to .env.",
      "notes": "MUST reject keys the manifest does not declare, MUST reject a masked value being written back, and MUST reject emptying a setting the manifest marks required. `enabled`, `primary` and `version` are not settings and are not reachable here — each has its own command, and two controls for one field is how they come to disagree. Ports are the deliberate exception: they arrive as their own argument rather than in `patch`, because they are not settings, and they arrive *here* rather than through a command of their own because both need the container stopped and recreated — two commands would do that twice for one press of one button. A port is refused when the manifest does not declare the handle, when another instance holds the number, when the machine is already listening there, or when the patch would publish one number under two handles. The rebuild is the point: `instance_restart` keeps the environment the container was created with, so a setting saved and then restarted appears to have been applied and has not."
    },
    "instance_create": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "version": "string",
        "settings": "Record<string, string>? — what the create form collected; null creates with the package's defaults",
        "ports": "Record<handle, number>? — null allocates every port"
      },
      "returns": "string",
      "why": "Create an instance of an installed package. Returns its id — the slug of service@version, which every derived name comes from.",
      "notes": "A supplied secret is written to the keystore before the container has ever run, which is the only moment an image reads one. A requested port is used or refused, never moved: the allocator's job is to find *a* free port, and handing back 3407 to somebody who typed 3307 is a different thing. Unrequested ports allocate as before."
    },
    "instance_disable": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string"
      },
      "returns": "OperationId",
      "emits": ["instance:disabling", "instance:disabled", "instance:error"],
      "why": "Switch an instance off. NOTHING IS DELETED (ADR 0012) — not the volume, not the image.",
      "notes": "service_disable deletes all three, and in a single-version world that was right: 'off' should be a state rather than a label. It stops being right per version. Somebody switching MySQL 8.0 off to try 9.4 wants 8.0's rows when they switch back. Deleting lives behind instance_remove and market_uninstall, where the word on the button matches what happens."
    },
    "instance_enable": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string"
      },
      "returns": "OperationId",
      "emits": ["instance:enabling", "instance:progress", "instance:enabled", "instance:error"],
      "why": "Write the instance down as on, regenerate, then bring its compose profile up. The order is the one service_enable uses: the compose file has to describe the container before compose is asked to start it.",
      "notes": "The hosts entry is keyed on the SERVICE, not the instance — a service with a domain declares instancing.multiple:false, so there is exactly one instance of it. Per-instance subdomains are a separate, smaller job."
    },
    "instance_list": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "InstanceRow[]",
      "why": "What is installed, how many times, and which one answers to the pre-package name.",
      "notes": "`packagePresent` is false when the instance names a package that is not installed. That is a state a user has to see, not an error that hides the row."
    },
    "instance_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "service": "string",
        "version": "string"
      },
      "returns": "InstancePlan",
      "why": "What creating this instance would produce, before it does — so a first-boot password can be chosen at the one moment an image will read it.",
      "notes": "Writes nothing and reserves nothing. The ports are what the allocator would choose right now; `instance_create` allocates again for real, because between the two something else may have taken one. A port the allocator cannot place comes back null rather than failing the call, so the form can name it and let the user choose. `refused` is reported rather than thrown, so a single-instance service opens a dialog that says why instead of no dialog at all. A secret's value here is the manifest's published first-boot default and is NOT masked: there is no instance and no keystore entry, and masking a string that is in a file on disk would be theatre."
    },
    "instance_promote": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "why": "Move the pre-package name — `stackvo-mysql` — to another version of the same service.",
      "notes": "One write, not two. The state between 'clear that one' and 'set this one' is a table with no primary, and if the process dies there every project pointing at the old name resolves nothing. Two instances holding one alias is refused: Docker answers at random, and the symptom is 'it connects to the wrong database sometimes'."
    },
    "instance_remove": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "why": "Forget an instance. Volumes are left alone (ADR 0012).",
      "notes": "If the removed instance was primary, the oldest survivor takes the alias. A service with instances and no primary is one whose pre-package name resolves to nothing, and every project pointing at it breaks at once."
    },
    "instance_restart": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "emits": ["instance:restarting", "instance:restarted", "instance:error"],
      "why": "The container, without touching what the instance table says. Distinct from enable/disable, which change whether the instance is part of the stack at all.",
      "notes": "Goes through the same lifecycle helper as project_* and service_*; the id is validated against the instance table first, because it is not in the service catalog and never will be."
    },
    "instance_reveal": {
      "new": true,
      "kind": "query",
      "args": {
        "id": "string",
        "key": "string"
      },
      "returns": "string",
      "why": "The real value behind a masked secret, for the one instance and the one key.",
      "notes": "Returns the manifest's default when the keystore holds nothing, because that is what the container is running with — a reveal showing an empty box for a service reachable with `root` would be a lie of omission. Errors: NOT_FOUND for a key the manifest does not declare, INVALID_INPUT for a key that is not a secret."
    },
    "instance_settings": {
      "new": true,
      "kind": "query",
      "args": {
        "id": "string"
      },
      "returns": "InstanceSetting[]",
      "why": "What one instance is configured with, as its manifest declares it. Replaces service_settings, which read SERVICE_<ID>_* keys out of .env — a name that identifies a service, of which two versions can be running.",
      "notes": "Order is the manifest's, not sorted: a package author who put the database name above the password meant that. Values resolve the way render::context resolves them — what is stored, then the manifest default — because a form that showed one value while the compose file rendered another would be a form about nothing. Secrets are masked without consulting the keystore, so the mask is the same length for a set password and an unset one. `options` is an offer, not a constraint."
    },
    "instance_start": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "emits": ["instance:starting", "instance:started", "instance:error"],
      "why": "The container, without touching what the instance table says. Distinct from enable/disable, which change whether the instance is part of the stack at all.",
      "notes": "Goes through the same lifecycle helper as project_* and service_*; the id is validated against the instance table first, because it is not in the service catalog and never will be."
    },
    "instance_stop": {
      "new": true,
      "kind": "operation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "emits": ["instance:stopping", "instance:stopped", "instance:error"],
      "why": "The container, without touching what the instance table says. Distinct from enable/disable, which change whether the instance is part of the stack at all.",
      "notes": "Goes through the same lifecycle helper as project_* and service_*; the id is validated against the instance table first, because it is not in the service catalog and never will be."
    },
    "licences_notice": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "string (markdown)",
      "why": "MIT, BSD, ISC and Apache-2.0 all require the copyright notice and the licence text to travel with the software. A NOTICE.md in a source repository does not reach the person who received a .dmg, and pointing them at the repository is the same answer as not shipping it.",
      "notes": "Returns the generated NOTICE.md verbatim — markdown covering every Rust crate and npm package that ships, resolved for all platforms at once so one notice describes every bundle. include_str! rather than a bundle resource: a resource is a run-time path that can resolve to nothing, and an app that silently ships no attribution is the failure this closes. Generated by tools/generate-notice.mjs from Cargo.lock and package-lock.json; `npm run notice:check` fails the build when a shipped dependency is missing from it."
    },
    "locale_get": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "\"en\" | \"tr\"",
      "why": "The window and the tray have to open in the same language, and neither could read the machine's on its own. The tray fell back to $LANG, which a Finder-launched app does not have; the window fell back to navigator.language, which in a WKWebView answers from the bundle's localised resources — this app ships none. Both defaulted to English on a Turkish machine.",
      "notes": "Resolution order lives in `locale.rs` and is one order for both surfaces: the choice in preferences.json, then the OS (AppleLanguages/AppleLocale on macOS, HKCU Control Panel\\International on Windows, LC_ALL/LC_MESSAGES/LANG elsewhere), then English. Only ever returns a language the app has strings for — a near miss like pt-BR resolving to a half-translated UI is worse than an honest English one. The front end must NOT write the answer back to preferences.json: that would turn a detection into a stored choice and freeze the first machine's language for ever."
    },
    "locale_packs": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Array<{ tag: string, label: string, path: string, strings: number, broken: string | null }>",
      "why": "The language set used to be a constant in three places — locale.rs, i18n/index.js and the tray's fallback table — so a third language meant a source change and a rebuild. Nobody who can actually translate this app can do any of that, which is why M-7 sat on the list as '~2,000 strings': the strings were never the blocker, the rebuild was.",
      "notes": "A pack that does not parse is LISTED WITH ITS ERROR rather than skipped — a hand-edited file with a trailing comma that simply vanishes from the picker is the worst failure this could have. A pack names itself through `language.label`, because this app cannot hold a name for a language it has never heard of. The tag is validated as a BCP 47 language rather than accepted, because it becomes a file name in a directory this app writes into. M-7. Adding a language stops being a code change: a pack is one JSON file in the app's config directory with the same shape as the shipped catalogue. The front end merges it over English and registers it, so an untranslated screen is in English rather than showing raw keys, and vue-i18n's per-key fallback makes a partial pack a working pack — which is what makes starting one possible at all. NO machine translation and no seeding a new pack with English silently relabelled: a missing string that falls back to English is honest, a fabricated one is a sentence somebody has to find and disbelieve."
    },
    "locale_pack_read": {
      "new": true,
      "kind": "query",
      "args": {
        "tag": "string"
      },
      "returns": "object",
      "why": "The messages, for the front end to merge over English.",
      "notes": "INVALID_INPUT for a tag that is not a language tag, and for a file that is not JSON. Measured with examples/locale_pack_probe.rs, which writes a pack into the real config directory, lists it, reads it back, breaks it on purpose to check the error is reported, and removes it."
    },
    "locale_pack_write": {
      "new": true,
      "kind": "mutation",
      "args": {
        "tag": "string",
        "messages": "object"
      },
      "returns": "string (path)",
      "why": "How 'start a translation' works: the front end sends the English catalogue as the starting point.",
      "notes": "The messages come from the front end because the front end is where the catalogue lives — asking Rust to produce a template would mean a second copy of every string in this app, which is the duplication trayLabels already exists to undo. Written atomically. M-7. Adding a language stops being a code change: a pack is one JSON file in the app's config directory with the same shape as the shipped catalogue. The front end merges it over English and registers it, so an untranslated screen is in English rather than showing raw keys, and vue-i18n's per-key fallback makes a partial pack a working pack — which is what makes starting one possible at all. NO machine translation and no seeding a new pack with English silently relabelled: a missing string that falls back to English is honest, a fabricated one is a sentence somebody has to find and disbelieve."
    },
    "locale_pack_delete": {
      "new": true,
      "kind": "mutation",
      "args": {
        "tag": "string"
      },
      "returns": "void",
      "why": "A language somebody installed has to be removable.",
      "notes": "Removing one that is not there is success — the caller is asking for it to be gone, and it is."
    },
    "logs_info": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ directory: string|null, newestFile: string|null, totalBytes: number }",
      "notes": "Where the diagnostic log lives. New to the desktop app — the container UI wrote to stdout, which docker logs captured and nobody kept. The path differs per platform (~/Library/Logs on macOS, %LOCALAPPDATA% on Windows, XDG_STATE_HOME on Linux — which is where the specification puts logs, and where this moved once ~/.local/share was noticed to be the wrong half of XDG) and none is somewhere a user would look unprompted. The folder is named for the app, not for its bundle identifier; `appdir` owns that choice for both this and the config directory."
    },
    "mail_attachment_save": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string",
        "partId": "string",
        "path": "string"
      },
      "returns": "u64",
      "why": "Reading a captured invoice PDF meant leaving for the browser UI.",
      "notes": "partId is a handle the catcher issued and path comes from the system save dialog — the front end names neither the source nor the destination."
    },
    "mail_clear": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "void",
      "notes": "DESTRUCTIVE in the small: it empties the inbox. The UI MUST confirm, but this needs no backup — a mail catcher is a bin by design."
    },
    "mail_delete": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "notes": "One message, not the inbox. The servers disagree on the verb's address: MailHog takes the id in the path, Mailpit takes a JSON body of ids on the collection route. No confirmation — a catcher is a bin; only the whole-inbox clear keeps its dialog."
    },
    "mail_html_check": {
      "new": true,
      "kind": "query",
      "args": {
        "id": "string"
      },
      "returns": "HtmlCheck?",
      "why": "The report a developer actually opens a catcher for: how this markup fares across 186 real mail-client features. Mailpit only — null on MailHog, which is how the UI knows to hide the tab instead of showing zeroes.",
      "notes": "Warnings are sorted worst-first; a report whose first row is a construct every client supports buries the one that breaks Outlook."
    },
    "mail_link_check": {
      "new": true,
      "kind": "query",
      "args": {
        "id": "string"
      },
      "returns": "LinkCheck?",
      "why": "Dead links in a transactional mail are found by the recipient today.",
      "notes": "**This leaves the machine** — it fetches every link in the message, so it is never run as part of opening one; the UI asks for it explicitly. 30s timeout, because it waits on third-party servers."
    },
    "mail_message": {
      "new": true,
      "kind": "query",
      "args": {
        "id": "string"
      },
      "returns": "MailBody",
      "notes": "Mailpit returns decoded text and HTML parts. MailHog returns the raw MIME document and leaves decoding to the caller, so `html` is always null there — showing a half-decoded multipart as if it were the message would be worse than showing the source."
    },
    "mail_messages": {
      "new": true,
      "kind": "query",
      "args": {
        "limit": "u32 (default 50)"
      },
      "returns": "MailMessage[]",
      "notes": "Both wire formats are normalised in Rust. They agree on almost nothing: MailHog spells an address as `{Mailbox, Domain}` and buries the subject in a header array, Mailpit uses `{Name, Address}` and a field. Reading either as the other yields an inbox of blank rows that still look like messages."
    },
    "mail_relay_get": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ enabled: boolean, host: string, port: number, username: string, security: 'starttls' | 'tls' | 'none', from: string, allowedRecipients: string[], hasPassword: boolean, keystore: boolean }",
      "why": "M-2. The catcher catches everything, which is right, and it is not the whole job: the message somebody actually needs to check is the one that renders differently in Outlook, or the invoice a colleague has to look at — both of which mean THIS ONE MESSAGE, TO A REAL ADDRESS. Every rival calls that release and StackVo could not do it. Deliberately not the other shape: pointing the application at a real SMTP server would send the forty password resets a test suite generates in an hour to whatever addresses the fixtures happen to contain.",
      "notes": "Never returns the password: `hasPassword` is a boolean, and there is no command in this app that reads a stored credential back. `keystore` says whether the OS store is reachable at all, so a machine where it is not says so rather than silently storing nothing. Mailpit does the relaying and reads its settings from its own environment; this app reaches that environment through a COMPOSE OVERLAY, the same mechanism site.rs and perf.rs use — so the service package is untouched, nothing is re-sealed, and a workspace that has never configured a relay renders exactly the bytes it did before. The catcher's compose service is found BY IMAGE (`axllent/mailpit`), not by name: the key is `mailpit` on a .env workspace and `mailpit-1-30` on one that moved to the instance table. The password is in the OS keystore and the settings file carries no value; it IS written into generated/, which is the same partial answer ADR 0010 states for service passwords and is said out loud rather than implied. `allowedRecipients` is a regular expression to Mailpit, so each entry's dots are escaped — unescaped, `me@test.com` would also permit `me@testxcom`, which is an address somebody could own. A value containing a line break is REFUSED, because a newline ends a YAML scalar and everything after it is read as configuration somebody else wrote."
    },
    "mail_relay_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "config": "RelayConfig",
        "password": "string | null"
      },
      "returns": "RelayStatus",
      "why": "Somewhere to put the relay, once somebody has been told a release was refused.",
      "notes": "`password` has three states and they are all different intentions: `null` leaves the stored one alone, `''` removes it, anything else replaces it. A relay that is switched ON must have a host, a port and a sender address — a provider rejects a From it does not own, and the error comes back as a number. The overlay is rendered immediately as well as before every compose call, so a failure to write it is reported while somebody is looking at the pane. Mailpit does the relaying and reads its settings from its own environment; this app reaches that environment through a COMPOSE OVERLAY, the same mechanism site.rs and perf.rs use — so the service package is untouched, nothing is re-sealed, and a workspace that has never configured a relay renders exactly the bytes it did before. The catcher's compose service is found BY IMAGE (`axllent/mailpit`), not by name: the key is `mailpit` on a .env workspace and `mailpit-1-30` on one that moved to the instance table. The password is in the OS keystore and the settings file carries no value; it IS written into generated/, which is the same partial answer ADR 0010 states for service passwords and is said out loud rather than implied. `allowedRecipients` is a regular expression to Mailpit, so each entry's dots are escaped — unescaped, `me@test.com` would also permit `me@testxcom`, which is an address somebody could own. A value containing a line break is REFUSED, because a newline ends a YAML scalar and everything after it is read as configuration somebody else wrote."
    },
    "mail_release": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string",
        "to": "string[]"
      },
      "returns": "void",
      "why": "The whole point of M-2: one caught message, sent on to a real person.",
      "notes": "Mailpit only. MailHog has no equivalent and that is REPORTED rather than worked around — releasing by opening our own SMTP connection would make this app a mail sender, with a TLS stack and a credential in this process, to add a feature to a catcher somebody has already replaced. Mailpit's own refusal sentence is carried through unchanged when no relay is configured: a generic \"release failed\" sends somebody looking at their SMTP provider for a setting that is missing here. The recipient is typed by the user rather than taken from the message's own To — the reason to release is that somebody ELSE has to see it, and re-sending to the fixture address is the one thing that is never useful."
    },
    "mail_search": {
      "new": true,
      "kind": "query",
      "args": {
        "query": "string",
        "limit": "u32?"
      },
      "returns": "MailMessage[]",
      "why": "A catcher with fifty captured mails needs a search box more than it needs another button. The query reaches the catcher verbatim, so Mailpit's own syntax (from:, to:, subject:, quoted phrases) works as documented rather than being reimplemented worse here."
    },
    "mail_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "MailStatus",
      "why": "StackVo has shipped a mail catcher all along and never showed it — reading a captured message meant leaving for a browser tab, which is exactly the round trip four competitors charge for removing.",
      "notes": "Reads both MailHog and Mailpit. The upstream template installs the unmaintained `mailhog/mailhog`; Mailpit is what the ecosystem moved to, and swapping renames a service, its .env keys, its container and its volume — a migration for every running stack. Supporting both is the same stance the app takes everywhere else: it manages a checkout, it does not dictate one. `error` is set when the container is up but its API did not answer, because that otherwise renders as an empty inbox, which is a lie."
    },
    "package_scaffold": {
      "new": true,
      "kind": "mutation",
      "args": {
        "category": "string",
        "service": "string",
        "version": "string",
        "image": "string"
      },
      "returns": "PackageReport",
      "why": "C-1. Writes a package that is valid on the first read — identity, manifest, fragment, hashes already correct. MUST build the path from the workspace root and checked components: the webview names a service and a version, never a directory. MUST refuse to overwrite an existing version, and MUST leave an existing package.json alone so adding 8.4 does not rewrite the identity somebody filled in for 8.0."
    },
    "package_lint": {
      "new": true,
      "kind": "query",
      "args": {
        "category": "string",
        "service": "string",
        "version": "string"
      },
      "returns": "PackageReport",
      "why": "What sealing would change and what would still be wrong afterwards, with nothing written."
    },
    "package_seal": {
      "new": true,
      "kind": "mutation",
      "args": {
        "category": "string",
        "service": "string",
        "version": "string"
      },
      "returns": "PackageReport",
      "why": "Recomputes the hashes after an edit and THEN validates — parse, manifest check, compose policy — refusing the whole operation if any fail. Writing the hashes of a fragment the policy rejects would be a tool for producing packages that install and cannot run."
    },
    "market_catalog": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "MarketPackage[]",
      "why": "What is published, and for each version whether it is already installed and whether an instance is using it — so the UI can offer Install or refuse Uninstall without a second round trip.",
      "notes": "Empty when nothing has been fetched. An `eol` version is listed and installable; the picker hides it behind 'show older versions' rather than withdrawing it, because somebody's workspace may name it."
    },
    "market_install": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "version": "string"
      },
      "returns": "MarketStatus",
      "why": "Fetch one package, verify it whole, and put it where the renderer looks.",
      "notes": "The manifest is refused as BYTES when it does not hash to what the index says — before any field of it is read. The package is unpacked into a scratch directory, verified, and only then moved into place: a half-written package is the one failure a client cannot recover from on its own. Refused before any byte is fetched when policy.market.allowedPackages excludes the service, and after the manifest is parsed when policy.market.allowedRegistries excludes its image's registry — the second needs the manifest, which is why it is not beside the first."
    },
    "market_bundle": {
      "new": true,
      "kind": "mutation",
      "args": {
        "destination": "string — a directory that does not exist yet, or an empty one"
      },
      "returns": "Bundled",
      "why": "Write everything an air-gapped machine needs into one directory, on a machine that still has a network. ADR 0011 makes market.offlineBundle the only way a catalogue reaches a machine with no network, and until now nothing could produce one — the consuming half shipped without the producing half (§3 #31).",
      "notes": "The output is a source: `LocalSource` reads it and the far end cannot tell it from a checkout, so `market_refresh` against it and then `market_install` complete the round trip. registry.json is copied byte for byte rather than re-serialised, because the signature is over the bytes (ADR 0015) and manifestSha256 chains from them. Every manifest is verified against the index while bundling — the same check `market_install` makes, moved to the machine with a person at it rather than the one with no network. A withdrawn version keeps its index row and loses its files: ADR 0014 keeps the row so the far end can learn what happened, and market_install refuses to install one before fetching anything. A destination that is not empty is refused; a failed bundle is removed rather than left looking whole. The tar named in servis-market-mimarisi.md §9 is a packaging of this directory, not a second mechanism."
    },
    "market_probe": {
      "new": true,
      "kind": "query",
      "args": {
        "location": "string — an https:// address or a directory path"
      },
      "returns": "SourceProbe",
      "why": "Answering 'is my address right' without doing the thing. The first address anybody pastes is the repository's web page, which a working server correctly 404s, and there was no way to find that out except by refreshing and reading the failure.",
      "notes": "Caches nothing and remembers nothing: it fetches into a scratch directory and removes it. A GitHub repository URL is translated to its raw base and BOTH are reported, so the answer explains itself. An index older than the cached one is reported as `goesBackwards` rather than raised as an error — that refusal belongs to market_refresh, and knowing about it beforehand is the point of a probe."
    },
    "market_refresh": {
      "new": true,
      "kind": "mutation",
      "args": {
        "location": "string — an https:// address or a directory path; which one it is comes from the string"
      },
      "returns": "MarketStatus",
      "why": "Read a catalogue from a directory the user chose — an offline bundle, or a checkout of the packages repository. The only source this build has; HTTPS waits on the key ceremony.",
      "notes": "`location` is input, not configuration: the source is remembered, not obeyed, and both sources refuse a path that walks out of them. Precedence is policy.market.offlineBundle, then policy.market.registryUrl, then what the user gave — a machine handed a bundle was not expected to reach anything. http:// is refused outright: nothing verifies a signature yet (ADR 0015), so TLS is the whole of the protection. policy.market.requireSignature raises Trust to Signed, which refuses rather than downgrading — the one policy key that can only tighten (ADR 0009). The index is fetched with If-None-Match; a 304 keeps the cached copy. Any single file over 8 MiB is abandoned mid-stream, counted on bytes received rather than on Content-Length, which the sender chooses (T-8). Runs on a blocking thread: the Source trait is synchronous so pkg and render need not know what a runtime is, and the HTTPS source blocks on the runtime handle."
    },
    "market_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "MarketStatus",
      "why": "A machine that has never fetched a catalogue has none at all — ADR 0011 embeds nothing — so 'not fetched yet' and 'the catalogue is empty' are different sentences and the market page shows a different thing for each.",
      "notes": "`signed` is always false today and is reported rather than assumed: market::Trust::Signed refuses instead of downgrading, because no registry key is pinned until ADR 0015's ceremony happens."
    },
    "market_uninstall": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "version": "string"
      },
      "returns": "MarketStatus",
      "why": "Remove a package's files. Data is not touched — ADR 0012 puts that behind purgeData on a command that also stops containers.",
      "notes": "Refuses while an instance still names the package. The filesystem would allow it and the renderer would then refuse that instance forever, with the reason two screens away."
    },
    "migrate_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "spec": "ProjectSpec? (defaults to the reviewed one)",
        "services": "bool? (default true)"
      },
      "returns": "MigrationPlan",
      "emits": ["preset:applied"],
      "notes": "Adopts first, enables services second, and the order is load-bearing: adoption is the half that can fail on a schema violation, and enabling services for a project that then did not get created leaves the stack carrying a database nothing uses. Skips adoption when the directory already has a manifest, so re-running it against a managed project only reconciles the services. Re-scans afterwards, so the reply describes the state that now exists rather than the one that did before the write."
    },
    "migrate_scan": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string (project directory)"
      },
      "returns": "MigrationPlan",
      "why": "The other half of P2-12. The folder half shipped with adoption in Sprint 4 — detection infers runtime, server and document root from artisan/wp-config.php/composer.json. What it cannot see is everything the person who wrote the compose file already decided: the PHP version, the domain, the extensions, and — with no equivalent in any marker file — WHICH BACKING SERVICES the project needs. A compose file with mysql:8.0 and redis:7.2 is a statement about the stack; adoption alone leaves the developer to rediscover it from a stack trace about a refused connection.",
      "notes": "**Docker parses the YAML, not this app.** `docker compose config --format json` is the reference implementation of the Compose spec: it resolves anchors, `extends`, .env interpolation and profiles, and normalises every shorthand (a label list becomes a map, \"8080:80\" becomes a structured port, a relative bind becomes an absolute path). The alternative was a YAML crate, and that was the wrong trade twice: serde_yaml is archived, and deny.toml here says a DIRECT dependency going unmaintained still fails the build; and a hand parser would be wrong on real files — xdebug::generated_services parses by indentation only because it reads a file this project generated, whose shape is fixed. Compose is already a hard preflight requirement, so this adds no dependency at all; when it is missing the answer is an error, not a half-parse. Three kinds of compose service are told apart because they become three different things: the one with a `build:` (or a php/node image) is the APPLICATION and becomes the manifest; nginx/apache/caddy become the manifest's `server` field and NOT a StackVo service, because StackVo runs the web server inside the project container and importing it as a sidecar would give the project two; everything in the catalog becomes a service to enable. Anything else is NAMED in `unmapped`, never dropped — a migration that silently ignores the one service the project actually needs is worse than one that refuses, because it looks finished. Reading only; the reply's `spec` is already validated against project.schema.json, because reviewing a spec that would then be refused is a review of nothing."
    },
    "open_folder": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "void",
      "notes": "Shows a directory in the system file manager. Its own command rather than the opener plugin's open_path, whose permission is documented as enabling the command with no scope — and a scope wide enough for an arbitrary workspace is a scope over everything. MUST reject a path that is not an existing directory."
    },
    "open_in_browser": {
      "new": true,
      "kind": "mutation",
      "args": {
        "url": "string"
      },
      "returns": "void",
      "why": "A project's own domain deserves the browser the developer works in, not whatever the OS last associated with https. The opener plugin has no notion of which browser.",
      "notes": "Also the fix for a real defect: `opener:allow-open-url` was granted without a scope, and open_url answers ForbiddenUrl when nothing matches — so every visit button in the app did nothing at all, silently. http/https only; a launcher that took file:// or a custom scheme from its own front end would be a way to start arbitrary things. Falls back to the system default when the chosen browser cannot start."
    },
    "open_in_editor": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "void",
      "notes": "Editor command from preferences; default: detect VS Code, fall back to the OS handler."
    },
    "open_path": {
      "new": true,
      "kind": "frontend-plugin",
      "args": {
        "path": "string"
      },
      "returns": "void",
      "notes": "Implemented in the front end via the opener plugin rather than as a Rust command: the plugin already does exactly this, and wrapping it would add a hop with no behaviour of its own. The capability is allow-listed in capabilities/default.json.",
      "implementedAs": "@tauri-apps/plugin-opener → openPath"
    },
    "open_url": {
      "new": true,
      "kind": "frontend-plugin",
      "args": {
        "url": "string"
      },
      "returns": "void",
      "notes": "Same as open_path. The plugin restricts schemes; the allow-list in capabilities/default.json is the control point.",
      "implementedAs": "@tauri-apps/plugin-opener → openUrl"
    },
    "php_ini_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "patch": "Record<string, string|null>"
      },
      "returns": "PhpIniStatus",
      "emits": ["php_ini:changed"],
      "why": "The half of P1-9 that was cut on evidence: `memory_limit` and `upload_max_filesize` are not manifest keys and cannot become them (the schema is additionalProperties: false), and the file the docs pointed at was mounted by nothing.",
      "notes": "`null` removes a directive rather than setting it to PHP's default — this file is an override layer, and leaving `memory_limit = 128M` behind because it matches today's default would keep overriding a php.ini the user may later change underneath it. Removing the last directive removes the FILE, so the mount goes with it on the next compose invocation. Comments, order and directives this app does not manage survive a patch untouched; the file is the user's and safe to commit. Values are format-checked before they are written, because PHP does not fail loudly on a malformed directive — it warns to a log nobody is watching and keeps the old value, which presents as \"the setting did nothing\"."
    },
    "php_ini_status": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "PhpIniStatus",
      "why": "Every competitor exposes memory_limit and upload_max_filesize; StackVo could not, because .stackvo/php.ini was documented but never real — docs/*/configuration/project.md lists it and the old web UI's DockerService.js:388 lists it, but `php.ini` appears NOWHERE in core/cli. No generator mounted it, so dropping the file in did nothing. The mount had to exist before a form was worth building.",
      "notes": "Three states kept apart, like Xdebug's. `exists` is the file on disk. `mounted` is read from the RUNNING container, never inferred — `stackvo up` from the Bash CLI layers three compose files, not five, and will recreate the container without the mount; None means nothing is running. `needsRecreate` is the two disagreeing. PHP reads its ini at process start, so editing a value in the bind-mounted file changes nothing until the container restarts — which is a third thing again, and the reason none of this is one boolean."
    },
    "pick_directory": {
      "new": true,
      "kind": "frontend-plugin",
      "args": {
        "title": "string?"
      },
      "returns": "string?",
      "why": "Adopting an existing folder as a project is impossible today; paths are typed by hand.",
      "implementedAs": "@tauri-apps/plugin-dialog → open({directory:true})",
      "notes": "Used directly by the front end. `workspace_pick` is the Rust command that matters, because it pairs the picker with validation and persistence."
    },
    "policy_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ active: boolean, source: string|null, managed: string[], locked: string[], registryPrefix: string|null, market: PolicyMarket, error: string|null }",
      "why": "A fleet of machines has settings somebody other than the person at the keyboard cares about — the domain suffix, the web server, and on a network without Docker Hub the registry every image is pulled from. Without this the Settings panes can only grey a field out, and a greyed-out field with no reason reads as a broken app rather than a managed one.",
      "notes": "Reads one JSON file: /Library/Managed Preferences/com.stackvo.desktop.json on macOS, %ProgramData%\\StackVo\\policy.json on Windows, /etc/stackvo/policy.json on Linux; STACKVO_POLICY_FILE overrides all three. Read once per process, like every managed-preferences reader — a restart is how a change is picked up. This is NOT a security boundary and must not be presented as one: the override variable and, usually, the file itself are within the user's reach. `managed` and `locked` carry keys only, never values — env_get redacts secrets and this must not be a way around that. `error` is a parse or rule failure and is returned rather than swallowed, because a policy that quietly does nothing is one the administrator believes is in force; a broken file yields an empty policy and never a refusal to start. See src-tauri/src/policy.rs and docs/durum.md §6, decision 0009. The `market` block reports where packages may come from (docs/servis-market-mimarisi.md §9). It is reported in full rather than as one flag because its whole purpose is to let somebody who has just been refused an install read WHICH rule refused it. `additionalKeys` is a count, not the keys."
    },
    "preflight": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Preflight",
      "why": "Everything that has to be true before the app can do anything: a checkout, a reachable daemon, compose v2, the shared network, a projects directory, bash, and mkcert. Without it the app opened regardless and each button failed on its own terms — three different errors, one cause each, none stated up front.",
      "notes": "Runs before the first screen. `Unknown` is a distinct state from failure: with the engine down there is no answer to \"does the network exist\", and reporting one sends the user after the wrong problem. Declared retroactively — this shipped without a contract entry."
    },
    "preflight_fix": {
      "new": true,
      "kind": "mutation",
      "args": {
        "id": "string"
      },
      "returns": "void",
      "why": "A requirement that reports a problem the app could have fixed itself is a diagnosis pretending to be a product. `fixable` on each Requirement says which ids this accepts.",
      "notes": "Rejects any id it cannot fix rather than silently succeeding — a no-op that returns Ok would report the requirement as repaired on the next poll and it would still be broken. Accepts 'network', 'projects' and 'engine'; the UI reaches engine start through engine_start (it polls for the daemon coming up), the 'engine' arm here is for headless callers. Declared retroactively."
    },
    "prefs_get": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Preferences",
      "notes": "Replaces the localStorage-backed usePreferences composable with the Tauri store plugin."
    },
    "prefs_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "patch": "Partial<Preferences>"
      },
      "returns": "void"
    },
    "preset_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "PresetPlan",
      "emits": ["preset:applied"],
      "notes": "Re-plans from the file rather than trusting a plan the frontend hands back: between the review and the click, .env may have been changed by a service toggle in another pane, and writing a stale diff would silently undo it. Writes through env_writer, which backs the file up, preserves comments and ordering, and serialises against the other .env writers. `needsRegenerate` says what has to happen next — enabling a service changes what the generator emits, so the change is not live until regenerate-then-up."
    },
    "preset_export": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string?"
      },
      "returns": "Preset",
      "why": "The roadmap said \"turn the commit-friendly stackvo.json into a flow\". Read against the code that framing is wrong: stackvo.json needs no flow — it is already in the project directory, already schema-validated, and a teammate who clones the repo already has it. What they do NOT get is the STACK: which of the twenty services are enabled and at which versions. That lives in .env, the one file nobody commits, because it is also where every password is. So the clone succeeds, the manifest is perfect, and the project still will not start until somebody says \"you need MySQL 8.0, Redis and Elasticsearch on\". That sentence is the preset.",
      "notes": "A preset can never carry a secret, and that is enforced by construction rather than by filtering. `services` entries hold `enabled` and `version` — two named fields, not a passthrough map — so there is no code path by which SERVICE_MYSQL_ROOT_PASSWORD reaches one; it is not that a filter drops it, it is that there is nowhere to put it. `settings` is an ALLOW-LIST of five global keys, not a deny-list: Env::is_secret matches on suffix, so a key added upstream tomorrow called SERVICE_FOO_APIKEY would sail through a deny-list. An allow-list fails closed against a file this app does not own. Disabled services are included too — a preset listing only what is on cannot express \"and turn Elasticsearch off\", which is half of what makes two machines match. Ports and paths are excluded on purpose: they are properties of one developer's machine, and importing somebody else's is how two people end up fighting over 3306."
    },
    "preset_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "path": "string"
      },
      "returns": "PresetPlan",
      "why": "Importing a colleague's stack must never be a blind write over your own. Same shape as hosts_plan/hosts_apply and cert_plan/cert_apply — the pattern this app already uses everywhere a change leaves its own process.",
      "notes": "Rejections are NAMED, never silently dropped: an unknown service id, or a settings key outside the allow-list, appears in `rejected` with its reason. A preset that quietly ignores half of what it was given is how somebody concludes the feature works and then spends an afternoon on the service it skipped. Unknown ids are refused here rather than becoming a SERVICE_<JUNK>_ENABLE key in the user's .env and a compose profile that matches nothing — the failure mode CONFLICTS.md C-09 records. A file that is valid JSON but not a preset (`kind` mismatch) is an error, not a plan with zero changes: pointing the importer at a package.json has to say so rather than look successful. A preset from a NEWER version is refused rather than half-read."
    },
    "preset_save": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string",
        "name": "string?"
      },
      "returns": "string (path)",
      "notes": "Pretty-printed, because the point of the file is being read in a pull request. Written atomically."
    },
    "profiler_clear": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "{ removed: number, freed: number }",
      "why": "Profiling fills a disk fast — the 200,000-iteration loop that shaped the parser produced a 10 MB file from one run — so \"clear these\" has to be one button, not sixty."
    },
    "profiler_delete": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "id": "string"
      },
      "returns": "void",
      "notes": "`id` is a bare `cachegrind.out.*` file name, checked before it is joined to a path — the same handle-not-a-path rule app_log_open runs on."
    },
    "profiler_read": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "id": "string (cachegrind.out.* file name)"
      },
      "returns": "ProfileReport",
      "notes": "Cachegrind, read off real Xdebug 3.4.0 output rather than from a specification. Four properties decide the parser. (1) Names are COMPRESSED and the ids are NOT in order — a real file defined fl=(2) before fl=(1), and cfn=(n) can reference a name defined further down, so names are resolved at the end; resolving as you read produces blanks. (2) Every call gets its own block: a 200,000-iteration loop produced **200,004 fn= blocks across 1.6 million lines**, so aggregating by name is not a nicety. (3) A cost line's meaning depends on what preceded it — after fn= it is SELF cost, after calls= it is the INCLUSIVE cost of that call, attributed to the callee. Getting that wrong doubles every caller's self time and is invisible in the output. (4) Units are declared in the file (`events: Time_(10ns) Memory_(bytes)`), not fixed — assuming microseconds would be wrong by two orders of magnitude on this build. `percent` is a share of `selfTotal`, not of the file's `summary`: that denominator is the one that makes \"how much of the work happened in here\" true and adds to 100. A gzipped file is REFUSED with an explanation rather than parsed into nonsense. Capped at 256 MB with `truncated` reported, because a truncated profile with no warning is a performance conclusion drawn from half the data."
    },
    "profiler_set_mode": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "mode": "'debug'|'profile'"
      },
      "returns": "ProfilerStatus",
      "emits": ["xdebug:changed"],
      "notes": "Stored in `projects/<name>/.stackvo/xdebug.json`, beside php.ini and devserver.json, for the same two reasons: the manifest schema is additionalProperties:false, and a file under .stackvo/ travels with a clone. On/off stays in `php.extensions`, because that is what compiles the extension in. Switching to `profile` writes `generated/xdebug/<name>.ini` and mounts it read-only at /usr/local/etc/php/conf.d/zzz-stackvo-xdebug.ini. **Why an ini and not the environment**: measured against a running container, XDEBUG_CONFIG does carry `output_dir` and `start_with_request` — both took effect — but it silently ignores `use_compression`, and Xdebug 3.4 gzips by default. One compressed file is the difference between a profile view and a parse error. `zzz-` rather than `zz-` because zz-stackvo.ini is the USER's php.ini from php_ini_set; a name sorting before it would let a hand-written xdebug.output_dir send profiles somewhere this app cannot read, which presents as profiling that produced nothing. The mode itself stays in XDEBUG_MODE and is deliberately absent from the ini: the environment variable takes precedence over xdebug.mode, so setting both invites them to disagree."
    },
    "profiler_status": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ProfilerStatus",
      "why": "P3-17 named Blackfire and SPX, and both are the wrong door. Blackfire ships a template already and needs an ACCOUNT — a signup wall in a local development tool. SPX, XHProf and Excimer are not in contracts/php-extensions.json (only xdebug is), so adding one is a change to a contract shared with upstream, the same class of decision as the Mailpit swap. **Xdebug is already a profiler**: xdebug.mode=profile writes cachegrind files, the extension is in the catalog, and the overlay that sets XDEBUG_MODE already belongs to this app. That is the one route with no contract change attached.",
      "notes": "Profiling is a MODE of the existing Xdebug toggle, not a second switch — the extension has to be compiled in either way, and two switches for one extension is two states to explain instead of one. The mode is `debug` or `profile`, never both: they want opposite start triggers (stepping wants to connect on the next request, profiling wants a trigger so an idle stack does not write a multi-megabyte file per page load), so a combined mode would have to break one of them. `trigger` is the name a request must carry — XDEBUG_TRIGGER as a cookie, GET, POST or environment value."
    },
    "project_adopt": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "spec": "ProjectSpec (optional — defaults to what was detected)",
        "overrides": "{ domain?, phpVersion?, server?, extensions? } — each replaces one detected value"
      },
      "returns": "OperationId",
      "emits": ["project:creating", "project:created", "project:error"],
      "why": "Writes stackvo.json for a directory that is already there, then regenerates. The counterpart of project_create, which requires the directory to be absent.",
      "notes": "Refuses when a manifest already exists — adopting a managed project would silently overwrite settings the user chose. The spec is validated against project.schema.json exactly as project_create's is; detection fills the form, it does not bypass the check. `overrides` sit on top of detection rather than replacing it, one value at a time: detection reads the code and can say \"this is Laravel, its document root is public\", but it has no way to know the user wanted `shop.loc`, and the three PHP fields are the scaffolding gap — a composer.json states the version the framework NEEDS (`\"php\": \"^8.3\"`), which read as an answer pins a new Laravel to the floor of its own range, nothing in a checkout names a web server, and detection has no opinion about extensions at all, so an adopted project took the generator's seven-entry fallback. The PHP three apply only when the detected spec has a `php` block, so a Node template cannot pick up a second runtime block (W-02). Passing a full spec still bypasses detection entirely; passing overrides does not. Also writes the project's hosts entry and extends the certificate — see project_create."
    },
    "project_adoptable": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Adoptable[]",
      "why": "`project_create` refuses when the directory already exists, so a folder cloned into projects/ could not be brought under management at all — the only way in was writing stackvo.json by hand. On the checkout this was written against, 11 of 21 directories under projects/ were in that state: real code, unmanaged.",
      "notes": "Reads only, and needs no Docker. Each entry carries a detected runtime, server, document root and framework, plus the files that detection was based on — a document root inferred wrongly produces a project that builds, starts and serves a 404, which is a failure with no error attached to it."
    },
    "project_build": {
      "replaces": "POST /api/projects/:p/build",
      "kind": "operation",
      "args": {
        "name": "string",
        "noCache": "bool (default false)"
      },
      "returns": "OperationId",
      "emits": ["build:start", "build:progress", "build:success", "build:error"]
    },
    "project_clone": {
      "new": true,
      "kind": "operation",
      "args": {
        "url": "string",
        "name": "string?"
      },
      "returns": "{ operationId: OperationId, name: string, hasManifest: boolean }",
      "why": "The only way a repository reached the workspace was the user cloning it in a terminal and then adopting the folder. The clone is the one step the app was not doing, and it is the first one.",
      "notes": "StackVo authenticates NOTHING. No keys, no agent, no known_hosts, no tokens, no host trust — git and ssh read the user's own config, and a URL that works in their terminal works here. GIT_TERMINAL_PROMPT=0 and GIT_SSH_COMMAND='ssh -o BatchMode=yes -o ConnectTimeout=15' are set for one reason only: a windowless subprocess must fail rather than wait for an answer nobody can give. StrictHostKeyChecking is deliberately NOT disabled — silently trusting an unknown host is the one failure worth stopping for. No host is special-cased; a self-hosted forge on a company domain is the case this was built for. The URL is the first webview-supplied string in this codebase to reach a subprocess argv, so `git::parse` is an allowlist of two shapes (ssh/https URL, or scp-style user@host:path) rather than a filter: `ext::` transports execute commands, a leading `-` is read by git as an option, and file:// is not a network fetch. Emits scaffold:progress / scaffold:done deliberately — same step of the same flow, and the console already subscribes. Ends with code on disk. `hasManifest` says which follow-up the caller owes, and the two are opposites: a repository without a stackvo.json is adopted (detect, then write one), a repository with one is registered (the committed settings are the team's answer and are not overwritten). Cloning a repository that ships its manifest — the arrangement the file is commit-friendly for — used to end in \"already has a stackvo.json\" with nothing generated. Either way a clone never becomes a second way to create a project."
    },
    "worktree_support": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "WorktreeSupport",
      "why": "N. Everything a project can say about worktrees before a dialog opens: whether git is here, whether the directory is a repository, which branches exist and which of them are already checked out somewhere, which database instances a branch could be given a database on, and the worktrees this project already has. One call rather than five, because the answer decides whether the button is even drawn.",
      "notes": "`reason` is a sentence, not a boolean — 'not a git repository', 'itself a worktree', 'no domain in its manifest'. `linked` is git's own answer (a linked checkout has its own git dir and a shared common dir), which is a different question from `record`: `record` means this app made it and therefore knows what it gave it. Branches come back newest-commit-first, because the branch somebody wants a worktree for is almost always one they touched this week. Instances that are stopped are listed and marked rather than hidden: 'why is that greyed out' has an answer on the row, 'why is the list empty' does not."
    },
    "worktree_list": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "WorktreeRow[]",
      "why": "Every worktree in the workspace, so the projects list can say `branch of shop` on a row instead of showing two unrelated siblings. Asking per project would be one command per row.",
      "notes": "Each row carries what is true right now beside what was recorded: `exists` for the directory, `dirty` for uncommitted work (null when git could not answer — the same third state `projectLocalRead` reports for ignore rules), and `orphaned` when the registration outlived the checkout. Reads two files and asks git per row; no Docker call, so it answers with the engine down."
    },
    "worktree_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "branch": "string",
        "options": "{ newBranch?: boolean, name?: string, database?: 'none' | 'create' | 'copy', instance?: string }?"
      },
      "returns": "WorktreePlan",
      "why": "A worktree creates a directory, a hostname, a container and a database at once, and the only moment any of those can be argued with is before they exist. The same plan-then-apply pair as hosts_plan/hosts_apply and db_move_plan/db_move_apply.",
      "notes": "No side effects — safe to call on every keystroke in the branch field. A refused plan still carries the name, path, hostname and database it *would* have produced, because a dialog that blanks out when it says no makes the reason harder to act on. `refused` is one sentence naming the cause: git missing, the project not a repository or itself one, no `domain` to build under, a branch git will not accept, a branch already checked out elsewhere (git allows one working tree per branch), a directory or hostname already taken, or a database instance that is absent or stopped. `warnings` is for what proceeds anyway: a parent with a `*.` alias means two Traefik routes match the new name, MongoDB has no CREATE DATABASE, and a database of that name already existing means it will be used rather than created."
    },
    "worktree_create": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "branch": "string",
        "options": "{ newBranch?: boolean, name?: string, database?: 'none' | 'create' | 'copy', instance?: string }?"
      },
      "returns": "OperationId",
      "why": "N — the item nothing else in this space does. `git worktree add` gives a branch its own directory; this gives that directory its own subdomain, its own database and its own environment, so two branches of one application run side by side.",
      "notes": "Plans first and refuses on the plan's own reason, so the command and the preview cannot disagree. Steps in order, each rolling back the ones before it: git worktree add (streamed as scaffold:progress / scaffold:done, the same events project_clone uses), then the manifest, then the database, then the record, then the generate. NOTHING IS WRITTEN INTO THE CHECKOUT THAT GIT TRACKS — the files in a worktree are the branch's. Identity goes into stackvo.local.json, the machine-local overlay that exists to be the file that is never committed, and which is added to the repository's .git/info/exclude (git's own per-clone ignore list, never .gitignore) when git says it is not ignored yet. A branch that carries no stackvo.json gets a full one derived from the parent's instead, with the parent's aliases and LAN sharing dropped so it cannot claim the same names. The environment — database credentials, APP_URL, STACKVO_WORKTREE_BRANCH — is app-owned, outside every checkout, and reaches the container through the compose overlay site.rs already renders. The database is a database on an instance that is already running, not an instance of its own: a branch is not a different engine. Emits project:creating / project:created, because a worktree IS a project and nothing downstream knows the word. Audited as worktree_create."
    },
    "worktree_remove": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "options": "{ force?: boolean, dropDatabase?: boolean, deleteBranch?: boolean }?"
      },
      "returns": "OperationId",
      "why": "The way back. A worktree left behind is a directory that looks like a project, a branch git will not check out anywhere else, and a database nothing points at.",
      "notes": "Four things can go and three of them are separate decisions the screen asks first: the checkout always, its uncommitted changes (`force`), its database (`dropDatabase`) and its branch (`deleteBranch`). Order is the reverse of creation with one exception — the containers go first, because Docker holds the tree open through its bind mount and git refuses to remove a worktree files are being written into. The database is dropped before the checkout: one that will not drop should leave the worktree there to try again from, rather than leaving a database nothing points at. `db::drop_database` refuses the instance's own database and the engines' system databases whatever the record says. Deleting the branch is best effort and never fatal — the worktree is already gone by then, and refusing to finish over an unmerged branch would leave the removal half done. project_delete reaching a worktree through the ordinary Delete button forgets the record and prunes git's registration, and deliberately leaves the branch and the database alone: `removeFiles` is that command's only licence to destroy anything. Audited as worktree_remove."
    },
    "worktree_env_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "env": "Record<string, string>"
      },
      "returns": "{ env: Record<string, string>, effective: Record<string, string> }",
      "why": "A worktree's own environment variables. site_save cannot serve this: it writes .stackvo/site.json, which is inside the checkout, and on a worktree the checkout is a branch somebody else is working on.",
      "notes": "Keys and values go through the same checks the project overlay applies (site::checked_key / checked_value) — a POSIX variable name, and a value with no line break in it, because the overlay is YAML and a newline in a scalar is a second directive somebody else wrote. `effective` is what the container will actually be given: the derived database variables and APP_URL beside the typed ones, with DB_PASSWORD masked and the same password masked inside DATABASE_URL. The credentials are never stored in the record — they are derived from the instance on every render, so a password changed in Settings reaches every worktree at the next generate instead of leaving a stale copy. A typed variable of the same name still wins, which is what makes the derived ones a default rather than a decision."
    },
    "project_create": {
      "replaces": "POST /api/projects/create",
      "kind": "operation",
      "args": {
        "spec": "ProjectSpec"
      },
      "returns": "OperationId",
      "emits": ["project:creating", "project:created", "build:*", "project:error"],
      "notes": "Writes the manifest, runs generate, then builds. The build half is what makes it an operation rather than a mutation."
    },
    "project_delete": {
      "replaces": "DELETE /api/projects/:name",
      "kind": "mutation",
      "args": {
        "name": "string",
        "removeFiles": "bool (default false)"
      },
      "returns": "void",
      "notes": "remove_files is NEW and MUST default to false. The current deleteProject() removes the project directory outright with no confirmation contract — a desktop app deleting user source code needs an explicit opt-in. The flag guards the user's CODE and nothing else: deleting a project always removes its container (web, worker sidecars, tunnel), every image tagged stackvo-<name>, generated/projects/<name>, logs/projects/<name>, its /etc/hosts line if StackVo wrote it, and its name from the certificate — all of which exist only because the project did. Docker-side cleanup is best effort and logged: a stopped engine is a fine moment to stop managing a project, and making the daemon a lock on deletion would be worse than a leftover image. Traefik needs nothing extra — project routing is compose labels, so it goes with the regenerated compose file. Named volumes likewise: a project service declares only bind mounts. Build cache is NOT touched; it is shared and global, and pruning it belongs to docker_prune."
    },
    "project_dockerfile_preview": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "strict": "bool (default true)"
      },
      "returns": "DockerfilePreview",
      "why": "Runs the Rust generator port alongside the Bash one so its output can be compared before it replaces anything. `matchesBashOutput` is the differential check, live against the user's own projects rather than only against fixtures.",
      "notes": "Strict mode is the behaviour change made visible: where Bash silently drops an incompatible extension, this refuses and names it. `skipped` always reports what compat mode would have dropped, so the difference is legible either way."
    },
    "project_get": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "Project",
      "why": "ProjectDetail.vue currently re-fetches the whole list and filters client-side."
    },
    "project_manifest_read": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ProjectManifest"
    },
    "project_local_read": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "LocalOverride",
      "why": "B-2. A committed manifest is what makes a checkout reproducible and is exactly why there was nowhere to say “on this machine, PHP 8.3”. Reads the overlay beside it."
    },
    "project_local_write": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "text": "string"
      },
      "returns": "LocalOverride",
      "why": "Text rather than a parsed object: this is a file somebody types, and a struct round-trip would reformat what they wrote. Empty text removes the file — “no overrides” and “an empty overrides file” are the same state and only one of them is something to wonder about in a directory listing. MUST reject `name` and `runtime`, and MUST validate the merged document rather than the fragment."
    },
    "project_hooks_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "HookPlan[] — one per event, in lifecycle order",
      "why": "B-3. A hook is a command from a repository somebody cloned, so what would run has to be readable before it does."
    },
    "project_hooks_approve": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "digest": "string"
      },
      "returns": "HookPlan[]",
      "why": "Approves this project's HOST commands exactly as they are now. The digest is sent back rather than recomputed server-side, and that round trip is the point: it is a receipt for the list the person actually read. MUST refuse when the manifest changed between the screen being drawn and the button being pressed — that refusal is what makes this consent rather than a checkbox. Container steps are never gated: the container already runs the repository's code."
    },
    "project_hooks_revoke": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "HookPlan[]",
      "why": "Withdraws approval. Takes no digest — you may always revoke."
    },
    "project_manifest_write": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "manifest": "ProjectManifest"
      },
      "returns": "void",
      "why": "Editing an existing project is impossible today — the UI can only create and delete. MUST honour the write rules in project.schema.json (extensions last, one runtime block)."
    },
    "request_timeline": {
      "new": true,
      "kind": "query",
      "args": {
        "project": "string",
        "service": "string? (database service id; omit for dumps only)"
      },
      "returns": "Timeline",
      "why": "F-2, whose note read 'dump/mail/log three separate screens, no correlation'. What the code thought it had (dd()) and what it actually asked the database for are two halves of one question, and reading them meant comparing clocks by eye across two panes.",
      "notes": "Dumps correlate: the bridge writes the request they happened in, so several from one page load group together and the group is named. Queries do NOT — MySQL's general log records the statement and the connection and nothing about which HTTP request caused it, and guessing from adjacency would be wrong on the first concurrent request and wrong silently. They are placed by time and left ungrouped, and the screen says which is which. Attributing a query to a request needs the application to say so, which is code inside somebody's project — the thing this feature exists to avoid needing. A database that cannot be reached yields the dumps alone rather than an error. See src-tauri/src/timeline.rs."
    },
    "profiler_tree": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string (project)",
        "id": "string (profile id from profiler_status)"
      },
      "returns": "Frame[]",
      "why": "F-3. `profiler_read` answers where the time went — a table of the costliest functions — and cannot answer what called that, which is the question a flame view exists for. The parser was already reading caller→callee edges to attribute inclusive cost and was discarding the caller.",
      "notes": "A call TREE, not strictly a flame graph, and the difference is stated rather than glossed: a flame graph is built from sampled stacks, so a box's width is how often that exact path appeared. Cachegrind holds no stacks — it holds edges summed over every place A called B. A function called from two places is one box here with the total, two boxes there. What a branch means is 'reaching B through A cost this much in total', which is answerable and is the question people bring to a profile; 'this path was taken this often' is not, and no arrangement of the edges recovers it. Separate from profiler_read because the tree is thousands of nodes and the table is sixty rows. Recursion stops at the first repeat on the path and the frame is kept and marked. See src-tauri/src/profile.rs."
    },
    "profiler_flame": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "id": "string"
      },
      "returns": "Flame",
      "why": "F-3, and the reason the word flame graph can now be used. profiler_tree draws what cachegrind holds — the SUMMED cost of \"A called B\" over every place A called B — so a function reached from two callers is one box carrying both, and no arrangement of those edges recovers which caller was expensive. An Xdebug trace holds every entry and exit with its depth, which is a stack: folding those gives one box per distinct path, with its own measured width. MUST be a separate command from profiler_tree rather than a flag on it, because the two are different claims about the same picture and a reader cannot tell them apart by looking."
    },
    "query_log": {
      "new": true,
      "kind": "query",
      "args": {
        "service": "string (database service id)"
      },
      "returns": "QueryLogSession",
      "why": "F-1, and §2 called it the largest product gap: three competitors sell query logging and N+1 detection, and this stack could not say what the database had been asked. The row also said it needed a collector inside the container — for MySQL and MariaDB that is wrong, and this is why.",
      "notes": "All four databases this app runs, each switched on at runtime with no agent, no rebuild and no code in the application. MySQL and MariaDB point their general log at a TABLE with two SET GLOBAL statements; Mongo profiles per database; Postgres takes three ALTER SYSTEM statements and a reload, which pin the switch, an epoch-stamped line prefix and the destination format. Reading Postgres back means asking the server where it writes — pg_current_logfile() — rather than assuming the container's stdout: every StackVo Postgres package ships logging_collector = on, which takes stderr away from the stream and puts it in a file under the data directory. Anything that is not a database answers supported:false rather than an error, because 'not this kind' must not read as 'something broke'. Verified against this workspace's live mysql:9.7, mariadb:12.3, postgres:14 and mongo:8.0 by examples/querylog_probe.rs. See src-tauri/src/querylog.rs."
    },
    "query_log_record": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string",
        "recording": "bool"
      },
      "returns": "QueryLogSession",
      "why": "The log records every statement unsampled and costs write throughput, so it is an instrument you switch on, look at, and switch off — never a default.",
      "notes": "One command with a boolean rather than two, because the pair is one decision and the interesting failure is leaving it on. Stopping also clears: what the log holds is statement text, and in a development database an INSERT carries the row. MySQL truncates the table and Mongo drops the collection; Postgres cannot, because the log is the server's own file, so stopping writes the same watermark query_log_clear does and nothing this app shows reaches back past it. Nothing is written to disk by the app and nothing reaches a diagnostics bundle — on Postgres the server itself keeps what it wrote, which the pane says while recording."
    },
    "query_log_clear": {
      "new": true,
      "kind": "mutation",
      "args": {
        "service": "string"
      },
      "returns": "QueryLogSession",
      "why": "Start again from here — what somebody presses before reloading the page they are investigating. Without it every read is the whole session and the one request under study is buried.",
      "notes": "A deletion on three of the four: MySQL truncates mysql.general_log, Mongo drops system.profile. Postgres deletes nothing — the log belongs to the server and this app must not rewrite it — and used to answer with a no-op, which is the same reply as success and was hidden by hiding the button. It now writes a watermark statement, and the reader drops everything above it. 'Cannot delete' was never the same as 'cannot start again'."
    },
    "project_lan_share": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "enabled": "bool"
      },
      "returns": "ProjectManifest",
      "why": "E-3. `shop.loc` exists in exactly one place — this machine's /etc/hosts — so opening the site on a real phone has never been possible without editing a file on that phone. This writes the intent; `lan_status` derives the name.",
      "notes": "Writes `lan_share` and nothing else. The hostname is NOT stored: sslip.io resolves an address out of the name arithmetically, so the name is computed from whatever this machine's address is at the moment it is asked for — a stored one would survive a DHCP lease and point at whichever machine took it next. Regeneration is still the caller's, exactly as it is for every other manifest change; a switch that quietly rebuilt the workspace would be doing an expensive thing behind a cheap-looking control."
    },
    "lan_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "LanStatus",
      "why": "The screen has to be able to say three different things: here is the address a phone can use, this machine has no address to offer, and the name baked into the compose file is from a network this laptop is no longer on.",
      "notes": "`address` is null both when there is no network and when the address is public — the second is a refusal, because handing out `shop.<public-ip>.sslip.io` would publish a development site under a name anybody can resolve. `stale` reads the rendered compose file rather than recomputing: the question is whether what was written still matches what would be written. The certificate is the local CA's and the visiting device does not trust it, so its browser warns; that is a missing trust anchor on a device this app cannot reach, not a fault it can fix. See src-tauri/src/lan.rs."
    },
    "oauth_callbacks": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "path": "string"
      },
      "returns": "{ path: string, local: string | null, public: string | null, providers: Array<{ id: string, label: string, accepts: 'any' | 'publiconly', note: string }> }",
      "why": "M-12, which sat on the list as 'an OAuth callback for .loc' and was not defined enough to build. Reading what the providers require settles it: a redirect URI is a BROWSER REDIRECT, not a fetch — the provider sends a 302 and never resolves the hostname — so https://shop.loc/auth/callback works for the flow, because the browser is on this machine, the name is in this machine's hosts file and the certificate is issued by a CA it trusts. What varies is whether the provider will accept the string at REGISTRATION time, and that is a per-provider rule that is invisible at their console.",
      "notes": "Implements no OAuth: no client, no token exchange, and no store for a secret — the application being developed does all of that, and an app that held somebody's client secret to be helpful would be one more place for it to leak from. Returns two strings and the rule for choosing between them. The tunnel URL is read live inside the command rather than passed in, because a quick tunnel's address changes on every start and a callback registered from a stale one fails at the last step of a flow with an error naming neither side. `path` is normalised — leading slash added, duplicate slashes collapsed — and a query string or fragment is REFUSED rather than stripped: most providers compare the registered URI to the redirect exactly. See src-tauri/src/oauth.rs."
    },
    "landing_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ running: boolean, container: string, url: string, rendered: string | null, projects: number, services: number }",
      "why": "M-4. Every rival in the category ships one and it is the address people bookmark. StackVo has had the NAME for it since the beginning with nothing answering on it: core_domains already writes the bare suffix into the hosts file and certs::required_domains already issues for it, so opening https://<suffix> got Traefik's own 404 — a name the app went out of its way to make resolve, serving nothing.",
      "notes": "`rendered` is read off the file's own timestamp rather than remembered, so an app restart, a hand-edited page and a workspace copied from another machine all stay truthful. A project with no domain is left OFF the page rather than listed as an unclickable row. A sidecar in the shape tunnel_start already established: one `docker run` on the stack network with Traefik labels, --rm so stopping is removal. The generated compose file is deliberately untouched — it is compared byte for byte against the Bash generator's output, and a service only this app knows about would break that comparison for a page. nginx:alpine rather than the alpine:3 this app already pulls, because Alpine's busybox is built WITHOUT the httpd applet (measured: `httpd: applet not found`). The page is one static file with no scripts, no fonts and no requests of its own — a landing page that needs the network to render is blank exactly when something is wrong — and every name on it is HTML-escaped, because a project is a directory and a directory can be called `R&D`. Measured end to end with examples/landing_probe.rs: the container is started against the live stack, https://stackvo.loc is requested with the system trust store and no --insecure, and the bytes that come back are compared with the bytes that were written; the container and the scratch directory are removed either way."
    },
    "landing_refresh": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "LandingStatus",
      "why": "The sidecar serves a FILE, so starting a project after the page was written leaves it stale with nothing having stopped. Rewriting is therefore a different action from serving, and one button doing both would restart a container to update a list.",
      "notes": "Writes atomically into generated/landing/index.html and returns the fresh status. Safe to call while the container is running: nginx serves whatever is on disk at request time and the mount is read-only from its side."
    },
    "landing_start": {
      "new": true,
      "kind": "operation",
      "args": {},
      "returns": "OperationId",
      "emits": ["landing:progress", "landing:done"],
      "why": "An operation rather than a mutation: the first start pulls nginx, which belongs in the operation console rather than behind a frozen button.",
      "notes": "Renders the page BEFORE starting the container — nginx answers 403 for an empty directory, which reads as a broken proxy rather than as a page that has not been written yet. A sidecar in the shape tunnel_start already established: one `docker run` on the stack network with Traefik labels, --rm so stopping is removal. The generated compose file is deliberately untouched — it is compared byte for byte against the Bash generator's output, and a service only this app knows about would break that comparison for a page. nginx:alpine rather than the alpine:3 this app already pulls, because Alpine's busybox is built WITHOUT the httpd applet (measured: `httpd: applet not found`). The page is one static file with no scripts, no fonts and no requests of its own — a landing page that needs the network to render is blank exactly when something is wrong — and every name on it is HTML-escaped, because a project is a directory and a directory can be called `R&D`. Measured end to end with examples/landing_probe.rs: the container is started against the live stack, https://stackvo.loc is requested with the system trust store and no --insecure, and the bytes that come back are compared with the bytes that were written; the container and the scratch directory are removed either way."
    },
    "landing_stop": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "void",
      "why": "Turning it off has to be as easy as turning it on.",
      "notes": "`--rm` means stopping is removal, so nothing is left answering on the name after the page is switched off."
    },
    "project_register": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string"
      },
      "returns": "OperationId",
      "why": "project_adopt refuses a directory that already has a stackvo.json, and refuses correctly — it must not overwrite settings somebody chose. But that was only half an answer: the compose files, the hosts entry and the certificate had not happened either, and nothing else did them. The manifest watcher reports a change and regenerates nothing, on purpose.",
      "notes": "The other half. Reads and schema-checks the manifest that is already there, generates, syncs the hosts entry and the certificate, and writes nothing back — a committed manifest is the team's answer, and the Manifest tab is where it changes. Refuses when there is no stackvo.json, because that is adoption's job. Fixes the manual case too: a repository cloned into projects/ by hand with its manifest committed was skipped by project_adoptable and rejected by project_adopt, so it never came online at all."
    },
    "project_requirements": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "{ declared: Array<{ id: string, known: boolean, enabled: boolean }>, suggested: Array<{ service: string, key: string }>, plan: Plan }",
      "why": "Competitive review B-1: herd.yml, .lerd.yaml and .ddev/config.yaml all describe the whole environment and are committed, so a teammate clones and runs one command. stackvo.json carried the project and a preset carried the stack, and the two never met. `services` in the manifest is the missing half; this reports what the repository declares, what this machine gives it, and the diff.",
      "notes": "Reads only. `declared` comes from the manifest — the statement a colleague agreed to — and `suggested` from the project's own .env, which is a guess and is kept separate for that reason; suggestions already declared are dropped. A suggestion carries the KEY that implied it and never the value, because that file holds credentials. `plan` is produced by the same planner preset_plan uses, so an unknown service id is rejected there by name rather than becoming a SERVICE_<JUNK>_ENABLE key. See src-tauri/src/detect.rs `services_from_env` for what counts as evidence."
    },
    "project_requirements_apply": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "PresetPlan",
      "why": "Turns the declaration into an enabled stack, through the plan-then-apply path a preset import already uses rather than a second one with its own rules.",
      "notes": "Writes .env and stops, exactly as preset_apply does: the returned plan says needsRegenerate and regenerating is a visible step with its own progress. Never disables anything — a project declares what it needs, and one project not needing Redis is not a statement that another project's Redis should stop. Never writes a version: there is one SERVICE_<NAME>_VERSION per workspace, so a project pinning one would change every other project's database. Re-plans inside the call, so a .env changed between the review and the click is not overwritten with a stale diff."
    },
    "project_requirements_declare": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "services": "string[]"
      },
      "returns": "Manifest",
      "why": "Writes the `services` list into stackvo.json, which is the file that gets committed — the point of the feature is that the next person to clone does not have to be told.",
      "notes": "Reads the manifest, replaces one list and writes it back, so a field this version of the UI does not know about cannot be lost on the round trip. Ids are trimmed, lower-cased and de-duplicated the same way manifest::read normalises them. An unknown id is a WARNING and not an error: an invalid manifest is a project the app refuses to build, and a typo in an optional field must not take a project offline."
    },
    "project_restart": {
      "replaces": "POST /api/projects/:c/restart",
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "emits": ["project:restarting", "project:restarted"]
    },
    "project_scaffold": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "template": "'laravel'|'symfony'|'cakephp'|'yii'|'codeigniter'|'laminas'|'slim'|'wordpress'|'drupal'|'prestashop'|'typo3'|'tina'|'nextjs'|'nuxt'|'vue'|'react'|'svelte'|'astro'|'nest'|'angular'|'django'|'rails'|'gin'|'echo'|'flask'|'fastapi'|'sinatra'|'rocket'"
      },
      "returns": "OperationId",
      "emits": ["scaffold:progress", "scaffold:done"],
      "why": "The create half of P0-1 (detection/adoption shipped first). Runs the framework's own installer in a throwaway container — composer create-project, wp-cli, create-next-app — so nothing is installed on the host and only the bind-mounted project directory survives.",
      "notes": "Ends where adoption begins: the UI follows with project_adopt, which detects runtime/server/document root from what the installer actually wrote — the same machinery as a git clone. Nine installers, every command pinned non-interactive — each flag measured against a real container rather than read off a README (nuxi needs both --template and --force; sv needs --no-add-ons; astro needs --yes). The JavaScript ones pass --no-install: the node Dockerfile installs for the container's platform, and a host node_modules in the image is the arm64-in-amd64 mistake NODE_DOCKERIGNORE exists to prevent (a prompt inside an operation console is a hang, not a question). On unix runs --user uid:gid so files land owned by the user, not root. Refuses a non-empty directory; a failed install that wrote nothing removes the husk so the retry is not blocked. Seventeen installers now, across PHP, JavaScript, Python and Ruby. Composer projects carry --ignore-platform-reqs because the installer container is not the machine the code runs on (CakePHP wants ext-intl, the composer image has none, and StackVo's PHP image does); Rails needs the full ruby image rather than slim, which cannot compile websocket-driver. Twenty-eight templates in two kinds: an installer container where the ecosystem has a scaffolder, and **written files** where it does not — Gin, Echo, Flask, FastAPI, Sinatra and Rocket are each 'write two files and let the language build them', so pulling an image to write thirty lines would be a download for nothing. Their dependencies are not skipped, they move: the project's own Dockerfile already runs pip/go build/bundle/cargo for the container's platform. Every written template binds 0.0.0.0 on the port lang_defaults gives its runtime — a test asserts it over code lines only, because the comments explaining the loopback trap were tripping the check."
    },
    "project_start": {
      "replaces": "POST /api/projects/:c/start",
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "emits": ["project:starting", "project:started"]
    },
    "project_stop": {
      "replaces": "POST /api/projects/:c/stop",
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "emits": ["project:stopping", "project:stopped"]
    },
    "project_validate": {
      "new": true,
      "kind": "query",
      "args": {
        "spec": "ProjectSpec"
      },
      "returns": "ValidationReport",
      "why": "Pre-flight the new-project form against project.schema.json + php-extensions.json before anything touches disk. Today a bad extension name is only discovered when the Docker build fails minutes later."
    },
    "projects_list": {
      "replaces": "GET /api/projects",
      "kind": "query",
      "args": {},
      "returns": "Project[]"
    },
    "pty_close": {
      "replaces": "socket 'disconnect'",
      "kind": "mutation",
      "args": {
        "sessionId": "string"
      },
      "returns": "void"
    },
    "pty_open": {
      "replaces": "socket 'create-terminal'",
      "kind": "stream",
      "args": {
        "target": "PtyTarget",
        "cols": "u16",
        "rows": "u16"
      },
      "returns": "SessionId",
      "emits": ["terminal:ready", "terminal:output", "terminal:closed", "terminal:error"],
      "v1Change": "PtyTarget is NEW: { kind: 'container', name } or { kind: 'host', cwd }. node-pty could only reach containers because it ran inside one; portable-pty on the host can open a real host shell in the project directory."
    },
    "pty_resize": {
      "replaces": "socket 'terminal-resize'",
      "kind": "mutation",
      "args": {
        "sessionId": "string",
        "cols": "u16",
        "rows": "u16"
      },
      "returns": "void"
    },
    "pty_write": {
      "replaces": "socket 'terminal-input'",
      "kind": "mutation",
      "args": {
        "sessionId": "string",
        "data": "string"
      },
      "returns": "void"
    },
    "quick_command_run": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "id": "string (catalog id)"
      },
      "returns": "OperationId? (null for interactive)",
      "emits": ["build:start", "build:progress", "build:success"],
      "why": "Runs one, by id.",
      "notes": "**The fixed catalog is the security model.** The frontend sends an ID, never a command; the argv is built on the Rust side from compiled-in words, so there is no code path by which the webview can name a program to execute — the same handle-not-a-path rule app_log_open uses, for the same reason: a project pane that accepted an arbitrary command string from its own frontend is a remote shell with extra steps. Every command is spawned as an argv ARRAY, never through a shell, so a project called `a; rm -rf ~` is a container name that does not exist rather than a second command. Two kinds, because they behave differently: interactive commands (tinker, wp shell) need a TTY and a human, so they open the user's own terminal and return null — an in-app REPL would be a second, worse one next to the one they have configured; one-shot commands stream through the operation console. `-it` is added only for the interactive ones, because Docker refuses `-t` when stdin is not a TTY. Refuses when the container is not running, so the failure is \"the project is not running\" rather than Docker's \"No such container\". B-4: the id may now name a command the project declared in its own stackvo.json as well as one compiled in. The webview still never names a program — `quickcmd::resolve` is the only place either becomes an argv, and a declared command reaches `docker exec` through the same function a built-in does."
    },
    "quick_commands": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "QuickCommand[]",
      "why": "P3-19 was \"a tinker quick action — the PTY exists, so it is nearly free\". It is, and on its own it is also one button. What is worth building is the set it belongs to: artisan tinker, artisan migrate, composer install, npm install, wp shell. Each of those today means opening a terminal, remembering the container name and typing `docker exec -it stackvo-<project> …`.",
      "notes": "Offered only when the marker file exists — driven off the same Fingerprint adoption uses, so \"does this project have artisan\" is answered in one place. Offering a command the project cannot run is worse than omitting it: the failure arrives as `artisan: not found` in an operation console, which reads as a broken app rather than as a button that never applied. `because` names the file the offer is based on. Deliberately ABSENT: migrate:fresh, migrate:reset, db:wipe and composer update — the first three drop the user's data behind a button four characters from the safe one, and the last rewrites a lock file. Those are things to type deliberately in the terminal that is one click away. M-9 adds the four frameworks this app scaffolds and did not offer a row for: Symfony (bin/console), Django (manage.py), Rails and plain Ruby. Each is the same shape — a fixed id, an argv and a marker file only that framework writes. Rails is detected on `bin/rails` rather than on a Gemfile, which is Sinatra and Jekyll as often as it is Rails, and it runs through `bundle exec` rather than the binstub: a binstub is only executable if its permission bit survived the checkout, and `bundle exec` needs the gem rather than the bit. Doctrine's migrate carries --allow-no-migration because it exits non-zero with nothing to run, which the console would draw as a failed operation. This is NOT the B-4 unlock: B-4 is a command the WORKSPACE declares, and every row here is still compiled in. B-4: the list is the built-in catalogue filtered by the project's marker files, then whatever the project's own stackvo.json declares under `commands`, marked with `declared` so a caller can say which is which. Declared entries are container commands only — there is no `host` form, which is why this needed no approval flow; see src-tauri/src/quickcmd.rs."
    },
    "repl_runners": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ReplRunner[]",
      "why": "F-5, and §5.5 held it as a decision rather than a task: quickcmd refused an in-app REPL in writing — \"a second, worse REPL next to the one they already have configured\" — and reversing a refusal is not something a commit does quietly. The refusal stands for a LINE repl and this is not one. A snippet is twenty lines you edit: write a query, run it, change line three, run it again. In tinker that is retyping. `tinker` still opens the user's own terminal, and the two are split by what the person is doing rather than ranked.",
      "notes": "Offered from the same Fingerprint the quick commands use. Two tiers, and each row says which it is: `booted` means the application is loaded (artisan tinker, wp eval, manage.py shell, bin/rails runner), and false means the language on its own (php -r, node -e) — a pane that showed them alike would let somebody debug for ten minutes before finding out their models were never loaded. Laravel is offered only where composer.json also declares laravel/tinker, because --execute is Tinker's flag and a --no-dev install does not have it. See src-tauri/src/repl.rs."
    },
    "repl_run": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "runner": "string (runner id)",
        "code": "string (the snippet)"
      },
      "returns": "ReplRun",
      "why": "Run the snippet against the booted application and hand back everything about the run — both streams, the exit code, the duration.",
      "notes": "**The webview picks a runner, it never names a program.** `laravel` means `php artisan tinker --execute` and nothing else; an id outside the table is refused before anything is spawned, and so is one the project has no files for. The snippet is ONE argv element and always the LAST, so a snippet containing `; rm -rf ~` is a string rather than a second command — the rule hooks and quickcmd state as their security model. It runs in the project's own container, which already runs that repository's code, and there is no host runner: a snippet that has to run on this machine is a hook, approved against a digest first. Two limits, guarding different failures: `timeout` INSIDE the container stops the snippet (killing a docker exec client does not stop what it started), and this process's own clock stops the wait. `limited` records which — measured present in php, node, python, ruby and wordpress-cli images, but \"every image I checked\" is not \"every image\". Success is read from the exit code and never from \"stderr was empty\": a PHP fatal is written to stdout, measured. Laravel's --execute does not echo the last expression the way the interactive REPL does, so the pane says to dump()."
    },
    "repl_history": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "ReplSnippet[]",
      "why": "The snippet you ran ten minutes ago, back in the editor. Without it the workbench is a text box that forgets, which is the terminal REPL's weakness rather than an improvement on it.",
      "notes": "The code, never the output. A snippet is what the person wrote and is the thing they want back; the output is the application's DATA — a row, a customer, a token — and querylog states the rule this follows: what came out of somebody's database is not written to disk by this app. Kept in the app's own config directory beside hook-consent.json, never in the project, because a file written into a checkout turns up in somebody's git status. A re-run moves a snippet back to the top rather than adding a copy."
    },
    "repl_history_clear": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "ReplSnippet[]",
      "why": "Forget them. A snippet can hold a customer's id or a token somebody pasted in, and a history with no way to clear it is one people learn not to use."
    },
    "release_build": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "tag": "string?"
      },
      "returns": "ReleaseResult",
      "emits": ["build:start", "build:progress", "build:success"],
      "notes": "**Verification is not optional and not a separate button.** The safety property here — no .env in the image, no active debugger — is exactly the kind that is easy to state in a Dockerfile and quietly wrong in the result, so the built image is RUN and asked. Proven both ways on a real project: with the ignore file, zero env files; without it, all five of that project's .env.* land in a layer and the check names every one. `hasApp` catches the other silent failure — a build whose context was empty produces an image that starts and serves nothing, which otherwise looks like a success. The ignore file is written BESIDE the generated Dockerfile as `Dockerfile.dockerignore`, which BuildKit reads in preference to the context's — confirmed to work before anything depended on it — so building somebody's image writes nothing into their repository. The tag is validated before it reaches a command line: lowercase only, because Docker rejects an uppercase repository with an error that does not say so."
    },
    "release_load": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "string[] (the image names the daemon adopted)",
      "notes": "`docker load` — the return trip `release_save` never had, and the half of an air-gapped install that was missing. Takes no project name and needs no workspace: a bundle is loaded on the machine that received it, which is exactly the machine that may have neither. A missing file is answered NOT_FOUND here rather than being handed to Docker, which calls it an invalid archive and sends the user looking for a corrupt bundle instead of a wrong path. The names come from `docker load`'s own stdout because nothing else knows them — the file name is whatever somebody called it, and one stream can carry several images. An image with no tag is reported by id rather than dropped: it did load, and an empty answer reads as an empty bundle."
    },
    "release_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "tag": "string? (defaults to <name>:production)"
      },
      "returns": "ReleasePlan",
      "why": "P3-20 — what `laradock ship` does. The analysis calls it the long-horizon differentiator because the container lineage makes it possible and no native-binary competitor can follow. It is also the item with a real question inside it, answered by looking at what the images actually contain rather than by assuming.",
      "notes": "**The dev image is not a production image**, measured on this checkout: a PHP project's image holds NO application code — /var/www/html contains one file, index.nginx-debian.html, because the source arrives through a bind mount — and it has Xdebug loaded. Exporting it ships a web server with nothing to serve and a step debugger that dials out on every request. A NODE image is the opposite: its Dockerfile does COPY . . and npm install, so it already holds the code and the build. Hence two strategies: `layer` (build FROM the dev image, add the source, remove the debugger inis) and `retag` (nothing to add; rebuilding would replace a Linux node_modules with whatever the host has). `excluded` carries every ignore pattern WITH ITS REASON, because an exclusion the user cannot explain is one they work around. `warnings` names what the result will be true of — no .env, vendor as it is on disk, Xdebug's binary still present though inactive — before the build rather than after."
    },
    "release_push_plan": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "tag": "string?"
      },
      "returns": "PushPlan",
      "why": "H-1. MUST re-run the verification rather than remember it from the build: the interval between building and pushing is exactly where somebody could retag something else onto the name."
    },
    "release_push": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "tag": "string?"
      },
      "returns": "PushPlan",
      "why": "MUST re-check on the way out — the plan the caller was shown crossed the IPC boundary and came back, and a check that only runs on the way out is not a check."
    },
    "release_recipe": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string",
        "tag": "string?"
      },
      "returns": "string — a compose file",
      "why": "A deployment recipe for the built image. Returned as TEXT, not written: where it belongs is the user's decision. MUST carry no source mount, no debugger, no database container, and variable NAMES with empty values — a recipe with credentials in it is a .env wearing a different extension, and this file is meant to be committed."
    },
    "release_save": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "tag": "string?",
        "path": "string"
      },
      "returns": "number (bytes written)",
      "notes": "`docker save` to a tarball — the artefact you hand to something that is not this machine."
    },
    "secret_move": {
      "new": true,
      "kind": "mutation",
      "args": {
        "key": "string"
      },
      "returns": "void",
      "why": "On a company machine ~/.stackvo/.env is backed up, synced and DLP-scanned, and it holds database passwords in plain text (readiness review §5.2). This moves one into Keychain / Credential Manager / Secret Service and leaves `keychain:<entry>` in its place.",
      "notes": "One key at a time and never automatic, because it has a consequence: the Bash CLI reads .env directly and would use the string `keychain:…` as the password. Writes the keystore first and the .env line second, and removes the entry again if the file write fails — the other order loses the password. Moving an already-moved key succeeds silently rather than failing, so a double click is not an error. Errors: INVALID_INPUT for a key that is not a credential, NOT_FOUND when there is no value to move, PERMISSION_DENIED when the keystore will not open. Audited as `secret_move`. This does NOT remove the value from generated/docker-compose.dynamic.yml, which is rendered with the real password — see docs/durum.md §6, decision 0010."
    },
    "secret_restore": {
      "new": true,
      "kind": "mutation",
      "args": {
        "key": "string"
      },
      "returns": "void",
      "why": "The way back. A user may only discover the CLI incompatibility after moving a key, and without this the only way out is hand-editing .env with a value the app will not show them.",
      "notes": "Writes the .env line first and deletes the keystore entry second — deleting first and failing to write would destroy the password. The only caller of env_writer::apply_verbatim, which is what stops the ordinary write path from undoing a move nobody asked to undo. Audited as `secret_restore`."
    },
    "secrets_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ available: boolean, keys: Array<{ key: string, moved: boolean, resolvable: boolean, set: boolean }> }",
      "why": "The Settings pane has to draw a row per credential saying where it lives, and it has to know before it offers a Move button whether this machine has a keystore at all — a headless Linux box with no Secret Service is a real machine somebody runs this on.",
      "notes": "Reads the .env text, NOT a loaded Env: loading replaces a keychain: reference with the value it points at, which is exactly the fact this reports. `available` is probed by reading an entry nothing ever writes, so asking leaves nothing behind. No value ever appears in the response — `env_reveal` remains the one deliberate way a password crosses the boundary. See src-tauri/src/secrets.rs."
    },
    "server_config_get": {
      "replaces": "none — new in the desktop app",
      "kind": "query",
      "args": {
        "server": "string"
      },
      "returns": "string",
      "notes": "The raw file including its comments. Only nginx and caddy are accepted; the other servers have no generated config file for a snippet to be added to."
    },
    "server_config_set": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {
        "server": "string",
        "content": "string"
      },
      "returns": "void",
      "notes": "Written atomically. Takes effect on the next generate: the directives are rendered into each project's server config, not into a file the server reads directly."
    },
    "service_connection": {
      "new": true,
      "kind": "query",
      "args": {
        "service": "string (service id)",
        "reveal": "bool (default false)"
      },
      "returns": "Connection?",
      "why": "The services sheet showed a container name, a port table and a credentials block, and left the reader to assemble a URI. The obvious assembly — `mongodb://stackvo-mongo:27017/` — cannot work from the host, because that name only resolves on the Docker network. Null for a service nobody connects to with a string.",
      "notes": "Two addresses come back, not one: `fromHost` is 127.0.0.1 and the published port, `fromContainer` is the container name and the port inside it. `fromHost` is null when a running container publishes nothing, rather than an address that would fail. The password is bullets unless `reveal` — the same act `env_reveal` is, and percent-encoded on the Rust side so a password containing `@` cannot rename the host. See src-tauri/src/connect.rs."
    },
    "service_db_clients": {
      "new": true,
      "kind": "query",
      "args": {
        "service": "string (service id)"
      },
      "returns": "App[]",
      "why": "The picker beside `service_open_in_client` has to be built from what is on this machine, not from a list of clients somebody might have. Empty for most services, which is the answer rather than a failure: a service with no connection string has nothing to open, and an AMQP or SMTP address is not one a desktop database client takes.",
      "notes": "Which protocols a client opens is read from the application rather than declared here — on macOS from the bundle's own CFBundleURLTypes. That is not a detail: Redis Insight declares only `redisinsight`, so a table written from the name would have offered it for `redis://` and produced a button that launches an app which ignores the address. An installed client that does not declare the scheme is omitted rather than greyed out — it is not missing, it is not applicable. The first entry is always the system handler, with the empty id. See src-tauri/src/apps.rs."
    },
    "service_open_in_client": {
      "new": true,
      "kind": "action",
      "args": {
        "service": "string (service id)",
        "client": "string (id from service_db_clients; empty = the system handler)"
      },
      "returns": "void",
      "why": "G-3. The correct connection string has existed since connect.rs was written and the sheet has offered to copy it since; what was missing was the step that pastes it, which everybody was doing by hand.",
      "notes": "Always the host address — the container one is a name on a Docker network that no client on this desktop can resolve, which is the confusion connect.rs exists to remove. Refuses when the container publishes nothing rather than inventing 127.0.0.1. The URI carries the real password, because a URI with bullets in it is one that fails to connect, so this is `reveal` in the sense of env_reveal and is recorded in the audit trail as one — the service and the client, never the URI. The cost is that the secret is an argument to `open` and so visible in `ps` while that process lives; the alternative is a temporary file with the same reachability and a lifetime nobody controls. Copying the string is still there for a shared machine."
    },
    "service_dependencies": {
      "replaces": "GET /api/services/:s/dependencies",
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "DependencyReport",
      "notes": "Kept for completeness; services_list already returns required/optional/unmetDependencies per service, so the front end has no reason to call this separately.",
      "rustInternal": true
    },
    "service_reveal": {
      "new": true,
      "kind": "query",
      "args": {
        "service": "string",
        "key": "string"
      },
      "returns": "string",
      "why": "The value behind one masked credential on the services list, whichever place this workspace keeps it.",
      "notes": "Dispatches the way every other reader of service state does — the instance table when there is one, `.env` when there is not. `key` is the manifest setting key for an instance (`ROOT_PASSWORD`) and the `.env` key for the legacy path (`SERVICE_MYSQL_ROOT_PASSWORD`), which is what the row carries in each case. The detail sheet called `env_reveal` directly before this existed, and after a handover that reported \"not set in .env\" over a password that does exist."
    },
    "services_list": {
      "replaces": "GET /api/services",
      "kind": "query",
      "args": {},
      "returns": "Service[]"
    },
    "system_accent": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ available: boolean, name: string|null, hex: string|null }",
      "why": "The desktop's own accent colour, so the app can match it instead of shipping a brand colour that clashes with everyone's system theme.",
      "notes": "macOS reads AppleHighlightColor via `defaults` and maps it to the eight colours the OS itself draws with. Elsewhere `available` is false and both fields are null — Windows keeps its accent in the registry, and saying \"unknown\" beats returning a wrong blue. Declared retroactively — this shipped without a contract entry."
    },
    "template_override": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "string",
      "notes": "Copies the shipped file into the workspace so it can be edited, and returns its absolute path. Refuses when a copy is already there — that file is the user's edit. Only paths in templates_list are accepted."
    },
    "template_revert": {
      "replaces": "none — new in the desktop app",
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "void",
      "notes": "Deletes the workspace's copy; the version in the binary takes over on the next render. Destructive and not undoable — the front end confirms first."
    },
    "templates_list": {
      "replaces": "none — new in the desktop app",
      "kind": "query",
      "args": {},
      "returns": "Array<{ path: string, overridden: boolean }>",
      "notes": "Every template compiled into the binary, with whether this workspace has a copy on disk. Installing writes none, so a copy means somebody chose to override it."
    },
    "terminal_open_external": {
      "replaces": "POST /api/terminal/:c/open",
      "kind": "mutation",
      "args": {
        "target": "PtyTarget"
      },
      "returns": "void",
      "v1Change": "Always works. The HTTP version returned HTTP 400 with 'Terminal cannot be opened from containerized environment' — it could never succeed in the shipped configuration."
    },
    "tray_relabel": {
      "new": true,
      "kind": "command",
      "args": {
        "labels": "Record<string,string>? (keeps the current catalog when omitted)"
      },
      "returns": "void",
      "notes": "Re-label the tray and the native menu bar, and adopt the catalog they are drawn from. The tray is created during setup(), before the webview exists, so Rust carries a fallback table for the two languages this build was born with — but that table is a bootstrap detail, not the app's translation of its own tray. The front end sends the real strings on boot and on every language change, which is what stops a third language from being a change to tray.rs. A map rather than a named type on purpose: a struct would make every new menu entry a contract change on top of a code change. The keys are checked instead — a catalog missing any of them is refused with INVALID_INPUT naming the gaps, because a menu half in one language reads as broken rather than untranslated. The three counted labels (containers, more, runningSummary) arrive as templates carrying {count} / {running} / {total}: Rust knows the numbers, the language file decides where they go."
    },
    "tunnel_start": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string"
      },
      "returns": "OperationId",
      "emits": ["tunnel:progress", "tunnel:done"],
      "why": "Starts a cloudflared quick-tunnel sidecar on the stack's network, forwarding a random trycloudflare.com URL to the project's container. No account, no token — and no host-binary competitor can attach to the container network at all.",
      "notes": "An operation because the first start pulls the cloudflared image, which can take minutes. Targets the project container directly (with the local domain as Host header): with SSL on, every Traefik project router is websecure-only, which a public visitor cannot handshake against. Refuses when the project container is not running — a tunnel serving 502s looks like it worked. The URL that results is public and unauthenticated while the tunnel runs; the UI says so."
    },
    "tunnel_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "TunnelStatus[]",
      "why": "Webhook testing (Stripe, GitHub) had no answer: myapp.loc does not exist on the internet. This reports every running tunnel sidecar and its assigned public URL.",
      "notes": "The URL is read from the sidecar's own log on every call rather than cached — what the log says is what is actually live, across app restarts and container crashes alike. `url` is null while cloudflared is still connecting; the UI polls until it appears."
    },
    "tunnel_stop": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "notes": "The sidecar runs with --rm, so stop is also removal — nothing is left behind to leak the old URL."
    },
    "updater_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ configured: boolean, hasPublicKey: boolean, endpoints: number }",
      "notes": "Whether this build can verify an update. Tauri checks a bundle's signature against the public key compiled into the app; with an empty `pubkey` every check fails inside the plugin with a message about signatures, which reads like a server fault and is a build fault. Read from tauri.conf.json via include_str! so the answer describes the binary, not the working tree."
    },
    "updates_check": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "UpdateInfo",
      "status": "deferred",
      "notes": "Needs tauri-plugin-updater plus a signing key and a release endpoint — i.e. a release pipeline, not just code. Deferred to Phase 4 alongside signed builds. Declared here so the surface is complete and the gap is visible."
    },
    "window_close_action": {
      "new": true,
      "kind": "command",
      "args": {
        "action": "\"tray\" | \"quit\" | \"stopAndQuit\"",
        "remember": "boolean"
      },
      "returns": "void",
      "notes": "Carries out the choice the close dialog collected, optionally storing it as closeBehaviour. Four behaviours exist including \"ask\"; the extra \"quit\" (close, leave the stack running) is meaningful here because the containers are Docker's and outlive the app."
    },
    "worker_options": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "string[]",
      "why": "Which workers this project can offer, from its files alone: artisan offers 'queue' and 'scheduler', laravel/horizon in composer.json adds 'horizon'. A Node project gets an empty list, not an error."
    },
    "worker_start": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "kind": "'queue' | 'scheduler' | 'horizon'"
      },
      "returns": "void",
      "notes": "The sidecar runs the project's own image (same PHP, extensions, bind mount and network, so .env and the database resolve as the web container sees them) with --restart unless-stopped — self-healing is Docker's own supervisor, not reimplemented. queue:work carries --max-time=3600 on purpose: a worker holds the code it booted with, and an hour caps how long it serves yesterday's code. Refuses when the project has no built container."
    },
    "worker_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "WorkerStatus[]",
      "why": "A Laravel app runs queue:work, the scheduler and Horizon beside the web server; locally they live in a forgotten terminal tab, which is why \"my job never ran\" is a support staple. This reports every worker sidecar with its restart count — the self-heal made visible; a large number is a crash loop, not a success story."
    },
    "worker_stop": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "kind": "'queue' | 'scheduler' | 'horizon'"
      },
      "returns": "void",
      "notes": "Removal, not just stop: with unless-stopped, a merely-stopped container is one engine restart away from coming back."
    },
    "workspace_get": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Workspace",
      "why": "The web UI never needed this — it ran inside the StackVo repo, so its root was `/app` by mount. A desktop app has to be told, or work it out. Returns the resolved root plus how it was resolved, so the UI can show the user which checkout it is driving."
    },
    "workspace_pick": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "Workspace?",
      "notes": "Opens the native directory picker, then applies workspace_set. Returns null if the user cancels."
    },
    "workspace_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "path": "string"
      },
      "returns": "Workspace",
      "notes": "Validates that the path contains core/cli/stackvo.sh and projects/ before accepting it, then persists it. Rejects with NO_WORKSPACE and an explanation rather than silently accepting a wrong folder."
    },
    "xdebug_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "enabled": "bool"
      },
      "returns": "XdebugStatus",
      "emits": ["xdebug:changed"],
      "why": "One switch instead of three manual steps: adding the extension to the manifest, configuring the client host and port, and telling the IDE where the code is mounted.",
      "notes": "Writes `php.extensions` in the manifest and re-renders `generated/docker-compose.xdebug.yml`. The extension is COMPILED IN, so enabling it needs a rebuild before it takes effect — `needsRebuild` says so rather than letting the toggle look finished. Writing the manifest canonicalises it the same way project_manifest_write does (`webserver` becomes `server`); that is the contract's rule for writers, not a side effect of this command."
    },
    "xdebug_status": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "XdebugStatus",
      "why": "Step debugging is the thing Docker makes hardest and the thing a PHP developer needs most. `xdebug` was already in the extension catalog with per-version pecl pins, so it could be compiled in by hand-editing stackvo.json — and then it still would not connect, because nothing set xdebug.mode or told it which host the IDE is on. This reports all three layers separately: listed in the manifest, present in the built image, and live in the running container.",
      "notes": "`active` is read from the running container's environment rather than assumed from the manifest. The two disagree in a case users hit: `stackvo up` from the CLI does not layer the overlay this app writes, so it recreates the container without the Xdebug environment and debugging stops working with nothing on screen to explain it."
    },
    "perf_export": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "path": "string"
      },
      "returns": "{ bytes: number }",
      "why": "The price of the feature, paid explicitly: the container reads its dependencies from a volume an editor cannot see, so autocomplete needs a copy on the host. It is a SNAPSHOT and the screen says so — the container keeps writing to the volume and this copy does not follow. Replaces the host directory rather than merging into it: a half-updated vendor/ is worse for an index than an old one, because nothing about it says which half is which."
    },
    "perf_forget": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "path": "string"
      },
      "returns": "PerfLayer[]",
      "why": "Deletes the volume. Its own act and never part of turning the layer off — a checkbox that throws away thirty thousand files as a side effect is one nobody should trust."
    },
    "perf_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "path": "string",
        "enabled": "bool"
      },
      "returns": "PerfLayer[]",
      "why": "MUST seed the volume from the host copy BEFORE writing the setting, and MUST fail without writing it if that copy fails. A fresh named volume is empty — Docker seeds one from the image only where the image has content there, and no PHP image ships a vendor/ — so a setting saved first and a copy that failed afterwards is a site that 500s on the next request from a switch that reported success. Turning it OFF copies nothing and deletes nothing: the volume may hold the only copy of a vendor/ that took ten minutes to build. MUST refuse a path that is not relative to the project — the value becomes a container path AND part of a volume name, and it arrives from a JSON file that may have come with a git clone."
    },
    "perf_status": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "PerfLayer[]",
      "why": "I-1. A bind mount costs 2-3x a named volume on metadata and writes, which is the measured reason a Docker workflow feels slow on macOS and Windows. This lists the directories worth moving off the host filesystem for one project, whether each is currently in a volume, how big that volume is, and whether the host still has a copy for an editor to index. Suggestions describe the project in front of them — vendor for PHP, storage/framework and bootstrap/cache when it is a Laravel, node_modules when there is a package.json — and are never applied on their own."
    },
    "qr_encode": {
      "new": true,
      "kind": "query",
      "args": {
        "text": "string"
      },
      "returns": "{ version: number, size: number, rows: string[], text: string }",
      "why": "M-3. Two features hand out an address whose whole point is that it is opened on ANOTHER device — the LAN name from lan_status and the public URL from tunnel_status — and both are long, both contain either a dashed IP address or four random Cloudflare words, and the only way to get one onto a phone was to type it. That is the moment somebody gives up and uses the desktop browser's device emulation instead, which is not the same thing and is exactly the class of bug it fails to show.",
      "notes": "Byte mode, error-correction level M, versions 1-10 — 213 bytes, against a tunnel URL of about sixty. Written here rather than taken as a dependency because a QR code is a closed specification with published vectors: it cannot acquire new cases later, so the usual argument for a maintained dependency does not apply. It is NOT verified by its author's own expectations: examples/qr_probe.rs hands each symbol to macOS's own CIDetector — the decoder behind the camera — and passes a row only when the text read back is byte-identical. That measurement found the one real bug: the first format-information copy was written along row 8 instead of down column 8, and every other check passed while no decoder on earth would read the result. Returns the module matrix as one string per row rather than a picture, so the caller draws it; the front end renders an SVG with a four-module quiet zone, black on white whatever the theme is, because scanners expect dark modules on a light ground and the failure mode of an inverted code is a phone that simply sits there. INVALID_INPUT for empty text or more than a version 10 symbol holds — never a truncated symbol, which is indistinguishable from a complete one until somebody scans it. See src-tauri/src/qr.rs."
    },
    "site_save": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "env": "Record<string,string>",
        "directoryListing": "bool",
        "sshAgent": "bool"
      },
      "returns": "SiteSettings",
      "why": "Whole document rather than per key: three settings in one small file, and three commands over one document is three chances for it and the screen to disagree - the same reasoning routes_save gives. MUST refuse a key that is not a POSIX variable name and a value carrying a line break: the overlay is YAML, where a newline in a scalar ends it and everything after is read as configuration somebody else wrote. Regenerates, because the directory listing lands in a GENERATED server config while the variables and the agent land in a compose overlay - two different paths for one save."
    },
    "stripe_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "Array<{ project: string, running: boolean, container: string, signingSecret: string | null, hasKey: boolean, failure: string | null }>",
      "why": "M-11. Testing a payment flow means Stripe reaching the application, and shop.loc does not exist on the internet.",
      "notes": "`signingSecret` and `failure` are read from the container's log on every call rather than cached, so an app restart or a crashed listener stays truthful. `hasKey` is a boolean and there is no command that reads a key back — the pane can replace it or clear it and never display it. MEASURED with examples/stripe_probe.rs against the real image and a deliberately invalid key: the image runs, the arguments are accepted, and the CLI's 401 comes back through the log reader. NOT measured, and stated rather than implied: that a real event reaches the application, which needs an account. `stripe listen` opens an OUTBOUND connection, so nothing has to be reachable from the internet — which is why this is not the tunnel with a different label: a quick tunnel's URL changes on every start, so the endpoint has to be re-registered in the dashboard each time and the signing secret changes with it. The API key is kept in the OS keystore by secrets.rs, one entry per project (two accounts must not share one, or one account's events are forwarded into the other's application, which looks like it is working). It reaches the container through `-e STRIPE_API_KEY` and never as an argument: argv is visible in `docker inspect`, in the host process list and in this app's own operation console, which streams the command it ran. The sidecar deliberately does NOT use `--rm`, unlike the tunnel's: an invalid key makes the CLI print its complaint and exit, and a removed container takes the log with it — so the pane showed no listener, no error and no reason for the single most likely failure this feature has. Found by examples/stripe_probe.rs, which also caught itself reporting success on Docker's own \"No such container\" message."
    },
    "stripe_key_set": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string",
        "key": "string | null"
      },
      "returns": "boolean (whether a key is now stored)",
      "why": "A credential needs somewhere to go that is not a file in the workspace, and this app already decided where that is.",
      "notes": "One command for both directions: `null` clears it, because a screen that could only ADD a credential is one people are right not to give one to. A key beginning `pk_` is refused here rather than by the CLI three seconds later inside a container — a publishable key is the one somebody has open in a browser tab, and it fails with an authentication error that says nothing about which key was pasted."
    },
    "stripe_start": {
      "new": true,
      "kind": "operation",
      "args": {
        "name": "string",
        "path": "string",
        "events": "string[]"
      },
      "returns": "OperationId",
      "emits": ["stripe:progress", "stripe:done"],
      "why": "An operation because the first start pulls the Stripe image.",
      "notes": "Refuses when the project container is not running: the CLI would otherwise accept the events, fail to deliver each one and report the failures back to Stripe, filling the dashboard with delivery errors for a project that was simply not started. Removes any previous listener before starting — the container is not `--rm`, so a crashed one still holds the name. An empty `events` list means every event, which is the CLI's own default and the right one for \"does anything arrive at all\". `path` is a path inside the application and a URL is refused. `stripe listen` opens an OUTBOUND connection, so nothing has to be reachable from the internet — which is why this is not the tunnel with a different label: a quick tunnel's URL changes on every start, so the endpoint has to be re-registered in the dashboard each time and the signing secret changes with it. The API key is kept in the OS keystore by secrets.rs, one entry per project (two accounts must not share one, or one account's events are forwarded into the other's application, which looks like it is working). It reaches the container through `-e STRIPE_API_KEY` and never as an argument: argv is visible in `docker inspect`, in the host process list and in this app's own operation console, which streams the command it ran. The sidecar deliberately does NOT use `--rm`, unlike the tunnel's: an invalid key makes the CLI print its complaint and exit, and a removed container takes the log with it — so the pane showed no listener, no error and no reason for the single most likely failure this feature has. Found by examples/stripe_probe.rs, which also caught itself reporting success on Docker's own \"No such container\" message."
    },
    "stripe_stop": {
      "new": true,
      "kind": "mutation",
      "args": {
        "name": "string"
      },
      "returns": "void",
      "why": "Turning it off has to be as easy as turning it on.",
      "notes": "Removes rather than stops: the signing secret is in that container's log and it is dead the moment the listener is, so leaving it behind would leave a stale secret on screen for the next person to paste."
    },
    "site_settings": {
      "new": true,
      "kind": "query",
      "args": {
        "name": "string"
      },
      "returns": "SiteSettings",
      "why": "M-5, M-6 and M-10 in one document, because they are one kind of thing: a per-project setting the generator cannot read from the manifest (project.schema.json is additionalProperties:false and frozen), kept in .stackvo/site.json so it travels with the project. listingSupported and agentAvailable are answered by the machine rather than assumed by the screen: Apache and Swoole have no configuration file for a directory index, and an agent cannot be forwarded when none is running - a control drawn for either would do nothing."
    },
    "updater_offer": {
      "new": true,
      "kind": "query",
      "args": {
        "manifest": "Record<string, unknown> (the fetched latest.json)",
        "channel": "'stable' | 'beta' | null (default stable)"
      },
      "returns": "UpdaterOffer",
      "why": "§3 #21. `tauri-plugin-updater` fetches a manifest, compares versions, verifies a signature and installs — that is the whole of it. It has no notion of a channel, no notion of a percentage, and no way to STOP: a release found to be broken cannot be recalled, because every running copy keeps asking the same endpoint and getting the same answer. Channels, staged rollout and rollback are therefore four extra fields in the manifest and one decision made BEFORE the plugin is asked, and this is that decision.",
      "notes": "Takes the manifest rather than fetching it, so the branches are testable — every one of them is a branch somebody's install depends on. Order is the design: `paused` wins over everything (an emergency stop a channel mismatch can override is not one), `supersededBy` is checked before the version comparison because its entire purpose is to defeat it, and the rollout wave is last because a wave only means anything for an update that would otherwise be offered. The bucket is `sha256(installId : version) % 100` — stable for one install and one version, so an update cannot appear and disappear between checks, and independent between versions, so the same unlucky 1% is not first every time. A manifest with none of these fields is offered to everybody, which is what `latest.json` looks like today. The install id never leaves the machine; the response carries the bucket so a person can check the arithmetic, and not the id."
    },
    "websurface_start": {
      "new": true,
      "kind": "mutation",
      "args": {
        "port": "u16? (0, meaning the OS picks)"
      },
      "returns": "{ address: string, token: string, tools: number }",
      "why": "§3 #34 / ADR 0026. The loopback API is OFF until somebody asks. Not because loopback is dangerous by itself, but because a listener nobody knows about is a listener nobody turns off — and the honest default for a surface answering questions about somebody's workspace is that it is not answering them.",
      "notes": "The token comes back ONCE, to the caller that started it. It is never written to disk and `websurface_status` does not carry it: a status call that did would hand it to every later caller, and the first of those is the surface itself. Starting while one is already up is a CONFLICT rather than a second surface — a caller that asked to start something and silently got somebody else's token would have no way to know. Port 0 by default so the OS picks; a fixed port is one something else on this machine may already hold, and a surface that fails over a collision is one people work around by choosing another fixed one."
    },
    "websurface_status": {
      "new": true,
      "kind": "query",
      "args": {},
      "returns": "{ running: boolean, address: string | null, tools: string[] }",
      "notes": "The address and what would be served, never the token. `tools` is the intersection of two policies computed rather than listed: a tool reaches the wire only if it does not write AND its contract command passes the keystore rule, so a tool added to the MCP table joins this list only when both agree."
    },
    "websurface_stop": {
      "new": true,
      "kind": "mutation",
      "args": {},
      "returns": "bool",
      "notes": "`false` when nothing was running. Aborting the accept loop drops the listener, which closes the port; an in-flight request on its own task is not waited for — it holds a token that was already checked, and a stop that blocked until every connection finished is a stop somebody presses twice."
    }
  },
  "events": {
    "_note": "Names are preserved from Socket.io so the Vue listeners keep working, with ONE deliberate change: the terminal events move from dash to colon separators for consistency. That is a 4-line change in the terminal component.",
    "app:close_requested": {
      "new": true,
      "payload": {},
      "notes": "The window was asked to close and closeBehaviour is \"ask\". Rust prevents the close and emits this; the front end shows the choice dialog and answers with window_close_action."
    },
    "project:creating": {
      "payload": {
        "project": "string"
      }
    },
    "project:created": {
      "payload": {
        "project": "string"
      }
    },
    "project:deleting": {
      "payload": {
        "project": "string"
      }
    },
    "project:deleted": {
      "payload": {
        "project": "string"
      }
    },
    "project:starting": {
      "payload": {
        "project": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:started": {
      "payload": {
        "project": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:stopping": {
      "payload": {
        "project": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:stopped": {
      "payload": {
        "project": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:restarting": {
      "payload": {
        "project": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:restarted": {
      "payload": {
        "project": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "project:error": {
      "payload": {
        "project": "string",
        "error": "string"
      }
    },
    "service:starting": {
      "payload": {
        "service": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "service:started": {
      "payload": {
        "service": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "service:stopping": {
      "payload": {
        "service": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "service:stopped": {
      "payload": {
        "service": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "service:restarting": {
      "payload": {
        "service": "string"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "service:restarted": {
      "payload": {
        "service": "string",
        "running": "bool"
      },
      "emittedDynamically": true,
      "emittedBy": "commands::lifecycle — the name is built from the subject kind and the phase verb, so no literal appears in the source."
    },
    "build:start": {
      "payload": {
        "project": "string",
        "operationId": "string"
      }
    },
    "build:progress": {
      "payload": {
        "project": "string",
        "operationId": "string",
        "line": "string"
      }
    },
    "build:built": {
      "payload": "FinishedEvent",
      "note": "The IMAGE finished building. `project_build` has three stages — generate, build image, recreate container — and this marks the end of the second. The UI shows 'starting container…' between this and build:success."
    },
    "build:success": {
      "payload": "FinishedEvent",
      "note": "The container is up from the fresh image; the whole operation is done."
    },
    "build:error": {
      "payload": {
        "project": "string",
        "operationId": "string",
        "error": "string",
        "logPath": "string?"
      }
    },
    "compose:progress": {
      "new": true,
      "payload": "ProgressEvent",
      "why": "`docker compose up/down` writes its progress to stderr line by line. The old UI held the HTTP request open for the whole run (proxy_read_timeout 600s) and showed nothing until it ended."
    },
    "compose:done": {
      "new": true,
      "payload": "FinishedEvent"
    },
    "generate:start": {
      "new": true,
      "payload": {
        "scope": "string",
        "subject": "string — the scope again, because progress and done are subjected on it and a reader that opens an operation under a different subject never closes it",
        "operationId": "string"
      }
    },
    "generate:progress": {
      "new": true,
      "payload": {
        "operationId": "string",
        "subject": "string — the SCOPE, not a project name. A caller that marked a project busy before invoking generate_run must clear that flag itself.",
        "line": "string"
      }
    },
    "generate:done": {
      "new": true,
      "payload": {
        "operationId": "string",
        "projects": "u32",
        "services": "u32"
      }
    },
    "scaffold:progress": {
      "new": true,
      "payload": "ProgressEvent",
      "why": "composer create-project downloads a framework — minutes on a slow line, and the installer's own lines are the only honest progress there is."
    },
    "scaffold:done": {
      "new": true,
      "payload": "FinishedEvent"
    },
    "tunnel:progress": {
      "new": true,
      "payload": "ProgressEvent",
      "why": "The first tunnel_start pulls the cloudflared image; the pull's own lines belong in the operation console like every other long-running docker invocation."
    },
    "tunnel:done": {
      "new": true,
      "payload": "FinishedEvent"
    },
    "terminal:ready": {
      "renamedFrom": "terminal-ready",
      "payload": {
        "sessionId": "string"
      }
    },
    "terminal:output": {
      "renamedFrom": "terminal-output",
      "payload": {
        "sessionId": "string",
        "data": "string"
      }
    },
    "terminal:closed": {
      "renamedFrom": "terminal-closed",
      "payload": {
        "sessionId": "string",
        "exitCode": "i32"
      }
    },
    "logs:line": {
      "new": true,
      "payload": {
        "streamId": "string",
        "container": "string",
        "line": "string",
        "stream": "'stdout'|'stderr'",
        "source": "string? (LogFile.id, fanout only)"
      },
      "notes": "`container` is the container for a container stream and the project for the cross-project fanout — one stream has one origin, a fanout has one per line, which is why it is carried per event rather than settled when the stream opens. `source` is omitted entirely on a single-source stream, so those payloads are byte-identical to what they were before the fanout existed."
    },
    "logs:closed": {
      "new": true,
      "payload": {
        "streamId": "string"
      }
    },
    "logs:sources": {
      "new": true,
      "payload": {
        "streamId": "string",
        "followed": "number",
        "total": "number",
        "projects": "number"
      },
      "why": "The fanout follows at most 60 files. Emitting what it dropped is the difference between a view that is honest about its coverage and one that silently implies it sees everything. Re-emitted after every rediscovery, so a file appearing at midnight moves the number on screen."
    },
    "engine:status_changed": {
      "new": true,
      "payload": "EngineStatus",
      "why": "Drives the whole-app connected/disconnected state and the tray icon."
    },
    "container:state_changed": {
      "new": true,
      "payload": {
        "name": "string",
        "state": "string",
        "running": "bool"
      },
      "why": "Sourced from the Docker events stream. Today the UI polls on a visibility-gated timer (useVisiblePolling); a push stream removes the polling entirely."
    },
    "manifest:changed": {
      "new": true,
      "payload": {
        "project": "string",
        "path": "string"
      },
      "why": "fs-watcher on projects/*/stackvo.json. Enables 'config changed — regenerate?' instead of the user remembering to run `stackvo generate`.",
      "emittedBy": "the fs-watcher on projects/*/stackvo.json",
      "notes": "Debounced 400ms — editors write a file several times per save. The app does NOT regenerate on its own; it offers the action, because rebuilding a container under someone who is mid-edit is worse than the staleness it fixes."
    },
    "hosts:changed": {
      "new": true,
      "payload": {
        "added": "string[]",
        "removed": "string[]"
      },
      "why": "Warn when a project's domain has no hosts entry, with a one-click fix."
    },
    "db:start": {
      "new": true,
      "payload": {
        "operationId": "string",
        "service": "string",
        "action": "'dump'|'restore'",
        "path": "string"
      }
    },
    "db:progress": {
      "new": true,
      "payload": {
        "operationId": "string",
        "line": "string"
      },
      "notes": "The tools write progress and warnings to stderr; stdout is the dump itself and never reaches the UI."
    },
    "db:done": {
      "new": true,
      "payload": {
        "operationId": "string",
        "service": "string",
        "action": "'dump'|'restore'",
        "path": "string",
        "bytes": "number"
      }
    },
    "db:error": {
      "new": true,
      "payload": {
        "operationId": "string",
        "service": "string",
        "error": "string"
      }
    },
    "xdebug:changed": {
      "new": true,
      "payload": {
        "project": "string",
        "enabled": "bool"
      },
      "why": "The toggle changes the manifest and the compose overlay, so anything showing the project's configuration or its pending-regenerate state is now out of date."
    },
    "preset:applied": {
      "new": true,
      "payload": {
        "changed": "number"
      },
      "why": "Importing a preset rewrites service enablement and versions across .env, so the services list, the enabled/disabled chips and the pending-regenerate state are all now out of date."
    },
    "devserver:changed": {
      "new": true,
      "payload": {
        "project": "string",
        "enabled": "bool"
      },
      "why": "Turning dev mode on changes the compose overlay, which changes what the container is and what an `up` would recreate — so anything showing the project's configuration is out of date."
    },
    "php_ini:changed": {
      "new": true,
      "payload": {
        "project": "string",
        "exists": "bool"
      },
      "why": "Creating or removing the file changes the compose overlay, which changes what a `up` would recreate — so anything showing the project's configuration is now out of date."
    },
    "certs:changed": {
      "new": true,
      "payload": {
        "added": "string[]",
        "removed": "string[]"
      },
      "why": "The certificate was reissued, so anything showing coverage or an expiry has to re-read it. Traefik picks the new file up on its own; the UI has no way to know without being told."
    },
    "_removed": {
      "generate:error": "Removed: `generate:done` carries `success` and `error`, and generate_run returns Err as well. A second failure channel is one more thing to keep consistent for no gain.",
      "terminal:error": "Removed: pty_open returns Err when the shell cannot start, which is the only case this covered."
    },
    "instance:enabling": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:progress": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:enabled": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:disabling": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:disabled": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:error": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:starting": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:started": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:stopping": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:stopped": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:restarting": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "instance:restarted": {
      "new": true,
      "payload": "SubjectEvent",
      "emittedDynamically": true,
      "notes": "The instance id rides in the `service` field. A fourth shape of event would be a fourth thing every listener has to know about, for no information it does not already have."
    },
    "perf:changed": {
      "payload": "{ project: string, path: string, enabled: bool }",
      "why": "A layer moved. The container is still reading the old arrangement until it is recreated, which is the same 'written to disk, not to the container' state xdebug:changed reports."
    },
    "site:changed": {
      "payload": "{ project: string }",
      "why": "A per-project setting was written. The container is still the old one until it is recreated, which is the same state xdebug:changed and perf:changed report."
    },
    "landing:progress": {
      "new": true,
      "payload": "{ operationId: string, line: string }",
      "why": "The image pull, streamed — the same console every other long-running thing in this app reports into."
    },
    "landing:done": {
      "new": true,
      "payload": "{ operationId: string, ok: boolean }",
      "why": "The sidecar is up, or it is not."
    },
    "stripe:progress": {
      "new": true,
      "payload": "{ operationId: string, line: string }",
      "why": "The image pull, streamed into the operation console."
    },
    "stripe:done": {
      "new": true,
      "payload": "{ operationId: string, ok: boolean }",
      "why": "The listener is up, or it is not — what it then says about the key is read from its log."
    }
  },
  "types": {
    "Adoptable": {
      "name": "string",
      "path": "string",
      "detected": "Detected",
      "hasFiles": "bool",
      "composeFile": "string?",
      "note": "`has_files` ignores dotfiles: a directory holding nothing but a `.DS_Store` is empty for this purpose, and offering it for adoption as if it had code in it is how a real checkout looked before this was handled. `compose_file` is reported but NOT resolved — resolving costs a `docker compose config` per project, which is not something to spend on a list that loads on every visit; migrate_scan does that for the one the user picks."
    },
    "App": {
      "id": "string",
      "name": "string",
      "icon": "string",
      "available": "bool",
      "default": "bool",
      "note": "One launchable application on this machine. `id` is the stable key stored in preferences and never the display name, which is localised and moves between versions. An entry that is not installed is returned with `available: false` rather than omitted, so a picker can grey it out — a list that drops them reads as lack of support. At most one entry per list carries `default: true`: the one that would be used when the preference is unset."
    },
    "Bundled": {
      "packages": "number — services the index lists",
      "versions": "number — versions whose files were carried; lower than the index's total when something was withdrawn",
      "files": "number — files written, index and package.json included",
      "bytes": "number — their total size, so the person choosing a stick knows",
      "skipped": "string[] — versions deliberately not carried, each with its reason. Empty is the normal case; a non-empty list is a fact the person carrying the bundle should read before they walk away from the network",
      "signed": "boolean — whether registry.json.minisig travelled with it. A machine whose policy sets requireSignature refuses a bundle without one, and this is where that is cheap to find out"
    },
    "Catalog": {
      "runtimes": [
        {
          "id": "string",
          "versions": "string[]",
          "default": "string",
          "available": "bool"
        }
      ],
      "servers": "string[]",
      "defaultServer": "string",
      "phpExtensions": [
        {
          "name": "string",
          "install": "string",
          "inDefaultSet": "bool",
          "minPhp": "string?",
          "removedIn": "string?"
        }
      ]
    },
    "CertPlan": {
      "add": "string[]",
      "remove": "string[]",
      "domains": "string[]",
      "covered": "string[]",
      "rejected": "string[]",
      "changed": "bool",
      "certPath": "string",
      "installCa": "bool",
      "reloaded": "bool (cert_apply only)",
      "note": "`domains` is the exact SAN list mkcert will be invoked with, so what the user approves is what lands — the same relationship `preview` has to the hosts file. `remove` names domains the certificate still covers that no project asks for any more; they disappear on the next reissue and are listed so that is not a surprise. `rejected` names domains dropped as invalid rather than silently omitting them.",
      "reloadedNote": "Writing the certificate is not enough to change what a browser gets. Traefik's file provider watches generated/traefik/dynamic, and the certificates are not in it — routes.yml only points at them, and a certFile is read while the dynamic configuration is parsed. So cert_apply rewrites the watched files with their own bytes to force a reparse. Observed on a real checkout: Traefik up two days, serving a certificate a day older than the one on disk. False after an apply means the new certificate is on disk but not yet served."
    },
    "CertStatus": {
      "sslEnabled": "bool",
      "mkcertAvailable": "bool",
      "mkcertVersion": "string?",
      "caRoot": "string?",
      "caPath": "string?",
      "caTrusted": "bool?",
      "certPath": "string?",
      "keyPath": "string?",
      "notAfter": "string?",
      "daysRemaining": "number?",
      "expired": "bool",
      "covered": "string[]",
      "required": "string[]",
      "missing": "string[]",
      "stale": "bool",
      "note": "`covered` is read from the certificate's subjectAltName, not from the project list. `missing` is wildcard-aware: `*.stackvo.loc` covers `api.stackvo.loc` but not `shop.loc` and not `a.b.stackvo.loc`, because a wildcard matches exactly one label. `ca_trusted` is null when the platform gives no cheap way to ask, which is not the same as false. `stale` is true when anything is missing or the certificate has expired — it is what the UI badges."
    },
    "Checkout": {
      "remote": "string | null",
      "note": "The `origin` remote, verbatim, read out of .git/config rather than by running `git remote get-url` — this is called once per project on every list, and a subprocess per row turns a directory listing into twenty forks on a page that reloads whenever a container changes state."
    },
    "CompanionRow": {
      "name": "string — the manifest's handle, and the suffix of the container name",
      "containerName": "string — stackvo-<instance>-<name>, derived exactly as render::context derives it",
      "image": "string — the reference actually pulled, digest-pinned when the manifest pins one",
      "built": "bool",
      "running": "bool",
      "health": "string? — same vocabulary as Service.health"
    },
    "Connection": {
      "service": "string",
      "kind": "'mysql'|'postgres'|'mongo'|'redis'|'memcached'|'amqp'|'http'|'hostport'|'smtp'",
      "fromHost": "Endpoint?",
      "fromContainer": "Endpoint",
      "masked": "bool",
      "passwordKey": "string?",
      "note": "`masked` is false both when `reveal` was asked for and when the service has no password at all, so the UI only offers the eye where there is something behind it. `passwordKey` names the .env key rather than carrying a value — it is the same key the credentials list reveals, and the two must agree about which secret is in play."
    },
    "ContainerDetails": {
      "name": "string",
      "id": "string?",
      "image": "string?",
      "state": "string?",
      "running": "bool",
      "startedAt": "string?",
      "created": "string?",
      "restartCount": "number",
      "restartPolicy": "string?",
      "health": "string?",
      "exitCode": "number?",
      "ports": "Port[]",
      "networks": "string[]",
      "gateway": "string?",
      "mounts": "Mount[]",
      "ipAddress": "string?",
      "env": "string[]",
      "imageSize": "number?",
      "note": "Read from one `inspect`. `gateway` is the first non-loopback gateway — the detail page shows one. `imageSize` is bytes on disk for the image this container runs, not the container's own writable layer. Nearly everything is nullable because a created-but-never-started container has no state to report, and reporting zero for it would be a number somebody reads as measured."
    },
    "ContainerStats": {
      "cpuPercent": "f32",
      "memoryUsed": "u64",
      "memoryLimit": "u64",
      "memoryPercent": "f32",
      "netRx": "u64",
      "netTx": "u64"
    },
    "CpuBreakdown": {
      "user": "number",
      "nice": "number",
      "system": "number",
      "idle": "number"
    },
    "CpuStats": {
      "percent": "number",
      "cores": "number[]",
      "coreCount": "number",
      "loadAverage": "number[] | null",
      "breakdown": "CpuBreakdown | null",
      "note": "`loadAverage` is the 1/5/15-minute triple and is null on Windows, which has no equivalent. `breakdown` is null until two samples exist — the counters are cumulative, so one reading cannot produce a percentage — and null for good on a machine where it was measured to disagree with `percent`. It is reported when it can be trusted and withheld when it cannot, rather than drawn next to a number it contradicts."
    },
    "Credential": {
      "key": "string — without the SERVICE_<ID>_ prefix",
      "envKey": "string",
      "value": "string — masked when secret",
      "secret": "bool"
    },
    "DbInstance": {
      "id": "string",
      "service": "string",
      "version": "string",
      "kind": "string",
      "container": "string",
      "enabled": "bool",
      "running": "bool",
      "note": "Separate from DbTarget rather than a field added to it: `db_targets` answers “which engines can this workspace dump”, one row per engine, and four callers read it that way. One row per instance would give all four a list twice as long for a question none of them asked. Stopped instances are included and marked — a list that hid one would leave somebody wondering where the instance they just created went."
    },
    "DbMovePlan": {
      "from": "string — instance id",
      "to": "string — instance id",
      "fromService": "string",
      "toService": "string",
      "fromVersion": "string",
      "toVersion": "string",
      "possible": "bool",
      "refused": "string? — why not",
      "warnings": "string[] — true and not blocking",
      "note": "G-4. Dumping and restoring are two calls this app already had; what it did not have is an answer to “will this work”. Same engine is allowed. MySQL↔MariaDB is allowed WITH a warning — the same dump format in practice and genuinely different servers, and refusing it would refuse a thing that usually works. Crossing families is refused: a mysqldump file is not Postgres input and never becomes it, so accepting the request would empty the target and then fail on thousands of syntax errors. A downgrade is warned about because it is the direction that breaks quietly and does so partway through a restore that has already replaced the data."
    },
    "DbMoved": {
      "from": "string",
      "to": "string",
      "bytes": "number — the size of the dump that crossed, the only number that says anything actually moved"
    },
    "DbTarget": {
      "service": "string",
      "kind": "'mysql'|'mariadb'|'postgres'|'mongo'",
      "container": "string",
      "database": "string?",
      "user": "string?",
      "enabled": "bool",
      "running": "bool",
      "extension": "string",
      "note": "`extension` is the suggested file extension, and it differs by engine rather than being cosmetic: mysql and postgres dump to plain SQL a person can read and edit, while mongodump produces a gzipped BSON archive that only mongorestore can read. `database` is null for mongo, where the dump covers the whole server."
    },
    "DeclaredPort": {
      "name": "string — the manifest's handle: main, console, smtp",
      "container": "u16",
      "host": "u16? — what the container publishes when there is one, the recorded allocation when there is not",
      "protocol": "'tcp'|'udp'"
    },
    "DependencyReport": {
      "service": "string",
      "description": "string",
      "dependencies": [
        {
          "name": "string",
          "type": "'required'|'optional'",
          "running": "bool"
        }
      ],
      "hasUnmetDependencies": "bool",
      "internal": "string[]"
    },
    "DependencyRow": {
      "capability": "string — what the manifest asks for, so MariaDB can answer 'sql'",
      "service": "string? — the one service that will do, when only one will",
      "provider": "string? — the installed instance answering it, null when none does",
      "required": "bool",
      "running": "bool — false whenever there is no provider; the two are told apart by `provider`"
    },
    "Detected": {
      "framework": "string?",
      "runtime": "'php'|'node'",
      "server": "string",
      "documentRoot": "string?",
      "phpVersion": "string?",
      "nodeVersion": "string?",
      "nodePort": "number?",
      "nodeStart": "string?",
      "confidence": "'certain'|'likely'|'guess'",
      "evidence": "string[]",
      "note": "`evidence` names the files the answer was read from, and `confidence` says how much to trust it. `guess` means nothing recognisable was there and the fields are defaults the user is expected to correct — presenting that as an answer is the failure this distinction exists to avoid."
    },
    "DevServerStatus": {
      "supported": "bool",
      "enabled": "bool",
      "tool": "'vite'|'nuxt'|'next'|'unknown'",
      "command": "string",
      "productionCommand": "string?",
      "mounted": "bool?",
      "running": "bool",
      "needsRecreate": "bool",
      "configFile": "string?",
      "hostAllowed": "bool?",
      "snippet": "string?",
      "domain": "string?",
      "port": "number",
      "overlayPath": "string",
      "note": "`tool` is read from package.json with the FRAMEWORK checked before its bundler — Nuxt and Next both pull one in as a dependency, and the wrong order tells a Nuxt project to edit a vite.config.js it does not have. `command` prefers the `dev` script over `start`, because `start` in a Next project serves a production build, which is the mode this feature exists to get out of. `mounted` is read from the RUNNING container, never inferred; `stackvo up` from the Bash CLI layers three compose files, not six, and will recreate the container in production mode. `host_allowed` is null when there is no config file to look in, which is NOT the same as false — it is checked as text, because parsing JavaScript to decide whether a key is present is a much larger promise than \"does the file mention it\", and the answer is only ever used to decide whether to show the snippet."
    },
    "DiskOwner": {
      "id": "string",
      "image": "string?",
      "imageSize": "u64",
      "imageDedicated": "bool",
      "containerRw": "u64",
      "running": "bool"
    },
    "DiskStats": {
      "readTotal": "number",
      "writeTotal": "number",
      "readRate": "number",
      "writeRate": "number",
      "note": "Totals are cumulative since the sampler started; the rates are bytes per second, derived from the gap since the previous sample."
    },
    "DnsCheck": {
      "name": "string — the probe name, under the suffix and in no hosts file",
      "udp": "DnsProbe — asked the responder directly",
      "tcp": "DnsProbe — asked the responder over TCP",
      "system": "DnsProbe — asked this machine, the way a browser would",
      "public": "DnsProbe — whether the rest of the internet still resolves",
      "ok": "bool — udp and system together, which is the pair that means 'this works'",
      "note": "Four probes rather than one verdict because the repair differs: a responder that does not answer is this app's fault, a machine that does not ask it is the resolver file's, and public names failing matters more than the feature does. `public` is informational on its own — an offline machine fails it — and is only ever decisive as a before/after comparison inside dns_resolver_install, which undoes its own change when public names stop resolving."
    },
    "DnsProbe": {
      "ok": "bool",
      "detail": "string — what came back, in words, for a screen that has to explain a failure"
    },
    "DnsStatus": {
      "mechanism": "string — resolver | network-manager | dnsmasq | systemd-resolved | nrpt | manual",
      "writable": "bool — whether this app can apply the change itself, or only report it",
      "suffix": "string",
      "tld": "string — the last label, which is what a resolver is pointed at",
      "port": "number",
      "listening": "bool — UDP",
      "tcp": "bool — the second socket, reported separately because a port can be half-taken",
      "file": "string? — the file this mechanism writes, where it has one",
      "configured": "bool — whether this machine currently asks us for the suffix",
      "instruction": "string — the file this app would write, or the line the user must place",
      "reload": "string? — what is reloaded after the write, spelled out rather than done quietly",
      "foreign": "string? — a file already at that path that is not ours, and what it says",
      "stale": "string[] — resolver files this app wrote for a suffix the workspace has since left",
      "broken": "bool — the machine asks us and nothing answers, which is the state where every name under the suffix fails",
      "note": "E-1. The responder answers ONE suffix and refuses everything else — it never forwards, has no upstream and holds no cache, because an open forwarder on a machine is a thing that can be pointed at and a development tool has no business becoming the resolver for anything it did not create. `listening` is read from whether the socket is bound rather than from a preference: a setting saying yes over a port something else took would be a screen that lies. `mechanism` is detected rather than derived from the platform — on Linux WHICH file to write is a question about the machine (NetworkManager's dnsmasq, a standalone dnsmasq, systemd-resolved), and a guessed path is worse than no feature because it rearranges name resolution on a machine this app was wrong about. `manual` is the honest answer when none of them is found. `foreign` MUST be reported before the switch is pressed and the file MUST be copied aside rather than replaced — /etc/resolver/test is a path dnsmasq, Valet or a colleague's script may own, and taking a suffix away from another tool with no way back is not a thing to discover afterwards. `stale` exists because macOS's mechanism is one file PER SUFFIX: changing DEFAULT_TLD_SUFFIX leaves the old file pointing at a responder that now REFUSES that TLD, which is worse than never having written it — before, those names went upstream and failed honestly. `broken` is asked of the socket rather than of this process's own flag: a responder whose thread died and one in another copy of the app are both cases where the flag and the machine disagree, and the machine is right."
    },
    "DockerfilePreview": {
      "project": "string",
      "runtime": "string",
      "server": "string?",
      "dockerfile": "string",
      "skipped": "[{extension, reason}] — what the Bash generator drops without telling anyone",
      "bashOutputPath": "string",
      "matchesBashOutput": "bool"
    },
    "Doctor": {
      "preflight": "Preflight",
      "ports": [
        {
          "port": "u16",
          "requiredBy": "string",
          "state": "'ok'|'warn'|'fail'|'unknown'",
          "process": "string?",
          "pid": "u32?",
          "ours": "bool"
        }
      ],
      "hostsMissing": "string[]",
      "dns": "{ suffix: string, port: number }? — null in every ordinary state, INCLUDING the feature being off. Set only when the machine's resolver points at a local responder that is not answering: the one DNS failure nothing else on screen reports, where the app, the containers and the proxy all look healthy and every project domain fails to resolve.",
      "generated": {
        "state": "'ok'|'warn'|'fail'|'unknown'",
        "detail": "string?"
      },
      "space": "SystemResources?",
      "extensions": [
        {
          "subject": "string (project name, or `.env`)",
          "phpVersion": "string",
          "extension": "string",
          "detail": "string",
          "isDefaultSet": "bool"
        }
      ],
      "extensionsNote": "**Added after reading `tools/validate-contracts.mjs`'s output instead of its summary line.** Three of that tool's four errors on this checkout are C-06 — all one root cause, `imap` removed in PHP 8.2, sitting in two project manifests AND in the stack's default extension set — and every one is local and fixable. The desktop app reported none of them, because the doctor knew about ports, disk and hosts and nothing about extensions. It matters more than a lint: the Bash generator SKIPS A BAD EXTENSION SILENTLY, so nothing fails and nothing is logged, and the symptom is a fatal `Call to undefined function imap_open()` at runtime with nothing connecting it to a build that reported success. `isDefaultSet` entries come first because they are the worse case — a project created right now, with nothing customised, cannot build. The default PHP version is itself contested (DEFAULT_PHP_VERSION vs SUPPORTED_LANGUAGES_PHP_DEFAULT, CONFLICTS.md C-12), so BOTH are checked rather than one being picked: an extension failing on either is a problem whichever key wins. Project rows reuse manifest::normalize's own C-06/C-07 findings rather than restating the rule.",
      "note": "ports.requiredBy is the compose service that publishes the port, read from the generated files — which is the enabled set, because the generator only writes enabled services. ports.state: ok = free or held by the stack itself; warn = a docker backend holds it but the engine could not say whose container; fail = a named conflict. generated.state: warn = an input (.env or a manifest) is newer than the oldest generated compose file, detail names it; fail = never generated."
    },
    "DumpsStatus": {
      "available": "bool",
      "configured": "bool",
      "binary": "string",
      "address": "string",
      "runtime": "string",
      "note": "**There is no on/off switch, and that is deliberate.** The overlay sets VAR_DUMPER_FORMAT and VAR_DUMPER_SERVER for every eligible project and leaves them set, because with the variables set and nothing listening `dump()` renders into the response exactly as it does today — ServerDumper falls back, verified in a running container. A state file for a setting with no observable off-state would be ceremony. Two things had to hold for this to reach a WEB request rather than only the CLI, and both were checked in the container: the official PHP image's docker.conf sets `clear_env = no`, so the container environment reaches FPM workers, and `variables_order` is EGPCS — the E is what puts it in $_SERVER, which is where VarDumper looks."
    },
    "Endpoint": {
      "uri": "string",
      "host": "string",
      "port": "number",
      "note": "`host` is 127.0.0.1 for the host address and the container name for the internal one. Both are in the URI already; they are carried separately so a client that takes a host and a port in two fields can be filled in without parsing the string back apart."
    },
    "EngineStatus": {
      "reachable": "bool",
      "version": "string?",
      "apiVersion": "string?",
      "context": "string?",
      "platform": "'docker-desktop'|'colima'|'orbstack'|'engine'|'unknown'",
      "socketPath": "string?",
      "error": "string?"
    },
    "FanoutStream": {
      "streamId": "string",
      "followed": "number",
      "total": "number",
      "projects": "number",
      "note": "`followed` < `total` means the 60-file cap bit and the oldest-modified files are not being watched. Reported rather than trimmed silently: a view that caps its own coverage and says nothing reads as \"nothing else is happening\"."
    },
    "FinishedEvent": {
      "operationId": "string",
      "subject": "string",
      "success": "bool",
      "durationMs": "u64",
      "error": "string?",
      "logPath": "string?",
      "note": "On failure `error` carries the last few output lines rather than a generic exit-code message — that tail is almost always the real cause."
    },
    "Flame": {
      "frames": "Frame[] — folded stacks; a function under two callers is two frames",
      "total": "number — microseconds accounted for, which is the width of the root row",
      "records": "number — entry and exit records read",
      "stacks": "number — distinct stacks the file held",
      "truncated": "bool — the file was longer than the reader's ceiling; this is the start of the request",
      "pruned": "number — paths too thin to draw, dropped and counted rather than silently missing",
      "depthCapped": "bool — the stack went deeper than 64 frames",
      "note": "F-3. Microseconds always, computed by this app from the trace's own timestamps — unlike a cachegrind report, whose unit is whatever the file declares and MUST be read from it. A trace costs far more than a profile to record (two lines per function call, hundreds of megabytes for one request), which is why the mode is trigger-started like profiling and why the tree is pruned before it crosses this boundary."
    },
    "Frame": {
      "name": "string",
      "value": "number",
      "children": "Frame[]",
      "recursive": "bool",
      "note": "One box in the flame view, and the type is recursive because a call tree is. `value` is the cost of this BRANCH in the file's own time unit. `recursive` is true when this function is already on the path above it — the walk stopped there, which is different from a branch that cost nothing."
    },
    "GeneratorReport": {
      "files": "[{ file, path, status: 'match'|'differ'|'missing'|'error', firstDifferenceLine?, error? }]",
      "matched": "u32",
      "differed": "u32",
      "readyToTakeOver": "bool",
      "warnings": "string[] — configuration problems the port can see and StackVo does not report, e.g. C-20"
    },
    "HandoverInstance": {
      "id": "string",
      "service": "string",
      "version": "string",
      "ports": "map<string, u16>",
      "volumes": "map<string, string>"
    },
    "HandoverNote": {
      "kind": "string (resolvedMovingTag | portMoved | adoptedVolume | settingHasNoHome | unknownService | versionNotInstalled | nothingToInstall | noFreePort). `versionNotInstalled` is NOT 'unpublished': the catalogue handover reads is the local package tree, so the honest sentence is 'not installed here', and the two ask for different things — one is a click in the Market, the other is a version that was never in the index.",
      "subject": "string",
      "detail": "string"
    },
    "HandoverPreview": {
      "pending": "bool",
      "migrated": "bool",
      "instances": "HandoverInstance[]",
      "notes": "HandoverNote[]",
      "blockers": "HandoverNote[]",
      "backup": "bool",
      "missing": "MissingPackage[] — packages the handover needs and this machine does not have"
    },
    "HookPlan": {
      "event": "string — post-build | post-start | pre-stop",
      "steps": "HookStep[]",
      "digest": "string? — what an approval would be recorded against; absent when the project has no host steps",
      "note": "B-3. A plan, not a result: it says what would run for one event and what would not, before anything is spawned — the same review-then-apply shape as hosts_plan/hosts_apply and a preset import. A refused step is NAMED here rather than silently skipped at run time, because “nothing happened and nothing said why” is indistinguishable from a hook that ran and did nothing."
    },
    "HookStep": {
      "kind": "string — exec | host",
      "command": "string — display only; nothing parses it back",
      "blocked": "string? — policy-off | policy-host | needs-consent; absent means it runs"
    },
    "HostStats": {
      "cpu": "CpuStats",
      "memory": "MemoryStats",
      "storage": "StorageStats",
      "network": "NetworkStats",
      "disk": "DiskStats",
      "timestamp": "u64"
    },
    "HostsEntry": {
      "ip": "string",
      "domain": "string",
      "configured": "bool",
      "managedByStackvo": "bool"
    },
    "HostsOverview": {
      "entries": "HostsEntry[]",
      "stale": "string[]",
      "note": "`stale` names domains inside StackVo's own block that nothing serves any more. Only managed lines are ever offered: a hand-written entry belongs to whoever wrote it, and a tool that tidies away lines it did not write is one nobody trusts with the file again."
    },
    "HostsPlan": {
      "add": "string[]",
      "remove": "string[]",
      "preview": "string",
      "changed": "bool",
      "path": "string",
      "note": "`preview` is the exact text that will be written — the same value the diff is rendered from, so what the user approves is what lands."
    },
    "HtmlCheck": {
      "supported": "f64",
      "partial": "f64",
      "unsupported": "f64",
      "tests": "u32",
      "warnings": [
        {
          "title": "string",
          "category": "string",
          "found": "u32",
          "supported": "f64",
          "partial": "f64",
          "unsupported": "f64"
        }
      ],
      "note": "Percentages are of the client/feature combinations Mailpit tests (186 on the version measured against)."
    },
    "IdleProject": {
      "project": "string",
      "router": "string",
      "seconds": "number? — since the last request; absent when the log has never mentioned this router",
      "suspendable": "bool",
      "note": "I-2. Container CPU is not the signal and neither is the network counter: php-fpm sits near zero whether it is serving or asleep, and network moves for health checks and DNS — one mistake stops something in use, the other never stops anything. The honest answer is the proxy's, which wrote down when it last routed to each router. A project the log has NEVER mentioned is not suspendable: that is the state of a stack that just came up, and stopping everything the first time somebody enables this would be the worst possible introduction to it."
    },
    "Install": {
      "source": "'xampp' | 'laragon' | 'mamp' | 'valet' | 'sail'",
      "path": "string — the installation root, or for Valet/Sail the directory that was pointed at",
      "sites": "Array<{ name: string, path: string, domain: string | null, bytes: number, partial: boolean, detected: Detected, taken: boolean, services?: string[] }>",
      "note": "L. `domain` is what the OTHER tool serves the site at, when the other tool says so: Laragon writes a vhost with a ServerName, Valet derives one from its own tld, and a Sail project's .env may carry an APP_URL. XAMPP and MAMP have nothing to read, so it is null and adoption asks. Sail's localhost defaults are treated as no domain rather than imported as one."
    },
    "InstancePlan": {
      "id": "string — the id this would take",
      "refused": "string? — why a second instance is not allowed, or null",
      "settings": "InstanceSetting[] — the manifest's, with defaults as values and secrets unmasked",
      "ports": "DeclaredPort[] — host is what the allocator would choose, or null when it could find nothing"
    },
    "InstanceRow": {
      "id": "string — the slug of service@version",
      "service": "string",
      "version": "string",
      "enabled": "boolean",
      "primary": "boolean — holds the pre-package network alias",
      "container": "string",
      "aliases": "string[]",
      "ports": "Record<handle, number>",
      "packagePresent": "boolean"
    },
    "InstanceSetting": {
      "key": "string — as the manifest spells it, no SERVICE_<ID>_ prefix",
      "kind": "string — the manifest's type: string | secret | int | bool | enum | instanceRef",
      "value": "string — masked when secret",
      "secret": "bool",
      "isDefault": "bool — the value in force is the manifest's own default",
      "defaultValue": "string? — the manifest's own default, so the form can put it back; null for a secret, whose default is never handed out unasked",
      "required": "bool — instance_apply_settings refuses to empty one of these",
      "options": "string[] — an offer, not a constraint. For an instanceRef it is the installed instances answering the capability it names",
      "label": "Record<locale, string>"
    },
    "LanProject": {
      "name": "string",
      "host": "string?",
      "note": "`host` is null when the project asked to be shared and this machine has no address to build a name from — the switch is on and there is nothing to show for it, which is a state the screen has to be able to say out loud."
    },
    "LanStatus": {
      "address": "string | null",
      "suffix": "string",
      "projects": "LanProject[]",
      "stale": "string | null",
      "note": "`address` is null both when there is no network and when this machine's address is PUBLIC — the second is a refusal, because handing out `shop.<public-ip>.sslip.io` would publish a development site under a name anybody can resolve. `suffix` is reported so the screen can say where the name comes from instead of showing a domain nobody recognises. `stale` is a name already written into a compose file or certificate that this machine would no longer produce — a laptop that changed networks — which is the one thing a derived address cannot tell you by being derived."
    },
    "LandingStatus": {
      "running": "bool",
      "container": "string",
      "url": "string",
      "rendered": "string | null",
      "projects": "number",
      "services": "number",
      "note": "`rendered` is an RFC 3339 timestamp, or null if the page has never been written. `running` and `rendered` are independent: the sidecar serves a FILE, so the container can be up while the page it serves is stale."
    },
    "LinkCheck": {
      "errors": "u32",
      "links": [
        {
          "url": "string",
          "status": "string",
          "statusCode": "u16"
        }
      ]
    },
    "LocalOverride": {
      "text": "string",
      "exists": "bool",
      "applied": "string[]",
      "refused": "string[]",
      "ignored": "bool?",
      "note": "B-2. `stackvo.local.json` is this machine's overrides for a committed manifest. `applied` is the dotted paths it currently supplies and `refused` the keys it may not carry (`name`, `runtime`) — both read back from the same merge the renderer runs, so the screen cannot disagree with what actually runs. `ignored` is git's own answer to whether the file would stay out of a commit, and is null when git had no answer: no git, or not a repository, which is not the same as “not ignored”."
    },
    "LogFile": {
      "id": "string",
      "label": "string",
      "group": "'application'|'server'",
      "bytes": "u64",
      "modified": "i64? (unix seconds)",
      "note": "`id` is `<root>:<relative path>` — `app:` for files the code wrote under projects/<name>, `server:` for files the stack wrote under logs/projects/<name>. It is a handle, not a path: it is what app_log_open takes, and it is the reason no absolute path is ever accepted from the frontend. `modified` is epoch seconds so the date is formatted in the user's locale rather than the backend's."
    },
    "MailAttachment": {
      "partId": "string",
      "fileName": "string",
      "contentType": "string",
      "size": "u64"
    },
    "MailBody": {
      "text": "string?",
      "html": "string?",
      "headers": "[{ \"name\": \"string\", \"value\": \"string\" }]",
      "note": "`headers`: every header flattened to one row per value, sorted by name; Mailpit serves them from a dedicated route, MailHog inlines them.",
      "attachments": "MailAttachment[]",
      "size": "u64?"
    },
    "MailMessage": {
      "id": "string",
      "from": "string",
      "to": "string[]",
      "cc": "string[]",
      "bcc": "string[]",
      "replyTo": "string[]",
      "subject": "string",
      "date": "string?",
      "snippet": "string?",
      "read": "bool",
      "note": "`date` is passed through unparsed: the UI formats it in the user's locale, and the two servers disagree about which field it lives in, not about the format. `snippet` is null on MailHog, which does not send one — deriving it from the raw body would render MIME boundaries as preview text. `bcc` is populated only by Mailpit, which derives it from the SMTP envelope; MailHog cannot tell a Bcc from a To, so there it is empty rather than guessed. MailHog's `cc`/`replyTo` come from headers as one joined string per entry."
    },
    "MailStatus": {
      "available": "bool",
      "kind": "'mailhog'|'mailpit'|null",
      "service": "string?",
      "enabled": "bool",
      "running": "bool",
      "uiUrl": "string?",
      "total": "number",
      "unread": "number?",
      "error": "string?",
      "note": "`unread` is null rather than 0 on MailHog, which has no concept of a read message — a zero would render as \"all caught up\" on a server that cannot know. `available` is false when the checkout has neither catcher at all, which is different from having one that is switched off."
    },
    "Manifest": {
      "name": "string",
      "domain": "string | null",
      "runtime": "string",
      "server": "string | null",
      "documentRoot": "string | null",
      "aliases": "string[]",
      "lanShare": "bool",
      "services": "string[]",
      "php": "{ version: string, extensions: string[], xdebug: boolean } | null",
      "node": "{ version: string, install: string, build: string | null, start: string, port: number, packageManager: string | null } | null",
      "lang": "{ version: string, install: string | null, build: string | null, start: string, port: number } | null",
      "valid": "bool",
      "errors": "ManifestFinding[]",
      "warnings": "ManifestFinding[]",
      "hooks": "Record<string, unknown>",
      "commands": "Record<string, unknown>",
      "sidecars": "Record<string, unknown>",
      "local": "string[]",
      "note": "`stackvo.json` as the READER returns it: the file's fields plus this app's verdict on them. ProjectManifest is the file itself, defined by project.schema.json — the difference is `valid`, `errors`, `warnings` and `local`, none of which are ever written. `runtime` is canonical and absent-means-php. `aliases` are lower-cased, de-duplicated and never contain `domain`; a leading `*.` is the one entry that reaches the certificate and the router and cannot reach /etc/hosts. `hooks`, `commands` and `sidecars` are keyed maps whose values are defined by project.schema.json; `hooks` and `commands` are omitted entirely when empty. `local` names the dotted fields this machine's stackvo.local.json supplied and is empty for a manifest read straight from the committed file — `project_manifest_write` refuses a manifest whose `local` is non-empty, which is what stops one developer's overrides being saved into the file the team shares."
    },
    "ManifestFinding": {
      "code": "string",
      "path": "string",
      "message": "string",
      "note": "`path` is the location inside the manifest, so a finding can be shown against the field it is about rather than at the top of the form."
    },
    "MarketPackage": {
      "service": "string",
      "category": "string",
      "name": "Record<locale, string>",
      "summary": "Record<locale, string>",
      "capabilities": "string[]",
      "keywords": "string[] — search terms the index publishes, so mysql is findable by typing 'database' and by typing 'mariadb'",
      "multiple": "boolean — whether two versions may run at once",
      "versions": "MarketVersion[]"
    },
    "MarketStatus": {
      "fetched": "boolean — false before the first refresh",
      "sequence": "number | null",
      "generatedAt": "string | null",
      "expires": "string | null",
      "sourceKind": "string | null — `local` or `https`",
      "sourceLocation": "string | null",
      "packages": "number",
      "installed": "number — version directories on this machine",
      "signed": "boolean — whether signatures are being checked; false until a key is pinned",
      "signatureRequired": "boolean — whether policy.market.requireSignature is set. Reported apart from `signed` because the pair is the story: required and not happening is a refusal, and it is the state a managed machine is in until ADR 0015's key exists.",
      "offlineBundle": "string | null — the bundle policy.market.offlineBundle points at, which wins over the path the user chose. ADR 0011 makes this the only way an air-gapped machine ever gets a catalogue.",
      "constrained": "boolean — whether a policy says anything about the market at all, so the page can explain a refusal before it happens"
    },
    "MarketVersion": {
      "version": "string",
      "recommended": "boolean — what `latest` resolves to",
      "support": "'supported' | 'deprecated' | 'eol'",
      "eolDate": "string | null",
      "sizeBytes": "number | null",
      "installed": "boolean",
      "inUse": "boolean — an instance names it"
    },
    "MemoryStats": {
      "total": "number",
      "used": "number",
      "free": "number",
      "available": "number",
      "percent": "number",
      "swapTotal": "number",
      "swapUsed": "number",
      "note": "`free` is `total - used` and is the only figure that can sit beside `used` in one control without the two contradicting each other. `available` is what a new process could actually get, which on macOS and Linux is MORE than `free` — page cache is reclaimable and is counted in `used`. Drawing a donut from used+available once filled it to 60% while the label in its middle said 78%, on numbers that were both true."
    },
    "Migration": {
      "source": "string (compose file path)",
      "appService": "string?",
      "runtime": "'php'|'node'|null",
      "server": "string?",
      "phpVersion": "string?",
      "nodeVersion": "string?",
      "documentRoot": "string?",
      "domain": "string?",
      "port": "number?",
      "extensions": "string[]",
      "services": [
        {
          "id": "string (catalog id)",
          "version": "string?",
          "from": "string (compose service)",
          "image": "string"
        }
      ],
      "unmapped": "string[]",
      "evidence": "string[]",
      "note": "`version` is null when the tag does not state one — `latest`, `alpine` and `stable` yield nothing rather than a pin, because importing `latest` as a version invents a decision the source file deliberately did not make. Image matching is on the repository with registry and namespace stripped, so docker.elastic.co/elasticsearch/elasticsearch and elasticsearch are one entry, and it is exact rather than fuzzy: a project running ghcr.io/acme/redis-shim is not running Redis, and guessing that it is enables a service nobody asked for. A registry port is not a tag — localhost:5000/mysql has no version. Two compose services mapping to one catalog id collapse to one, or the same .env key would be planned twice with different versions. `evidence` names what each conclusion was read from, for the same reason Detected carries it: a document root inferred wrongly produces a project that builds, starts and serves a 404."
    },
    "MigrationPlan": {
      "migration": "Migration",
      "spec": "ProjectSpec",
      "env": "PresetPlan",
      "alreadyManaged": "bool",
      "note": "`spec` is detection with the compose file layered on top, and the order is the point: detection reads the CODE and gets runtime, framework and document root; the compose file records what its author DECIDED — version, domain, extensions, services. Where both have an answer the compose file wins, because a guess loses to a declaration. `env` is the service enablement expressed as the same reviewed diff a preset import shows, rather than a second way of saying \"turn these on\"."
    },
    "MissingPackage": {
      "service": "string",
      "version": "string",
      "installable": "boolean — whether the cached index offers it, which decides whether the UI can offer a button or only an explanation"
    },
    "Mount": {
      "source": "string?",
      "destination": "string",
      "kind": "string?",
      "note": "`source` is a host path, or a volume name when Docker manages the storage — the two are not distinguishable from the string alone, which is why `kind` is reported beside it."
    },
    "NetworkStats": {
      "rxTotal": "number",
      "txTotal": "number",
      "rxRate": "number",
      "txRate": "number",
      "note": "Totals are cumulative since boot, unlike DiskStats — the counters have different origins and a UI that labelled both “since start” would be wrong about one of them. The rates are bytes per second across the gap."
    },
    "OperationId": "string",
    "PackageReport": {
      "service": "string",
      "version": "string",
      "dir": "string",
      "resealed": "string[] — files whose hash the manifest had wrong",
      "problems": "string[] — everything sealing cannot fix",
      "note": "C-1. A manifest states the sha256 of every file the package ships and pkg::verify checks them on every read, so editing a compose fragment leaves a package that refuses to load with a message about bytes rather than about the line somebody just edited. That bookkeeping — not the JSON — is what made authoring a package impractical. `resealed` is what sealing changed (or would change, from a lint); `problems` is what it refused. A report with problems means NOTHING was written: sealing is bookkeeping, not a way past the validator."
    },
    "PerfLayer": {
      "path": "string — project-relative directory",
      "enabled": "bool — whether the setting says it lives in a volume",
      "volume": "string — the named volume, stackvo-cache-<project>--<path>",
      "exists": "bool — whether that volume exists on the engine",
      "bytes": "number? — what it holds, when the engine can say",
      "onHost": "bool — whether the host still has a copy, which is what an editor indexes",
      "hostFiles": "number? — files in the host copy, counted up to a cap",
      "note": "I-1. Measured with examples/perf_layer_bench.rs on the machine this shipped from: vendor in a volume is 3.8x on a framework boot and does NOTHING for writes; storage/framework is what buys the writes, at 2.8x. That is why this is a list of directories rather than one 'make it fast' switch — the win depends on which directory moves, and a single switch would be a guess about somebody's project. NOT a synchronising layer: nothing on the host writes these directories, so nothing needs reconciling. A bidirectional sync (Mutagen's job) is deliberately not built here — see src-tauri/src/perf.rs for why a worse copy of it would be a liability rather than a feature."
    },
    "PhpIniStatus": {
      "supported": "bool",
      "exists": "bool",
      "path": "string",
      "containerPath": "string",
      "mounted": "bool?",
      "running": "bool",
      "values": "Record<string, string>",
      "unmanaged": "Record<string, string>",
      "needsRecreate": "bool",
      "warning": "string?",
      "effective": "Record<string, string>?",
      "overlayPath": "string",
      "effectiveNote": "What PHP in the RUNNING container reports right now, from one `docker exec php -r ini_get`. Null when nothing is running. Read rather than assumed because assuming was wrong: measured on a real stack, `php -i` says `Loaded Configuration File => (none)` — the official images ship no php.ini at all, so conf.d is not one layer among several but the only one — and `max_execution_time` is 0 under FPM, not the 30 the manual lists. The form uses these as placeholders, so it never shows a default the container does not have; it is also where an override is confirmed to have landed after a restart.",
      "note": "`values` holds the four directives the form manages (memory_limit, upload_max_filesize, post_max_size, max_execution_time); `unmanaged` holds everything else the file contains, which is preserved verbatim on every write — the file is the user's and is safe to hand-edit and commit. `containerPath` is /usr/local/etc/php/conf.d/zz-stackvo.ini: every PHP image the generator builds on (php:X-fpm, php:X-apache, php:X-cli for Swoole, and FrankenPHP, itself built on the official image) parses conf.d after its own php.ini, and `zz` sorts after the docker-php-ext-*.ini files the build writes, so what is set here is what PHP ends up with. Mounted read-only. `warning` is a legal but near-certainly-unintended combination — post_max_size below upload_max_filesize silently caps uploads at the smaller number, which is the classic \"I raised the limit and it still fails\"."
    },
    "PolicyMarket": {
      "constrained": "boolean — whether the policy says anything about the market at all",
      "registryUrl": "string | null — a mirror the organisation runs; read by the network source (Faz 5)",
      "offlineBundle": "string | null — a directory to install from with no network. ADR 0011 makes this the only way an air-gapped machine gets a catalogue, so it is a first-class install path rather than an enterprise extra",
      "requireSignature": "boolean — the one key in the block that is a lock rather than a note: it can only turn verification ON, and there is no value of it that turns a check off (ADR 0009). Raises market::Trust to Signed, which refuses today because no key is pinned",
      "allowedPackages": "string[] — services that may be installed. EMPTY MEANS NO OPINION, not 'none'",
      "allowedRegistries": "string[] — registries an image may come from; a reference with no host counts as docker.io. Empty means no opinion",
      "autoUpdate": "boolean | null",
      "additionalKeys": "number — how many extra signing keys were supplied, not which"
    },
    "Port": {
      "container": "u16",
      "host": "u16?",
      "protocol": "'tcp'|'udp'"
    },
    "Preferences": {
      "schemaVersion": "number",
      "locale": "'tr' | 'en' | null",
      "theme": "'light' | 'dark' | 'system'",
      "editorCommand": "string | null",
      "terminalApp": "string | null",
      "browserCommand": "string | null",
      "startMinimized": "bool",
      "closeBehaviour": "'ask' | 'quit' | 'tray'",
      "autostart": "bool",
      "notifyOnBuild": "bool",
      "backupSchedule": "string",
      "backupKeep": "number",
      "favourites": "string[]",
      "appearance": "Record<string, unknown>",
      "appearancePresets": "Record<string, unknown>[]",
      "note": "The whole preferences document, as `default_prefs` writes it. Rust never interprets `appearance`, `appearancePresets` or `favourites` — they are stored and handed back, which is why they have no shape here: giving them one would be this contract claiming to know something the code does not check. `theme` is the legacy top-level key and is carried into `appearance` on read rather than reset, so a user who chose light before the move does not silently lose it. `prefs_set` takes a PATCH, not this: it merges, and a caller that sent the whole document would overwrite keys a newer version of the app added."
    },
    "Preflight": {
      "os": "'macos'|'windows'|'linux'",
      "ready": "bool",
      "requirements": [
        {
          "id": "'workspace'|'engine'|'compose'|'network'|'projects'|'bash'|'mkcert'",
          "state": "'ok'|'warn'|'fail'|'unknown'",
          "detail": "string?",
          "fixable": "bool"
        }
      ],
      "note": "`detail` is what the machine said — a version, a path, the daemon's own error — and is deliberately not translated. `ready` ignores warnings: mkcert reports `warn` rather than `fail` because a stack without HTTPS still runs, it just serves a browser warning."
    },
    "Preset": {
      "kind": "'stackvo.preset'",
      "version": "number",
      "name": "string?",
      "description": "string?",
      "services": "Record<string, { enabled: bool, version?: string }>",
      "settings": "Record<string, string>",
      "note": "`kind` and `version` are what make this a file format rather than \"some JSON\": `kind` is checked on load so a package.json is refused instead of read as an empty preset, and a `version` above what the app understands is refused rather than half-read — a newer preset may mean something different by a field this version already has, and applying the half we recognise writes a stack that matches neither machine. A service entry has exactly two fields; that is the mechanism by which no secret can be in one. `settings` is limited to DEFAULT_TLD_SUFFIX, SSL_ENABLE, DEFAULT_PHP_VERSION, DEFAULT_NODE_VERSION and DEFAULT_WEBSERVER, and the limit is re-checked on import — trusting the export side alone would let a hand-edited preset write any key it liked into somebody else's .env."
    },
    "PresetPlan": {
      "name": "string?",
      "description": "string?",
      "changes": [
        {
          "key": "string (.env key)",
          "kind": "'service'|'setting'",
          "subject": "string",
          "from": "string?",
          "to": "string"
        }
      ],
      "rejected": "string[]",
      "unchanged": "number",
      "needsRegenerate": "bool",
      "note": "`from` is null when the key is absent from .env entirely, which is different from present-and-empty. `unchanged` exists so \"nothing to do\" is distinguishable from \"everything was rejected\" — an empty `changes` list alone is ambiguous between the two, and they need opposite responses from the user. `kind` lets the UI group without parsing the key."
    },
    "ProfileFile": {
      "id": "string (cachegrind.out.*)",
      "bytes": "number",
      "modified": "i64? (unix seconds)",
      "compressed": "bool",
      "note": "`compressed` is read from the file's magic bytes. The overlay turns Xdebug's compression off, but a file written under somebody else's ini — or before profiling was turned on here — can still be gzipped, and the UI says so rather than the parser failing."
    },
    "ProfileReport": {
      "creator": "string",
      "cmd": "string",
      "events": "string[]",
      "summary": "number[]",
      "selfTotal": "number",
      "functions": [
        {
          "name": "string",
          "file": "string",
          "selfTime": "number",
          "selfMemory": "number",
          "inclusiveTime": "number",
          "calls": "number",
          "percent": "number"
        }
      ],
      "functionCount": "number",
      "truncated": "bool",
      "note": "`events` carries the units the file declares — do not assume microseconds. `summary` is the file's own total and is LARGER than `self_total`; the two are reported separately rather than conflated, and `percent` is a share of `self_total` so the column adds to 100. `functionCount` is how many distinct functions the file held before the top-60 cut. Measured on real output: 200,004 blocks aggregated to 5 functions, 10 MB parsed in under a second."
    },
    "ProfilerStatus": {
      "xdebug": "XdebugStatus",
      "mode": "'debug'|'profile'|'trace'",
      "trigger": "string (XDEBUG_TRIGGER)",
      "profiles": "ProfileFile[]",
      "traces": "ProfileFile[] — read by profiler_flame, not profiler_read",
      "bytes": "number — profiles AND traces; they share a directory and a disk",
      "directory": "string",
      "note": "Layered on XdebugStatus rather than duplicating it: the extension has to be compiled in for any mode, so `needsRebuild` and `active` mean exactly what they already meant. `mode` is one value and not a set because the three want different start triggers — stepping connects on the next request, profiling and tracing wait for a trigger so an idle stack does not fill a disk. `trace` is the third and closes F-3: it writes stacks, which is what a flame graph is built from, where a profile writes summed edges. The two lists are separate because they open different views. IMPLEMENTORS: xdebug.output_dir MUST exist before either mode records — Xdebug does not create it and writes nothing, silently, when it is missing."
    },
    "ProgressEvent": {
      "operationId": "string",
      "subject": "string",
      "line": "string",
      "note": "One line of a running process's merged stdout+stderr. Merged deliberately: docker compose writes progress to stderr and results to stdout, and separating them makes the log read out of order."
    },
    "Project": {
      "name": "string",
      "domain": "string",
      "runtime": "'php'|'node'",
      "path": "string",
      "containerName": "string",
      "running": "bool",
      "built": "bool",
      "manifest": "ProjectManifest",
      "manifestValid": "bool",
      "domainConfigured": "bool",
      "generatedStale": "bool",
      "ports": "Port[]",
      "git": "Checkout | null",
      "note": "`manifestValid` is hoisted out of `manifest` so a list view does not have to dig; the findings themselves stay on `manifest.errors`, which is the only place they exist — this type used to declare a `manifest_errors: string[]` beside it that no payload has ever carried. `generatedStale` is measured from the manifest's mtime against the oldest of its generated outputs, NOT accumulated from `manifest:changed` events: the watcher cannot tell whose write it saw, so the app writing a manifest during project_create used to raise a “needs regenerating” badge on a project it went on to generate half a second later. Derived, it is also right on first load, which an event-driven flag never was for an edit made while the app was closed. `git` is null for a directory that was never versioned; `Some` with no `remote` is local history and no upstream, and the two are different answers to where a project came from."
    },
    "ProjectLogFile": {
      "project": "string",
      "...": "LogFile",
      "note": "A LogFile with the project it belongs to. The id alone does not identify a file across the workspace — every Laravel project has an `app:storage/logs/laravel.log` — so the cross-project surface carries the pair, and app_log_open still takes them separately."
    },
    "ProjectManifest": {
      "$ref": "project.schema.json"
    },
    "ProjectSpec": {
      "$ref": "project.schema.json",
      "note": "The create form's payload IS a project manifest. The old POST body used a flat shape (runtime, version, extensions at top level) that ProjectService then reassembled — and reassembled wrongly for node. v1 sends the manifest itself, so there is nothing to reassemble."
    },
    "PruneReport": {
      "imagesDeleted": "u64",
      "volumesDeleted": "u64",
      "spaceReclaimed": "u64"
    },
    "PtyTarget": {
      "oneOf": [
        {
          "kind": "'container'",
          "name": "string",
          "shell": "string?"
        },
        {
          "kind": "'host'",
          "cwd": "string"
        }
      ]
    },
    "PushPlan": {
      "tag": "string",
      "registry": "string? — the host the tag names",
      "possible": "bool",
      "refused": "string? — why not",
      "authenticated": "bool? — whether this registry is in the user's Docker config; null means the answer could not be read, which is NOT “not logged in”",
      "warnings": "string[]",
      "note": "H-1. An image is pushed only after it has been verified: a registry keeps layers, deleting a tag does not remove what was in it, and on a shared registry somebody has already pulled it. A tag naming no registry is refused too — `docker push myapp:v1` goes to Docker Hub under whichever account is logged in. Credentials are not this app's business: `docker login` is the user's, exactly as ~/.ssh/config is."
    },
    "QueryEntry": {
      "at": "number",
      "sql": "string",
      "shape": "string",
      "note": "`shape` is the statement with the parts that vary between iterations of a loop replaced by placeholders, so `WHERE id = 1` and `WHERE id = 2` are one shape. Two statements formatted differently are two shapes, which is honest: they came from different code."
    },
    "QueryLogSession": {
      "recording": "bool",
      "supported": "bool",
      "entries": "QueryEntry[]",
      "repeats": "QueryRepeat[]",
      "note": "`supported` is false rather than an error for anything that keeps no statement log — Redis, RabbitMQ, Memcached. The screen asks every database, and “this kind cannot” must not read as “something broke”. `repeats` holds shapes seen more than three times, most repeated first; three rather than two because two identical queries is a page reading a row and reading it again, and the ones that matter in practice are twenty and two hundred."
    },
    "QueryRepeat": {
      "shape": "string",
      "count": "number",
      "example": "string"
    },
    "QuickCommand": {
      "id": "string",
      "display": "string",
      "about": "string",
      "interactive": "bool",
      "because": "string (the marker file)",
      "note": "`display` is the command as typed, so what runs and what is shown cannot drift. `interactive` decides where it goes — the user's terminal, or the operation console. `id` is the only thing quick_command_run accepts."
    },
    "RelayConfig": {
      "enabled": "bool",
      "host": "string",
      "port": "number",
      "username": "string",
      "security": "'starttls' | 'tls' | 'none'",
      "from": "string",
      "allowedRecipients": "string[]",
      "note": "What `mail_relay_set` accepts. The password is never in here — it goes to the OS keystore through the command's own `password` argument. `from` is the envelope sender: providers reject a From they do not own, and the error comes back from the relay as a number. `allowedRecipients` empty means anywhere, which is Mailpit's own default; it is offered because a development stack that can email any address somebody types is one typo away from a real customer."
    },
    "RelayStatus": {
      "enabled": "bool",
      "host": "string",
      "port": "number",
      "username": "string",
      "security": "'starttls' | 'tls' | 'none'",
      "from": "string",
      "allowedRecipients": "string[]",
      "hasPassword": "bool",
      "keystore": "bool",
      "note": "RelayConfig as it is stored, plus the two things a caller cannot know from what it sent: whether a password is held (`hasPassword`) and whether the OS keystore is the thing holding it (`keystore`). The same shape `mail_relay_get` returns, which is why `mail_relay_set` returns it — a setter that answered `void` would leave the pane redrawing from what it hoped it wrote."
    },
    "ReleasePlan": {
      "strategy": "'layer'|'retag'",
      "baseImage": "string",
      "tag": "string",
      "dockerfile": "string (empty for retag)",
      "excluded": "[pattern, reason][]",
      "warnings": "string[]",
      "appPath": "string",
      "runtime": "string",
      "note": "`dockerfile` is shown before it is built — plan-then-apply, like the hosts file, the certificate and a preset import. A production image is the one artefact here that leaves the machine, so reviewing it is not optional politeness."
    },
    "ReleaseResult": {
      "plan": "ReleasePlan",
      "verification": "{ envFiles: string[], xdebugActive: bool?, hasApp: bool, clean: bool }",
      "note": "Read out of the BUILT image by running it, not inferred from the Dockerfile. `xdebugActive` is null for a node image — there is no PHP to ask, which is not the same as \"no\". `clean` is all three checks passing."
    },
    "ReplRun": {
      "runner": "string",
      "display": "string",
      "stdout": "string",
      "stderr": "string",
      "exitCode": "int?",
      "ms": "int",
      "timedOut": "bool",
      "truncated": "bool",
      "limited": "bool",
      "note": "Success is `exit_code == 0`, never \"stderr is empty\" — a PHP fatal goes to stdout. `limited` false means the in-container limit was not available, so the snippet may still be running in there after this app stopped waiting; the pane says so rather than letting a leak look like a clean stop. Streams are cut at 256 KiB from the FRONT, because a stack trace's useful half is its last lines."
    },
    "ReplRunner": {
      "id": "string",
      "display": "string (the command, snippet excluded)",
      "language": "'php'|'python'|'ruby'|'javascript'",
      "booted": "bool",
      "about": "string",
      "because": "string (the marker file)",
      "note": "`display` is the command as typed with the snippet left off, so what runs and what is shown cannot drift. `id` is the only thing repl_run accepts. `booted` is the difference between User::count() meaning something and meaning nothing."
    },
    "ReplSnippet": {
      "at": "int (unix seconds)",
      "runner": "string",
      "code": "string",
      "note": "The code, never the output — see repl_history."
    },
    "Service": {
      "id": "string — the instance id (mysql-8-0) on the market model, the service id before it",
      "category": "string",
      "enabled": "bool — whether it should run, which is not whether it is running",
      "running": "bool",
      "built": "bool — a container exists, running or not",
      "version": "string?",
      "containerName": "string",
      "url": "string? — the whole domain, suffix included",
      "health": "string? — 'healthy' | 'unhealthy' | 'starting'; null when the image declares no healthcheck",
      "hostPort": "u16?",
      "ports": "Port[]",
      "declaredPorts": "DeclaredPort[] — every port the package declares, by handle, with the host number in force; empty before the migration",
      "aliases": "string[] — the Docker network names this answers to, its own first; the second is the pre-package alias",
      "support": "string? — 'supported' | 'deprecated' | 'eol', from the version manifest",
      "eolDate": "string? — ISO date upstream support ends or ended",
      "companions": "CompanionRow[] — containers shipped with this instance and not separately installable",
      "credentials": "Credential[]",
      "required": "DependencyRow[]",
      "optional": "DependencyRow[]",
      "unmetDependencies": "string[] — the subject of every required dependency that is unprovided or stopped"
    },
    "SessionId": "string",
    "SiteSettings": {
      "env": "Record<string, string>",
      "directoryListing": "bool",
      "sshAgent": "bool",
      "listingSupported": "bool",
      "agentAvailable": "bool",
      "server": "string",
      "note": "What `site_settings` reads and `site_save` gives back, so the pane redraws from what was stored rather than from what it hoped it wrote. `listingSupported` and `agentAvailable` are about THIS machine and this project's server, not about the settings — a switch that cannot take effect is shown disabled rather than accepted and ignored."
    },
    "SourceProbe": {
      "location": "string — exactly what was asked for",
      "resolved": "string — where the bytes were actually fetched from; differs when a GitHub repository URL was translated",
      "kind": "string — `local` or `https`",
      "reachable": "boolean",
      "packages": "number",
      "versions": "number",
      "sequence": "number | null",
      "generatedAt": "string | null",
      "expires": "string | null",
      "currentSequence": "number | null — the cached index's sequence, when there is one",
      "goesBackwards": "boolean — this index is older than the one already here, which market_refresh refuses (T-6, replay)",
      "error": "string | null",
      "hintKey": "string | null"
    },
    "StatSample": {
      "t": "u64",
      "cpu": "f32",
      "memory": "f32"
    },
    "StorageStats": {
      "total": "number",
      "used": "number",
      "available": "number",
      "percent": "number",
      "mountPoint": "string"
    },
    "StreamId": "string",
    "SystemResources": {
      "images": {
        "total": "u32",
        "inUse": "u32",
        "unused": "u32",
        "size": "u64"
      },
      "volumes": {
        "total": "u32",
        "inUse": "u32",
        "unused": "u32",
        "size": "u64"
      }
    },
    "Timeline": {
      "moments": "TimelineMoment[]",
      "requests": "string[]",
      "queriesRecording": "bool",
      "note": "Oldest first: a timeline read downward is a request read forward. `requests` names the requests the dumps mentioned, in the order they first appear. `queriesRecording` false means the database half is ABSENT rather than empty — without it an unrecorded window and a window with no queries are the same picture."
    },
    "TimelineMoment": {
      "at": "number",
      "source": "'dump' | 'query' | 'mail'",
      "summary": "string",
      "request": "string | null",
      "shape": "string | null",
      "note": "`request` is the request id a dump named, null for anything the bridge did not attribute. `shape` is set on query moments only and is the same normalisation QueryEntry.shape uses, so repeats line up across the two."
    },
    "TunnelStatus": {
      "project": "string",
      "running": "bool",
      "url": "string?",
      "container": "string"
    },
    "UpdateInfo": {
      "available": "bool",
      "version": "string?",
      "notes": "string?"
    },
    "UpdaterOffer": {
      "offer": "{ outcome: 'update' | 'upToDate' | 'paused' | 'waiting' | 'otherChannel', detail?: unknown }",
      "channel": "'stable' | 'beta'",
      "currentVersion": "string",
      "bucket": "number",
      "note": "`detail` is the version for `update`, `{ bucket, percent }` for `waiting`, and the other channel's name for `otherChannel`. `bucket` at the top level is this install's hundredth for the manifest's version — returned so somebody who did not get an update they expected can compute `bucket < percent` themselves rather than guess."
    },
    "UserRoute": {
      "domain": "string",
      "target": "string — http:// or https://, origin only",
      "enabled": "bool",
      "rewrittenFrom": "string? — what the user typed, when normalisation changed it",
      "notes": "string[] — true and not errors",
      "error": "string? — set instead of `notes` when the route no longer normalises",
      "note": "E-4. Traefik routes every project and every catalogue service and nothing else, so a dev server somebody started by hand has no way in. The whole difficulty is one string: `http://localhost:3000` is read INSIDE Traefik's container, where localhost is Traefik — the config loads, the browser gets a 502, and nothing says why. So localhost and 127.0.0.1 are rewritten to host.docker.internal and the screen says so; refusing them would be defensible and useless, because it is the address people have. Checked on the way out rather than stored checked, so a route written today does not keep today's answer after the rule changes."
    },
    "ValidationReport": {
      "valid": "bool",
      "errors": [
        {
          "path": "string",
          "code": "string",
          "message": "string"
        }
      ],
      "warnings": [
        {
          "path": "string",
          "message": "string"
        }
      ]
    },
    "WorkerStatus": {
      "project": "string",
      "kind": "'queue'|'scheduler'|'horizon'",
      "running": "bool",
      "restarts": "i64?",
      "container": "string"
    },
    "Workspace": {
      "root": "string?",
      "valid": "bool",
      "source": "'stored'|'env'|'discovered'|'none'",
      "stackvoVersion": "string?",
      "projectsDir": "string?",
      "envFile": "string?",
      "note": "root is null when source is 'none'. `source` exists so the UI can say WHY it is pointing at a given checkout — silently guessing wrong would be worse than asking."
    },
    "Worktree": {
      "name": "string — the project name, which is also the directory name, and the identity everything else keys off",
      "parent": "string — the project it was branched from",
      "branch": "string — the git branch checked out into it",
      "domain": "string — a subdomain of the parent's, so it stays inside any wildcard route or certificate the parent has",
      "path": "string — absolute, and recorded rather than derived: the project tree can be moved, and a derived path would send `git worktree remove` somewhere git has never heard of",
      "database": "{ instance: string, name: string, seededFrom?: string } | null",
      "env": "Record<string,string> — what somebody typed. The database credentials are NOT here; they are derived from the instance on every render",
      "createdAt": "string — RFC 3339",
      "note": "N. Stored in <root>/worktrees.json, outside every checkout, because the files in a worktree directory are the branch's and this app writes nothing there that git tracks. `seededFrom` is the only record of whether a branch started with a copy of the data or an empty schema, which is the question somebody asks three weeks later."
    },
    "WorktreePlan": {
      "parent": "string",
      "branch": "string",
      "newBranch": "bool — whether the branch would be created rather than checked out",
      "name": "string",
      "path": "string",
      "domain": "string",
      "database": "{ instance: string, service: string, name: string, seed: bool, source?: string } | null",
      "warnings": "string[] — what proceeds anyway",
      "refused": "string | null — one sentence naming why it cannot",
      "possible": "bool",
      "note": "Carries what it WOULD have produced even when refused: a dialog that blanks out on refusal makes the reason harder to act on, not easier."
    },
    "WorktreeRow": {
      "...": "every field of Worktree, flattened",
      "exists": "bool — is the directory still there",
      "dirty": "bool | null — uncommitted work a removal would discard. null when git could not answer, which is a third state and not a warning",
      "orphaned": "bool — the record points somewhere git no longer has a checkout, usually because the folder was deleted by hand"
    },
    "WorktreeSupport": {
      "gitAvailable": "bool",
      "repository": "bool — is the project's directory a git repository at all",
      "linked": "bool — is it itself a linked worktree, as git sees it. Different from `record`, which means this app made it",
      "record": "Worktree | null",
      "effectiveEnv": "Record<string,string> | null — what this worktree's container is actually given, present only when `record` is. DB_PASSWORD is masked, and so is the same password inside DATABASE_URL",
      "domain": "string | null — what a new worktree's hostname would be built under",
      "currentBranch": "string | null — null for a detached head",
      "branches": "Array<{ name: string, checkedOut: bool, current: bool }> — newest commit first",
      "instances": "DbInstance[] — stopped ones included and marked",
      "reason": "string | null — why worktrees are unavailable here",
      "worktrees": "WorktreeRow[] — this project's own"
    },
    "XdebugStatus": {
      "supported": "bool",
      "enabled": "bool",
      "active": "bool?",
      "needsRebuild": "bool",
      "running": "bool",
      "port": "number",
      "mode": "string",
      "ideKey": "string",
      "serverName": "string?",
      "hostPath": "string?",
      "containerPath": "string",
      "phpVersion": "string?",
      "peclVersion": "string?",
      "overlayPath": "string",
      "note": "Three layers, deliberately not collapsed into one boolean. `enabled` is what the manifest asks for; `needsRebuild` is true while the image predates that ask, because extensions are compiled in and a toggle that looked done but changed nothing would be worse than no toggle; `active` is what the running container actually carries, and is null when there is no container to read. `hostPath` and `containerPath` are the two halves of the IDE's path mapping — the mapping is the step people get wrong, and both sides are already known here."
    }
  },
  "coverage": {
    "httpEndpointsReplaced": 24,
    "socketEventsReplaced": 4,
    "newCommands": 30,
    "unreachableRemoved": [
      "POST /api/terminal/:c/open returned HTTP 400 unconditionally in the shipped container configuration — replaced by terminal_open_external, which works."
    ],
    "phase3": "Tray, notifications, fs-watcher, hosts helper, PTY, autostart and single-instance. The hosts helper replaces the README's manual `sudo tee -a /etc/hosts` step; the PTY replaces a terminal endpoint that returned HTTP 400 unconditionally in the shipped container configuration.",
    "implementationStatus": "Suite E of tools/validate-contracts.mjs cross-checks this file against the Rust registry in src-tauri/src/lib.rs and the JS wrappers in src/lib/ipc.js. A command declared here but not registered shows up as a NOT_IMPLEMENTED warning, so this list cannot quietly drift ahead of the code."
  }
}
