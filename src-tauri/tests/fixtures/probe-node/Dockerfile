# Auto-generated by Stackvo — standalone Node.js runtime
# Project: probe-node
FROM node:22-alpine

WORKDIR /app

# Copy project source (node_modules excluded via .dockerignore)
COPY . .

# Install dependencies
RUN npm install

# Build for production
ENV NODE_ENV=production
RUN npm run build

# Network configuration (Traefik proxies to this port)
ENV HOST=0.0.0.0
ENV PORT=3000
EXPOSE 3000

# Start the application
CMD ["sh", "-c", "node .output/server/index.mjs"]
